---
phase: 8
plan: 8.2
type: execute
wave: 2
depends_on:
  - 8.1
files_modified:
  - package.json
  - package-lock.json
  - electron-builder.yml
  - .github/workflows/release.yml
  - .planning/phases/08-upstream-sync-v1-1-0/evidence/build/*
autonomous: true
requirements:
  - REQ-001
  - REQ-018
  - REQ-024
  - REQ-025
---

# Plan 8.2: Build, Dependency, And Packaging Migration

<objective>
Resolve dependency/build/packaging changes from upstream `v1.1.0` first, then restore deterministic install, build, and typecheck before deeper contract and renderer conflict work.
</objective>

<threat_model>
## Threat Model

- **High:** Lockfile/package conflicts can hide accidental dependency drift. Mitigation: resolve from upstream `v1.1.0` plus fork requirements, then run deterministic install.
- **Medium:** Packaging config changes may break packaged app behavior while source build passes. Mitigation: preserve upstream packaging fixes and mark packaged smoke conditional for final verification.
</threat_model>

<tasks>
<task id="8.2.1" type="execute">
<read_first>
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
- `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
- `.planning/phases/08-upstream-sync-v1-1-0/08-PATTERNS.md`
- `.planning/phases/08-upstream-sync-v1-1-0/evidence/MERGE-STATE.md`
- `package.json`
- `package-lock.json`
- `electron-builder.yml`
- `.github/workflows/release.yml`
</read_first>
<action>
Continue from the single in-progress `git merge --no-ff v1.1.0` started by Plan 8.1. Do not run another merge, pull, rebase, squash, or cherry-pick. Resolve package/build/packaging conflicts first, then `git add` those resolved files while leaving unrelated conflict groups for later plans. Preserve upstream packaging fixes including asarUnpack scope and release/electron-builder updates while retaining local scripts required for FlashQuery unit and E2E suites.
</action>
<verify>
Run `git rev-parse -q --verify MERGE_HEAD`, `git diff --name-only --diff-filter=U`, and `npm ci` or the repository's deterministic install equivalent after package conflict resolution.
</verify>
<acceptance_criteria>
- `MERGE_HEAD` still exists after Plan 8.2; the merge has not been committed.
- `package.json` retains scripts for `build`, `typecheck`, `test`, `test:e2e`, and any existing smoke commands.
- `package-lock.json` is regenerated/resolved consistently with `package.json`.
- `npm ci` or equivalent exits 0.
</acceptance_criteria>
</task>

<task id="8.2.2" type="execute">
<read_first>
- `package.json`
- `tsconfig.json`
- `electron.vite.config.ts`
- `src/main/index.ts`
- `src/preload/index.ts`
</read_first>
<action>
Restore green source build and typecheck after dependency/build conflict resolution. Fix only build/type errors needed for the post-merge tree to compile; defer semantic FlashQuery contract and renderer reconciliation to Plans 8.3 and 8.4 unless a compile error blocks progress.
</action>
<verify>
Run `npm run build` and `npm run typecheck`.
</verify>
<acceptance_criteria>
- `npm run build` exits 0.
- `npm run typecheck` exits 0, or any remaining typecheck failure is explicitly documented as blocked on a central contract file owned by Plan 8.3.
- Evidence under `.planning/phases/08-upstream-sync-v1-1-0/evidence/build/` records command output and exit codes.
</acceptance_criteria>
</task>

<task id="8.2.3" type="execute">
<read_first>
- `.planning/phases/08-upstream-sync-v1-1-0/08-VALIDATION.md`
- `.planning/phases/08-upstream-sync-v1-1-0/evidence/baseline/`
- `.planning/phases/08-upstream-sync-v1-1-0/evidence/build/`
</read_first>
<action>
Record the Phase 8.2 cumulative exit gate. Mark `T-A-003` and `T-A-004` with the command outputs. If packaging/Electron dependencies changed, note that `T-A-011` packaged smoke is required in Plan 8.6.
</action>
<verify>
Inspect the evidence notes for `T-A-002`, `T-A-003`, `T-A-004`, and conditional `T-A-011`.
</verify>
<acceptance_criteria>
- Evidence notes include `T-A-002 phase 8.2 exit gate`, `T-A-003`, and `T-A-004`.
- Any intentional build/typecheck deviation is documented with reason and owner plan.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm ci` or equivalent.
- `npm run build`.
- `npm run typecheck`.
- Phase 8.2 `T-A-002` exit gate evidence.
</verification>

<success_criteria>
- Build/dependency/packaging conflicts no longer block shared contract work.
- Upstream packaging fixes are retained.
- Deterministic install, build, and typecheck are green or any remaining issue is explicitly scoped to Plan 8.3.
</success_criteria>
