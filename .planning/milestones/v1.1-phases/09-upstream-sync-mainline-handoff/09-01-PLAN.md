---
phase: 9
plan: 9.1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/*
autonomous: false
requirements:
  - REQ-021
  - REQ-023
  - REQ-026
must_haves:
  truths:
    - "Downstream agents read the upstream-sync requirements and test plan before handoff work."
    - "The Phase 8 evidence chain exists before mainline handoff starts."
    - "The handoff includes Phase 8 gap-closure commits 918e3d7 and 184a7ca."
    - "The mainline handoff preserves the real upstream merge commit without rebase, squash, or cherry-pick."
  artifacts:
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md"
      provides: "Source-doc acknowledgement, Phase 8 evidence inventory, branch state, and handoff decision record"
  key_links:
    - from: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md"
      to: "docs/UPSTREAM-SYNC.md"
      via: "tracked runbook and sync ledger verification"
      pattern: "v1.1.0"
    - from: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md"
      to: ".planning/phases/08-upstream-sync-v1-1-0/08-VERIFICATION.md"
      via: "Phase 8 final evidence preflight"
      pattern: "Status: Passed"
---

# Plan 9.1: Handoff Preflight And Fast-Forward

<objective>
Read the upstream-sync source documents, verify the Phase 8 evidence chain, and hand off `sync/upstream-v1.1.0` to mainline only if the operation can preserve the existing upstream merge history without conflict resolution or history rewriting.
</objective>

<threat_model>
## Threat Model

- **High:** A handoff can accidentally rewrite, squash, cherry-pick, or obscure the real upstream merge required by REQ-026. Mitigation: use only a fast-forward handoff; stop if not possible.
- **High:** Agents can skip the product docs and weaken product-defined gates. Mitigation: record explicit source-doc acknowledgement in preflight evidence.
- **Medium:** Dirty worktree state can mix unrelated changes into the handoff. Mitigation: require clean status before branch operations.
</threat_model>

<tasks>
<task id="9.1.1" type="execute">
  <name>Task 1: Record source-doc and evidence preflight</name>
  <files>.planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/08-VERIFICATION.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/08-VALIDATION.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/evidence/`
  - `docs/UPSTREAM-SYNC.md`
  </read_first>
  <action>
  Create `.planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md` recording that the two product docs were read, Phase 8 verification is passed, required Phase 8 evidence paths exist, `docs/UPSTREAM-SYNC.md` is tracked, and the worktree/branch state is known before handoff.
  Record the current sync branch tip and explicitly confirm that commits `918e3d7` and `184a7ca` are ancestors of the handoff branch.
  </action>
  <verify>
    <automated>mkdir -p .planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight && test -f .planning/phases/08-upstream-sync-v1-1-0/08-VERIFICATION.md && test -d .planning/phases/08-upstream-sync-v1-1-0/evidence && git ls-files docs/UPSTREAM-SYNC.md | grep -q '^docs/UPSTREAM-SYNC.md$'</automated>
    <automated>test -z "$(git status --short)"</automated>
    <automated>git branch --show-current</automated>
    <automated>git merge-base --is-ancestor 918e3d7 HEAD && git merge-base --is-ancestor 184a7ca HEAD</automated>
  </verify>
  <done>Preflight notes explicitly acknowledge the upstream-sync requirements and test plan as first-read sources, list Phase 8 evidence, confirm the Phase 8 gap-closure commits are included, and show a clean handoff starting state.</done>
</task>

<task id="9.1.2" type="execute">
  <name>Task 2: Fast-forward mainline to the verified sync branch</name>
  <files>.planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `.planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md`
  - `docs/UPSTREAM-SYNC.md`
  </read_first>
  <action>
  Switch to `main`, update it with `git pull --ff-only`, verify `main` is an ancestor of `sync/upstream-v1.1.0`, then run `git merge --ff-only sync/upstream-v1.1.0`. If any step would require conflict resolution, rebase, squash, cherry-pick, or a non-fast-forward merge, stop and ask the user.
  </action>
  <verify>
    <automated>test -z "$(git status --short)"</automated>
    <automated>git merge-base --is-ancestor main sync/upstream-v1.1.0</automated>
    <automated>bash -c 'line=$(git log --merges --format="%H%x09%P%x09%s" --grep="Merge upstream v1.1.0" -1); test -n "$line"; parents=$(printf "%s" "$line" | cut -f2); subject=$(printf "%s" "$line" | cut -f3); set -- $parents; test "$#" -eq 2 && test "$2" = "5b6549d661a8427c829f60e15c4de9e71d49ac4d" && printf "%s" "$subject" | grep -q "Merge upstream v1.1.0"'</automated>
  </verify>
  <done>Mainline points at the verified sync branch by fast-forward only, with the existing upstream merge commit still visible in history and no release work claimed.</done>
</task>
</tasks>

<verification>
- `git status --short`
- `git merge-base --is-ancestor main sync/upstream-v1.1.0`
- `git log --merges --format='%H %P %s' -5`
</verification>

<success_criteria>
- Handoff preflight evidence exists and names the two product source docs.
- The handoff uses a fast-forward path only.
- The real upstream `v1.1.0` merge commit remains in repository history.
- The plan stops instead of inventing a strategy if main has diverged.
</success_criteria>
