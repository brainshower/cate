---
phase: 13
plan: 13.2
type: execute
wave: 2
depends_on:
  - 13.1
files_modified:
  - .planning/phases/13-release-readiness-provenance-closeout/evidence/provenance/NOTES.md
  - docs/UPSTREAM-SYNC.md
autonomous: true
requirements:
  - REQ-021
  - REQ-023
  - REQ-026
must_haves:
  truths:
    - "Process and provenance gates are rechecked on the current tree."
    - "Runbook edits must append or clarify; they must not erase prior sync-ledger history."
    - "The `.planning/` tracking check is blocking."
  artifacts:
    - path: ".planning/phases/13-release-readiness-provenance-closeout/evidence/provenance/NOTES.md"
      provides: "T-A-012..T-A-015 command outputs, runbook verdicts, tracking verdicts, and blockers"
---

# Plan 13.2: Provenance, Runbook, And Tracking Gates

<objective>
Re-run the final upstream-sync process gates for conflict-review notes, `.planning/` tracking, runbook completeness, and Git provenance.
</objective>

<threat_model>
## Threat Model

- **High:** A future sync becomes harder if the runbook or ledger is missing or rewritten. Mitigation: verify `docs/UPSTREAM-SYNC.md` is tracked and contains runbook, protected surfaces, and ledger history.
- **High:** Provenance can be broken by squash/rebase or a non-merge handoff. Mitigation: re-run ancestor, merge-base, behind-count, and merge-commit checks.
- **Medium:** Broad ignore rules can accidentally drop planning history. Mitigation: inspect `.gitignore` and tracked `.planning/` count.
</threat_model>

<tasks>
<task id="13.2.1" type="verify">
  <name>Task 1: Re-run T-A-013 and T-A-015 repository gates</name>
  <files>.planning/phases/13-release-readiness-provenance-closeout/evidence/provenance/NOTES.md</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `.planning/phases/09-upstream-sync-mainline-handoff/09-VERIFICATION.md`
  - `.planning/phases/13-release-readiness-provenance-closeout/13-CONTEXT.md`
  - `.gitignore`
  - `docs/UPSTREAM-SYNC.md`
  </read_first>
  <action>
  Create `evidence/provenance/NOTES.md`. Record source-doc acknowledgement and paste or summarize the exact current outputs for `.planning/` tracked count, `.gitignore` broad `.claude/` guard, `git merge-base --is-ancestor v1.1.0 HEAD`, `git merge-base HEAD v1.1.0`, `git rev-list --count HEAD..v1.1.0`, and the latest upstream merge commit metadata.
  </action>
  <verify>
    <automated>mkdir -p .planning/phases/13-release-readiness-provenance-closeout/evidence/provenance</automated>
    <automated>git ls-files .planning | wc -l | tr -d ' ' | awk '$1 >= 113 { exit 0 } { exit 1 }'</automated>
    <automated>! grep -Eq '^\\.claude/?$|^\\.claude/\\*$' .gitignore</automated>
    <automated>git merge-base --is-ancestor v1.1.0 HEAD</automated>
    <automated>test "$(git merge-base HEAD v1.1.0)" = "5b6549d661a8427c829f60e15c4de9e71d49ac4d"</automated>
    <automated>test "$(git rev-list --count HEAD..v1.1.0)" = "0"</automated>
    <automated>bash -c 'line=$(git log --merges --format="%H%x09%P%x09%s" --grep="Merge upstream v1.1.0" -1); test -n "$line"; parents=$(printf "%s" "$line" | cut -f2); subject=$(printf "%s" "$line" | cut -f3); set -- $parents; test "$#" -eq 2 && printf "%s" "$parents" | grep -q "5b6549d661a8427c829f60e15c4de9e71d49ac4d" && printf "%s" "$subject" | grep -q "Merge upstream v1.1.0"'</automated>
  </verify>
  <acceptance_criteria>
  - `NOTES.md` contains T-A-013 and T-A-015 verdicts with command outputs.
  - `.planning/` tracked count remains at or above the established baseline.
  - Provenance still proves the fork's effective base is `v1.1.0`.
  </acceptance_criteria>
  <done>Repository tracking and upstream provenance gates are current.</done>
</task>

<task id="13.2.2" type="audit">
  <name>Task 2: Audit runbook and conflict-review coverage</name>
  <files>.planning/phases/13-release-readiness-provenance-closeout/evidence/provenance/NOTES.md, docs/UPSTREAM-SYNC.md</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `docs/UPSTREAM-SYNC.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/evidence/contracts/CONFLICT-REVIEW.md`
  - `.planning/phases/10-shared-contracts-audit/evidence/contracts/NOTES.md`
  - `.planning/phases/11-renderer-behavior-audit/evidence/renderer/NOTES.md`
  </read_first>
  <action>
  Audit T-A-012 and T-A-014. Confirm `docs/UPSTREAM-SYNC.md` is tracked and contains the standard flow, protected FlashQuery surfaces, conflict hotspots, verification matrix, E2E traceability, process gates, and sync ledger. Confirm central conflict-review evidence exists for the product doc's central files. If the runbook is incomplete, update it narrowly; do not rewrite the existing ledger line.
  </action>
  <verify>
    <automated>git ls-files docs/UPSTREAM-SYNC.md | grep -q '^docs/UPSTREAM-SYNC.md$'</automated>
    <automated>for term in "Sync Ledger" "Standard Flow" "Protected FlashQuery Surfaces" "Conflict Hotspots" "Verification Matrix" "E2E Traceability" "Process Gates"; do grep -q "$term" docs/UPSTREAM-SYNC.md || exit 1; done</automated>
    <automated>for file in "preload/index.ts" "shared/types.ts" "shared/ipc-channels.ts" "shared/electron-api.d.ts" "appStore.ts" "session.ts" "EditorPanel.tsx" "Sidebar.tsx" "DockTabBar.tsx" "e2e/fixtures/electron-app.ts" "main/index.ts"; do rg -q "$file" .planning/phases/08-upstream-sync-v1-1-0 .planning/phases/10-shared-contracts-audit .planning/phases/11-renderer-behavior-audit || exit 1; done</automated>
    <automated>for id in T-A-012 T-A-014 REQ-019 REQ-023; do grep -q "$id" .planning/phases/13-release-readiness-provenance-closeout/evidence/provenance/NOTES.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
  - T-A-012 and T-A-014 have current pass/fail verdicts.
  - Any runbook edits are narrow clarifications or additions.
  - Prior sync-ledger entries remain intact.
  </acceptance_criteria>
  <done>Runbook, surface inventory, ledger, and conflict-review gates are current.</done>
</task>
</tasks>

<verification>
- `git ls-files .planning | wc -l`
- `.gitignore` broad `.claude/` guard
- `git merge-base --is-ancestor v1.1.0 HEAD`
- `git merge-base HEAD v1.1.0`
- `git rev-list --count HEAD..v1.1.0`
- `git log --merges --grep="Merge upstream v1.1.0" -1`
- `git ls-files docs/UPSTREAM-SYNC.md`
</verification>

<success_criteria>
- T-A-012 through T-A-015 pass or have explicit blockers.
- `docs/UPSTREAM-SYNC.md` remains tracked and complete.
- `.planning/` tracking and upstream provenance remain queryable on the current tree.
</success_criteria>
