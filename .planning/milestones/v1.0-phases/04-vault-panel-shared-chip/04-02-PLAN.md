---
phase: 04-vault-panel-shared-chip
plan: 02
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - src/shared/types.ts
  - src/shared/panels.ts
  - src/shared/panels.test.ts
  - src/renderer/panels/registry.ts
  - src/renderer/panels/registry.test.ts
  - src/renderer/stores/appStore.ts
  - src/renderer/stores/appStore.test.ts
autonomous: true
requirements:
  - REQ-014
must_haves:
  truths:
    - "User can create or open a `flashqueryVault` panel through Cate's standard panel infrastructure."
    - "The panel has the locked FlashQuery Vault label, brand color, sizing, and canvas support."
  artifacts:
    - path: "src/shared/types.ts"
      provides: "`flashqueryVault` PanelType and size map support"
    - path: "src/shared/panels.ts"
      provides: "`PANEL_DEFINITIONS.flashqueryVault`"
    - path: "src/renderer/panels/registry.ts"
      provides: "`PANEL_REGISTRY.flashqueryVault` lazy renderer entry"
    - path: "src/renderer/stores/appStore.ts"
      provides: "`createFlashQueryVault` factory"
  key_links:
    - from: "src/renderer/panels/registry.ts"
      to: "src/renderer/stores/appStore.ts"
      via: "useAppStore.getState().createFlashQueryVault(workspaceId, canvasPoint, placement) || null"
      pattern: "createFlashQueryVault\\(workspaceId, canvasPoint, placement\\)"
---

<objective>
Register the `flashqueryVault` panel across shared metadata, renderer registry, and app-store creation.

Purpose: REQ-014 requires Cate's normal panel system to know the new FlashQuery Vault panel type.
Output: Shared panel definition, renderer registry entry, app-store factory, and unit coverage T-U-051..054.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
@.planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
@.planning/phases/04-vault-panel-shared-chip/04-04-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md

<interfaces>
From `04-CONTEXT.md`: D-09 locks `flashqueryVault` label, colors, `tintClass`, sizing, canvas support, and ghost SVG. D-10 locks the renderer registry icon/lazy component/factory. D-11 locks `createFlashQueryVault` as a mirror of `createFileExplorer`.

From `04-PATTERNS.md`: `src/shared/panels.ts` has `SharedPanelDefinition`; `src/renderer/panels/registry.ts` extends it with icon, component, and create factory; `src/renderer/stores/appStore.ts` has `createFileExplorer` as the direct factory analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared panel definition and tests</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/shared/types.ts
    src/shared/panels.ts
    src/shared/panels.test.ts
  </read_first>
  <files>src/shared/types.ts, src/shared/panels.ts, src/shared/panels.test.ts</files>
  <behavior>
    - T-U-051: `PANEL_DEFINITIONS.flashqueryVault` exists with required fields and `canLiveOnCanvas: true`.
    - T-U-052: `brandColor` and `switcherColor` are both `#5AD8B8`.
  </behavior>
  <action>Add `'flashqueryVault'` to `PanelType` and any explicit `Record<PanelType, Size>` maps such as `PANEL_CANVAS_DROP_SIZES`. Add `PANEL_DEFINITIONS.flashqueryVault` with `type: 'flashqueryVault'`, `label: 'FlashQuery Vault'`, `brandColor: '#5AD8B8'`, `switcherColor: '#5AD8B8'`, `mutedColor: '#4a9080'`, `tintClass: 'text-teal-400'`, file-explorer-like `defaultSize` and `minimumSize` using the product-suggested 320x500 default where it fits the existing pattern, `canLiveOnCanvas: true`, and a vault-themed teal ghost SVG. Extend or create `src/shared/panels.test.ts` to assert the exact locked fields.</action>
  <acceptance_criteria>
    - `PanelType` accepts `flashqueryVault` without casts.
    - `PANEL_DEFINITIONS.flashqueryVault.label === 'FlashQuery Vault'`.
    - `brandColor` and `switcherColor` exactly equal `#5AD8B8`; `mutedColor` exactly equals `#4a9080`; `tintClass` exactly equals `text-teal-400`.
    - `canLiveOnCanvas` is `true`, and default/minimum sizing follows the file explorer pattern.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/shared/panels.test.ts</automated>
  </verify>
  <done>Shared FlashQuery Vault panel metadata satisfies REQ-014.1 and T-U-051..052.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add renderer registry entry and tests</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/renderer/panels/FlashQueryVaultPanel.tsx
    src/renderer/panels/registry.ts
    src/renderer/panels/registry.test.ts
    src/renderer/stores/appStore.ts
  </read_first>
  <files>src/renderer/panels/registry.ts, src/renderer/panels/registry.test.ts</files>
  <behavior>
    - T-U-053: `PANEL_REGISTRY.flashqueryVault` exists with the expected lazy component reference, `Vault` icon, and app-store factory.
  </behavior>
  <action>Import `Vault` from `@phosphor-icons/react`, add `const FlashQueryVaultPanel = React.lazy(() => import('./FlashQueryVaultPanel'))`, and add `flashqueryVault` to `PANEL_REGISTRY` using `...PANEL_DEFINITIONS.flashqueryVault`, `icon: Vault`, `Component: FlashQueryVaultPanel`, and `create: ({ workspaceId, canvasPoint, placement }) => useAppStore.getState().createFlashQueryVault(workspaceId, canvasPoint, placement) || null`. Add or extend `registry.test.ts` to assert the entry is present and that the factory delegates to `createFlashQueryVault` with the exact argument order.</action>
  <acceptance_criteria>
    - Registry entry key is exactly `flashqueryVault`.
    - Icon is Phosphor `Vault`.
    - Component is lazy-loaded from `./FlashQueryVaultPanel`.
    - Factory calls `createFlashQueryVault(workspaceId, canvasPoint, placement) || null`, not `createFileExplorer` or any direct panel placement code.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/renderer/panels/registry.test.ts</automated>
  </verify>
  <done>Renderer registry can construct the FlashQuery Vault panel through Cate's normal registry path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add app-store panel factory and tests</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/renderer/stores/appStore.ts
    src/renderer/stores/appStore.test.ts
    src/shared/types.ts
  </read_first>
  <files>src/renderer/stores/appStore.ts, src/renderer/stores/appStore.test.ts</files>
  <behavior>
    - T-U-054: `appStore.createFlashQueryVault(workspaceId)` produces a panel with `type: 'flashqueryVault'`.
  </behavior>
  <action>Add `createFlashQueryVault(workspaceId: string, position?: Point, placement?: PanelPlacement): string` to the app-store action interface and implementation. Mirror `createFileExplorer`: generate an id, create a `PanelState` with `type: 'flashqueryVault'`, `title: 'FlashQuery Vault'`, and `isDirty: false`, insert it into the target workspace, call `placePanel(panelId, 'flashqueryVault', placement, position, workspaceId === get().selectedWorkspaceId)`, rollback the inserted panel and log on placement failure, and return the panel id. Add or extend `appStore.test.ts` to assert the panel type/title and dock/canvas placement delegation where the existing test harness supports it.</action>
  <acceptance_criteria>
    - `useAppStore.getState().createFlashQueryVault` exists and returns a panel id on success.
    - Created panel has `type: 'flashqueryVault'`, `title: 'FlashQuery Vault'`, and `isDirty: false`.
    - The factory uses `placePanel` with panel type `flashqueryVault`; it does not custom-place the panel.
    - On placement failure, the inserted panel is removed from workspace state.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/renderer/stores/appStore.test.ts</automated>
    <automated>npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>REQ-014 and T-U-051..054 are complete without adding Phase 5 workspace menu behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer registry -> app store | Registry factory creates persistent workspace panel state |
| shared metadata -> renderer UI | Shared panel definition drives UI labels, colors, and placement eligibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `createFlashQueryVault` | mitigate | Mirror `createFileExplorer` rollback-on-placement-failure pattern so failed placement does not leave orphaned panel state. |
| T-04-02-02 | Spoofing | `PANEL_DEFINITIONS.flashqueryVault` | mitigate | Exact label/color tests pin the FlashQuery Vault identity and prevent accidental reuse of another panel's metadata. |
| T-04-02-SC | Tampering | npm installs | accept | This plan performs no package-manager installs. |
</threat_model>

<verification>
Run `npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts` and `npm run typecheck`. If the local default Node is outside Cate's `>=20 <23` engine, rerun through Node 22 and record the fallback.
</verification>

<success_criteria>
`flashqueryVault` is fully registered in Cate's shared panel system, renderer registry, and app-store factory with tests T-U-051..054 passing.
</success_criteria>

<output>
Create `.planning/phases/04-vault-panel-shared-chip/04-02-SUMMARY.md` when done.
</output>
