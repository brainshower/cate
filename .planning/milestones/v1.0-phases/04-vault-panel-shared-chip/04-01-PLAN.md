---
phase: 04-vault-panel-shared-chip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/renderer/components/Chip.tsx
  - src/renderer/components/Chip.test.tsx
autonomous: true
requirements:
  - REQ-024
  - REQ-025
  - REQ-026
must_haves:
  truths:
    - "User can see connection status as Connecting, Live, Disconnected, or Unknown in a compact Cate-native chip."
    - "Only a disconnected chip can trigger manual retry."
    - "The chip primitive is reusable outside the vault panel."
  artifacts:
    - path: "src/renderer/components/Chip.tsx"
      provides: "Reusable connection-status chip primitive"
      exports: ["Chip", "ConnectionStatus"]
    - path: "src/renderer/components/Chip.test.tsx"
      provides: "T-I-015..021 and T-U-103 coverage"
    - path: "package.json"
      provides: "React Testing Library dev dependencies"
  key_links:
    - from: "src/renderer/components/Chip.tsx"
      to: "src/renderer/components/Chip.test.tsx"
      via: "RTL render/click/hover assertions"
      pattern: "state=\\{\\{ kind: 'disconnected'"
    - from: "src/renderer/components/Chip.tsx"
      to: "Phase 6 vault badge"
      via: "shared component location"
      pattern: "src/renderer/components/Chip.tsx"
---

<objective>
Create the reusable Phase 4 chip primitive and install the approved component-test utilities required by the product test plan.

Purpose: REQ-024, REQ-025, and REQ-026 require a shared chip with precise status rendering, disconnected retry behavior, and React Testing Library style tests.
Output: `Chip.tsx`, `Chip.test.tsx`, and lockfile updates for `@testing-library/react` / `@testing-library/dom`.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
@.planning/phases/04-vault-panel-shared-chip/04-RESEARCH.md
@.planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md

<interfaces>
From `04-CONTEXT.md`: D-12 through D-15 lock the component location, state API, visual dimensions, and disconnected-only retry behavior.

From product Requirements §6.5.1:
`ConnectionStatus` accepts `{ kind: 'connecting' }`, `{ kind: 'live' }`, `{ kind: 'disconnected'; error?: string }`, and `{ kind: 'unknown' }`; future unknown variants render the unknown visual instead of crashing.

From product Test Plan §4.4.1:
`src/renderer/components/Chip.test.tsx` must cover T-I-015 through T-I-021.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install approved React Testing Library dependencies</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-RESEARCH.md
    package.json
    package-lock.json
    vitest.config.ts
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>Install `@testing-library/react` and `@testing-library/dom` as dev dependencies using npm, per the explicit Phase 4 planning decision and the package legitimacy audit in `04-RESEARCH.md`. Use a supported Node runtime (`>=20 <23`); if local `node` is v23+, run through Node 22 using the established Phase 3 fallback pattern such as `npx -p node@22 npm install --save-dev @testing-library/react @testing-library/dom`. Do not add unrelated test libraries or modify Vitest environment config unless imports fail after installation.</action>
  <acceptance_criteria>
    - `package.json` includes dev dependencies for `@testing-library/react` and `@testing-library/dom`.
    - `package-lock.json` records the installed dependency graph.
    - No runtime dependency entry is added for these test-only packages.
    - No package with `[SLOP]` or `[SUS]` status is introduced; `04-RESEARCH.md` audit marks both packages OK.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies?.['@testing-library/react']||!p.devDependencies?.['@testing-library/dom']) process.exit(1)"</automated>
  </verify>
  <done>React Testing Library is available for Phase 4 jsdom component tests with lockfile changes limited to the approved packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the shared chip primitive and tests</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/renderer/canvas/BulkActionChip.tsx
    src/renderer/sidebar/WorkspaceTab.test.tsx
    src/renderer/styles/globals.css
  </read_first>
  <files>src/renderer/components/Chip.tsx, src/renderer/components/Chip.test.tsx</files>
  <behavior>
    - T-I-015: connecting renders `CircleNotch` spinner and exact label `Connecting…`.
    - T-I-016: live renders a `#34C759` dot and exact label `Live`.
    - T-I-017: disconnected renders a `#FF453A` dot and exact label `Disconnected`.
    - T-I-018: unknown future state such as `{ kind: 'auth-failed' } as any` renders an unknown fallback without throwing.
    - T-I-019/T-I-021: only disconnected uses `cursor: pointer` and fires `onRetry` on click; live/connecting clicks never call `onRetry`.
    - T-I-020: disconnected hover shows `state.error` and `Click to retry`.
    - T-U-103: chip source/rendered class strings contain no forbidden stock neutral color classes: `gray`, `slate`, or `zinc`.
  </behavior>
  <action>Create `src/renderer/components/Chip.tsx` exporting `type ConnectionStatus` and `function Chip(props)` or `const Chip`. The component must implement the locked D-13 API, use an exhaustive `switch` with a default unknown branch, render a 22 px pill with radius 999, `rgba(255,255,255,0.04)` background, 1 px `rgba(255,255,255,0.06)` border, 11 px system font, `#5AD8B8` connecting spinner, `#34C759` live dot, and `#FF453A` disconnected dot. Use Phosphor `CircleNotch` for connecting. The disconnected chip must accept `onRetry?: () => void`, stop propagation on click, show a below-chip tooltip containing the error and `Click to retry`, and apply `cursor: pointer`; live, connecting, and unknown states must be non-interactive. Keep the component reusable for Phase 6 vault badge by isolating the pill surface in this file rather than inlining it into the vault panel. Add `Chip.test.tsx` using React Testing Library and Vitest to cover T-I-015..021 and T-U-103.</action>
  <acceptance_criteria>
    - `src/renderer/components/Chip.tsx` exists and is not defined inside a panel file.
    - Tests assert exact labels `Connecting…`, `Live`, `Disconnected`, unknown fallback, retry click behavior, and disconnected tooltip copy.
    - The component uses Cate semantic classes for text/hover surfaces where classes are needed and only literal product colors for the chip-specific dot/spinner/surface values.
    - `grep -R "gray\\|slate\\|zinc" src/renderer/components/Chip.tsx src/renderer/components/Chip.test.tsx` returns no forbidden rendered stock neutral color class usage; `text-teal-400` is not needed in this component.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/renderer/components/Chip.test.tsx</automated>
    <automated>npm run typecheck</automated>
    <automated>grep -R "gray\\|slate\\|zinc" src/renderer/components/Chip.tsx src/renderer/components/Chip.test.tsx | grep -v "forbidden" | grep -v "stock neutral" && exit 1 || exit 0</automated>
  </verify>
  <done>The chip primitive satisfies REQ-024, REQ-025, REQ-026, T-I-015..021, and T-U-103, and is ready for the vault panel to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer component props | `state` may include future or malformed discriminants from calling code |
| npm install | New dev dependencies enter the local build/test toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Denial of Service | `Chip` state rendering | mitigate | Default unknown branch renders a stable fallback for unrecognized `state.kind` instead of throwing. |
| T-04-01-02 | Tampering | `Chip` retry callback | mitigate | Only disconnected state invokes `onRetry`; tests prove live/connecting clicks are no-ops. |
| T-04-01-SC | Tampering | npm installs | mitigate | Use packages approved in `04-RESEARCH.md` Package Legitimacy Audit; no `[ASSUMED]`, `[SUS]`, or `[SLOP]` packages are installed. |
</threat_model>

<verification>
Run `npm test -- src/renderer/components/Chip.test.tsx` and `npm run typecheck`. If the local default Node is outside Cate's `>=20 <23` engine, rerun verification through Node 22 and record that in the summary.
</verification>

<success_criteria>
REQ-024, REQ-025, and REQ-026 are implemented with focused tests, the shared primitive lives in `src/renderer/components/Chip.tsx`, and no Phase 5 dialog or Phase 6 editor badge work is added.
</success_criteria>

<output>
Create `.planning/phases/04-vault-panel-shared-chip/04-01-SUMMARY.md` when done.
</output>
