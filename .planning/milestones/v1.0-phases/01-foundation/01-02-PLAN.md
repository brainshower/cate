---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/flashquery/credentials.ts
  - src/main/flashquery/credentials.test.ts
autonomous: true
requirements:
  - REQ-002
requirements_addressed: [REQ-002]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03: Downstream implementation reads the external Cate-FlashQuery Requirements and Test Plan before editing or asking questions."
    - "D-04: This plan implements only REQ-002 from the Phase 1 scope."
    - "D-10: FlashQuery bearer-token I/O is isolated behind getWorkspaceToken and setWorkspaceToken in the main process."
    - "D-11: Token storage is backed by electron-store only through credentials.ts so a later OS keychain swap is localized."
    - "D-12: electron-store write failures reject the setWorkspaceToken promise."
    - "D-13: Raw bearer tokens are not exposed to renderer state, logs, planning artifacts, or test snapshots."
    - Unknown workspace tokens return null, stored tokens round-trip, null clears tokens, and workspace tokens are isolated.
  artifacts:
    - src/main/flashquery/credentials.ts exports getWorkspaceToken and setWorkspaceToken with the exact signatures from REQ-002.
    - src/main/flashquery/credentials.test.ts covers T-U-005 through T-U-009.
  key_links:
    - Future FlashQueryClientManager and flashquery:setConnection IPC code must call credentials.ts instead of reading/writing tokens directly.
---

<objective>
Create the main-process FlashQuery bearer-token storage abstraction. Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-10, D-11, D-12, and D-13.

Purpose: Cate needs a single token I/O boundary now so later IPC and manager code cannot leak or duplicate raw secret handling.
Output: `src/main/flashquery/credentials.ts` and focused Vitest coverage for T-U-005 through T-U-009.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
@src/main/store.ts
@src/main/analytics.test.ts
</context>

<threat_model>
Trust boundaries:
- Main process owns token persistence.
- Renderer, workspace JSON, logs, and planning artifacts must not receive real bearer-token values.

STRIDE register:
- threat_id: FQ-P1-04-TOKEN-LEAK
  category: Information disclosure
  component: src/main/flashquery/credentials.ts
  disposition: mitigate
  mitigation_plan: Export only getWorkspaceToken and setWorkspaceToken; keep store naming/private keys inside credentials.ts; tests use dummy values only.
- threat_id: FQ-P1-05-WRITE-FAILURE
  category: Reliability / Tampering
  component: electron-store write path
  disposition: mitigate
  mitigation_plan: Let setWorkspaceToken reject when electron-store set/delete fails; include T-U-009 failure test.
- threat_id: FQ-P1-06-RENDERER-SECRET-ACCESS
  category: Elevation of privilege / Information disclosure
  component: Electron renderer boundary
  disposition: avoid
  mitigation_plan: Do not add preload APIs, renderer imports, or settings-store exposure for raw tokens in Phase 1.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create FlashQuery credential helper module</name>
  <files>src/main/flashquery/credentials.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, .planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-RESEARCH.md, src/main/store.ts</read_first>
  <action>Create `src/main/flashquery/credentials.ts`. Export exactly `getWorkspaceToken(workspaceId: string): Promise<string | null>` and `setWorkspaceToken(workspaceId: string, token: string | null): Promise<void>`. Use the existing `electron-store` ESM dynamic import style from `src/main/store.ts` or an equivalent main-process-safe lazy import. Store tokens under a FlashQuery-specific namespace keyed by workspace ID. When token is `null`, remove/clear that workspace token. Do not export the Store instance or key-building helper unless tests require a reset hook that is explicitly marked test-only.</action>
  <verify>npm test -- src/main/flashquery/credentials.test.ts</verify>
  <acceptance_criteria>
    - `src/main/flashquery/credentials.ts` contains `export async function getWorkspaceToken(workspaceId: string): Promise<string | null>`.
    - `src/main/flashquery/credentials.ts` contains `export async function setWorkspaceToken(workspaceId: string, token: string | null): Promise<void>`.
    - `getWorkspaceToken` returns `null` for missing values rather than `undefined`.
    - `setWorkspaceToken(workspaceId, null)` removes the stored token for that workspace.
  </acceptance_criteria>
  <done>Credential helper API exists with exact REQ-002 signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Add credential helper unit tests</name>
  <files>src/main/flashquery/credentials.test.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, src/main/flashquery/credentials.ts, src/main/analytics.test.ts, src/main/store.ts</read_first>
  <action>Add colocated Vitest tests for T-U-005 through T-U-009. Mock `electron-store` so tests do not write to real userData. Test: unknown workspace returns null; set then get round-trips token string; setting null clears; two workspace IDs keep independent tokens; store write/delete failure makes `setWorkspaceToken` reject. Use dummy token strings such as `test-token-alpha` only.</action>
  <verify>npm test -- src/main/flashquery/credentials.test.ts</verify>
  <acceptance_criteria>
    - Test names or comments reference T-U-005, T-U-006, T-U-007, T-U-008, and T-U-009.
    - The test suite mocks `electron-store` or injects a test store path so no real credential store is touched.
    - The write-failure test asserts `await expect(setWorkspaceToken(...)).rejects`.
    - The tests do not log or snapshot token values outside dummy literals.
  </acceptance_criteria>
  <done>T-U-005 through T-U-009 pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/main/flashquery/credentials.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "getWorkspaceToken|setWorkspaceToken" src/main/flashquery/credentials.ts`
- [ ] `rg -n "test-token|dummy-token" src/main/flashquery/credentials.test.ts`
- [ ] `rg -n "flashquery.*token|token.*flashquery" src/renderer src/preload` does not show renderer/preload token storage introduced by this plan.
</verification>

<success_criteria>
- REQ-002 is fully represented in source and tests.
- T-U-005 through T-U-009 pass.
- Token access is main-process-only and future call sites have a single helper module to use.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
