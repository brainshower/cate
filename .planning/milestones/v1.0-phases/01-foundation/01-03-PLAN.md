---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/main/flashquery/uri.ts
  - src/main/flashquery/uri.test.ts
  - src/main/flashquery/clientManager.ts
  - src/main/flashquery/clientManager.test.ts
autonomous: true
requirements:
  - REQ-003
  - REQ-013
requirements_addressed: [REQ-003, REQ-013]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03: Downstream implementation reads the external Cate-FlashQuery Requirements and Test Plan before editing or asking questions."
    - "D-04: This plan implements only REQ-003 skeleton behavior and REQ-013 from the Phase 1 scope."
    - "D-14: src/main/flashquery/uri.ts exports buildVaultUri and parseVaultUri with the exact signatures from CONTEXT.md."
    - "D-15: buildVaultUri and parseVaultUri implement the exact v1 `flashquery://<workspaceId>/<vault-path>` contract with no vault query component."
    - "D-16: URI helpers preserve literal folder separators while percent-encoding workspace ID and individual path segments, and non-FlashQuery inputs return null."
    - "D-17: URI tests cover the round-trip invariant for spaces, #, ?, %, non-ASCII, CJK, leading/trailing slashes, and empty vault paths."
    - "D-18: src/main/flashquery/clientManager.ts exports FlashQueryClientManager with per-workspace state, subscribe, unsubscribe, and dispose."
    - "D-19: FlashQueryClientManager constructs without eager connections and only prepares lifecycle/subscription scaffolding."
    - "D-20: dispose(workspaceId) releases workspace manager state and immediate unsubscribe is safe."
    - "D-05 D-06: Phase 1 manager code does not probe `/mcp/info`, create MCP clients, register IPC handlers, fetch vault data, or take ownership of FlashQuery runtime/storage."
  artifacts:
    - src/main/flashquery/uri.ts exports buildVaultUri and parseVaultUri.
    - src/main/flashquery/uri.test.ts covers T-U-013 through T-U-020.
    - src/main/flashquery/clientManager.ts exports FlashQueryClientManager.
    - src/main/flashquery/clientManager.test.ts covers T-U-010 through T-U-012.
  key_links:
    - src/main/flashquery/clientManager.ts can import FlashQueryConnection from src/shared/types.ts without introducing renderer or preload dependencies.
    - Future Phase 2 connection code can extend the manager state shape without changing the public subscribe/dispose API created here.
---

<objective>
Create the pure vault URI helper contract and the no-network FlashQueryClientManager lifecycle skeleton. Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-05, D-06, D-14, D-15, D-16, D-17, D-18, D-19, and D-20.

Purpose: Later phases need a canonical vault URI shape and manager lifecycle boundary, but Phase 1 must stop before real transport, IPC, or UI work.
Output: `uri.ts`, `clientManager.ts`, and unit coverage for T-U-010 through T-U-020.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
@src/shared/types.ts
@src/agent/main/agentManager.ts
</context>

<threat_model>
Trust boundaries:
- Vault URIs may originate from renderer state or persisted sessions in later phases.
- Manager subscribers and workspace IDs must remain workspace-scoped.

STRIDE register:
- threat_id: FQ-P1-07-URI-CONFUSION
  category: Spoofing / Tampering
  component: src/main/flashquery/uri.ts
  disposition: mitigate
  mitigation_plan: `parseVaultUri` must validate the exact lowercase `flashquery:` scheme, decode workspace ID/path segments, and return null for non-FlashQuery or malformed inputs.
- threat_id: FQ-P1-08-CROSS-WORKSPACE-EVENT-LEAK
  category: Information disclosure
  component: FlashQueryClientManager subscription map
  disposition: mitigate
  mitigation_plan: Store subscribers by workspace ID and event type; unsubscribe must remove only the registered handler.
- threat_id: FQ-P1-09-EAGER-NETWORK
  category: Elevation of privilege / Reliability
  component: FlashQueryClientManager constructor
  disposition: avoid
  mitigation_plan: Do not import MCP SDK, call fetch, probe `/mcp/info`, create clients, or schedule retry timers in Phase 1.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement vault URI helpers</name>
  <files>src/main/flashquery/uri.ts, src/main/flashquery/uri.test.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, .planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>Create `src/main/flashquery/uri.ts` with `buildVaultUri(workspaceId: string, vaultPath: string): string` and `parseVaultUri(uri: string): { workspaceId: string; vaultPath: string } | null`. `buildVaultUri('ws-abc', 'foo/bar.md')` must return exactly `flashquery://ws-abc/foo/bar.md`. Encode the workspace ID with `encodeURIComponent`. Encode each vault path segment with `encodeURIComponent` and join with literal `/`. Preserve leading/trailing slash behavior per T-U-019 and empty path behavior per T-U-020. Return null for non-FlashQuery inputs and malformed inputs; do not throw for `file:///local/path.md` or `not-a-uri`.</action>
  <verify>npm test -- src/main/flashquery/uri.test.ts</verify>
  <acceptance_criteria>
    - `src/main/flashquery/uri.ts` exports `buildVaultUri` and `parseVaultUri`.
    - `buildVaultUri('ws-abc', 'foo/bar.md')` returns `flashquery://ws-abc/foo/bar.md`.
    - `parseVaultUri('flashquery://ws-abc/foo/bar.md')` returns `{ workspaceId: 'ws-abc', vaultPath: 'foo/bar.md' }`.
    - Tests cover spaces, `#`, `?`, `%`, Latin-1 supplementary characters, CJK characters, leading slash, trailing slash, and empty path.
  </acceptance_criteria>
  <done>T-U-013 through T-U-020 pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement no-network FlashQueryClientManager skeleton</name>
  <files>src/main/flashquery/clientManager.ts, src/main/flashquery/clientManager.test.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, .planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-RESEARCH.md, src/shared/types.ts, src/agent/main/agentManager.ts</read_first>
  <action>Create `src/main/flashquery/clientManager.ts` exporting `FlashQueryClientManager`. The class must construct without network side effects. Add workspace-scoped internal state sufficient for future clients and subscribers. Expose `dispose(workspaceId: string): void` to remove that workspace's state/subscribers. Expose `subscribe(workspaceId: string, eventType: FlashQueryEventType, handler: FlashQueryEventHandler): () => void`; Phase 1 event types may include `'status'` and a future-safe `'vault-changed'`, but no events need to be emitted yet. Keep imports limited to shared types and local types; do not import MCP SDK, fetch, Electron IPC, or credential helpers unless needed only as type-safe future placeholders.</action>
  <verify>npm test -- src/main/flashquery/clientManager.test.ts</verify>
  <acceptance_criteria>
    - `src/main/flashquery/clientManager.ts` contains `export class FlashQueryClientManager`.
    - Constructing `new FlashQueryClientManager()` does not call `fetch`, import MCP SDK, register IPC handlers, or read credentials.
    - `dispose(workspaceId)` removes per-workspace manager state without throwing when called repeatedly.
    - `subscribe(workspaceId, 'status', handler)` returns an unsubscribe function; calling unsubscribe immediately does not crash.
  </acceptance_criteria>
  <done>T-U-010, T-U-011, and T-U-012 pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/main/flashquery/uri.test.ts src/main/flashquery/clientManager.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "fetch\\(|/mcp/info|ipcMain|@modelcontextprotocol|retry|setTimeout" src/main/flashquery/clientManager.ts` returns no Phase 2/3 behavior.
- [ ] `rg -n "buildVaultUri|parseVaultUri|FlashQueryClientManager" src/main/flashquery`
</verification>

<success_criteria>
- REQ-003 skeleton behavior and REQ-013 URI behavior are fully represented in source and tests.
- T-U-010 through T-U-020 pass.
- No network, IPC, MCP transport, retry/backoff, vault list/read/write, or renderer UI behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
