---
phase: 06-editor-uri-awareness-vault-badge
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
requirements:
  - REQ-030
  - REQ-041
  - REQ-042
autonomous: true
must_haves:
  truths:
    - "Vault editors ignore requested git diff mode and render as standard editors."
    - "Vault diff mode never computes local relative paths or calls local Git/file diff IPC."
    - "Local file diff mode still renders the Monaco diff editor."
    - "No frontmatter, revision, conflict, or staleness UI is introduced."
    - "D-01: Downstream agents must read the external Requirements and Test Plan before planning or implementing Phase 6."
    - "D-02: If ambiguity appears, agents re-read the external docs first, then inspect code, before asking the user."
    - "D-03: Every implementation/test task includes both external docs first in read_first."
    - "D-04: This plan stays within Phase 6 diff guardrails and focused tests."
    - "D-05: No frontmatter UI, conflict/staleness UI, version persistence, or vault-management behavior is added."
    - "D-07: Local diff mode remains the regression baseline."
    - "D-19: Vault URI diff requests short-circuit to standard editor mode, log a warning, and avoid relative-path/Git IPC work."
    - "D-20: Local diff mode for staged and working files continues unchanged."
    - "D-26: No frontmatter parsing/rendering surface is added."
    - "D-27: Editor diff work does not introduce version_token, expected_version, or if_match behavior."
    - "D-28: No staleness/conflict UI appears in v1."
    - "D-29: EditorPanel.test.tsx is the test home for Phase 6 editor coverage."
    - "D-31: Tests cover vault diff guardrails, local diff regression, and no local relative-path work."
    - "D-33: Verification uses focused EditorPanel tests and Node 22/typecheck commands."
  artifacts:
    - path: "src/renderer/panels/EditorPanel.tsx"
      provides: "Vault diff-mode guardrail"
    - path: "src/renderer/panels/EditorPanel.test.tsx"
      provides: "T-I-088 through T-I-090 diff guardrail coverage"
---

<objective>
Gate Git diff mode off for FlashQuery vault documents while preserving local-file diff behavior.

Purpose: Vault URIs are logical FlashQuery document identifiers, not local repository paths. A vault editor opened with `diffMode` must fall back to standard editor mode and must not touch local Git or filesystem diff code.
Output: `EditorPanel` diff guard plus T-I-088 through T-I-090 coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/Cate/cate/AGENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/ROADMAP.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/REQUIREMENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-RESEARCH.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add vault diff guardrail tests</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.4, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-088 through T-I-090 first per D-01/D-03.
    3. `src/renderer/panels/EditorPanel.test.tsx`
    4. `src/renderer/panels/EditorPanel.tsx`
    5. `src/shared/flashqueryUri.ts`
  </read_first>
  <files>src/renderer/panels/EditorPanel.test.tsx</files>
  <behavior>
    - T-I-088: vault URI with `diffMode: 'staged'` or `'working'` renders standard editor mode, not diff mode, and logs a warning.
    - T-I-089: local path with `diffMode: 'staged'` still creates a Monaco diff editor and calls `gitDiffStaged`.
    - T-I-090: vault diff entry does not call `fsReadFile`, `gitDiff`, `gitDiffStaged`, or local relative-path work for diff setup.
  </behavior>
  <action>Add or extend `EditorPanel.test.tsx` with focused tests for T-I-088, T-I-089, and T-I-090. Use the existing Monaco mock. For vault diff tests, mount an editor panel with a `flashquery://workspace-id/docs/example.md` filePath and `diffMode: 'staged'` or `'working'`; assert standard editor creation and `flashqueryGetDocument` behavior while `gitDiff`, `gitDiffStaged`, and diff-specific `fsReadFile` calls are absent. For the local regression, mount a local path with `diffMode: 'staged'` and assert the existing diff-editor and `gitDiffStaged` path still runs.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EditorPanel.test.tsx` contains test names or comments referencing T-I-088, T-I-089, and T-I-090.
    - Tests fail before implementation because vault diff mode currently enters local diff logic.
    - Tests prove local diff behavior remains covered.
  </acceptance_criteria>
  <done>Diff guardrail expectations are encoded before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement local-only diff mode</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.4, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-088 through T-I-090 first per D-01/D-03.
    3. `src/renderer/panels/EditorPanel.tsx`
    4. `src/renderer/panels/EditorPanel.test.tsx`
    5. `src/shared/flashqueryUri.ts`
  </read_first>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <action>In `EditorPanel.tsx`, compute `const vaultUri = filePath ? parseVaultUri(filePath) : null` before the diff branch in the mount effect. Replace the current `if (diffMode && filePath && rootPath)` branch with a local-only condition such as `if (diffMode && filePath && rootPath && !vaultUri)`. If `diffMode && vaultUri`, log `log.warn('[EditorPanel] Git diff mode is not supported for FlashQuery vault documents:', filePath)` and fall through to the regular editor branch created in Plan 06-01 so vault content reads through `flashqueryGetDocument`. Ensure the Plan 06-02 `save` callback treats vault documents as standard editors even when `PanelState.diffMode` is set. Do not compute `relativePath`, call `fsReadFile`, or call `gitDiff`/`gitDiffStaged` for vault URIs. Keep local diff mode code unchanged for non-vault paths.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - T-I-088, T-I-089, and T-I-090 pass.
    - Vault URI diff requests render a standard editor and load through `flashqueryGetDocument`.
    - Local path diff requests still create the Monaco diff editor and use existing Git IPC calls.
  </acceptance_criteria>
  <done>Vault URIs are excluded from local Git diff logic without regressing local diff behavior.</done>
</task>
</tasks>

<must_haves>
- REQ-030: vault URIs with diff mode render standard editor mode and never call local Git/file diff IPC.
- REQ-041: no frontmatter parsing/rendering surface is added.
- REQ-042: no version, conflict, staleness, expected-version, or if-match UI/data is added.
</must_haves>

<decision_coverage>
- D-01, D-02, D-03: Every task lists the external Requirements and Test Plan as the first two `<read_first>` entries.
- D-04: This plan is limited to Phase 6 diff guardrails and focused tests.
- D-05: The plan forbids frontmatter, conflict/staleness, version, and unrelated vault-management UI.
- D-07: Local diff behavior is an explicit regression requirement.
- D-19: Vault URI diff requests short-circuit to standard editor mode, log a warning, and avoid relative-path/Git IPC work.
- D-20: Local diff mode for staged and working files continues unchanged.
- D-26, D-27, D-28: No frontmatter parsing/rendering, version-token, expected-version, if-match, or conflict/staleness UI is added.
- D-29, D-31, D-33: `EditorPanel.test.tsx` covers vault diff guardrails and local diff regression, with Node 22 verification.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer vault URI to local Git IPC | Logical vault URIs must not cross into local Git diff or filesystem paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | `EditorPanel` diff mode | mitigate | Gate diff mode with `parseVaultUri`; tests assert no `gitDiff`, `gitDiffStaged`, `fsReadFile`, or relative-path work for vault URIs. |
| T-06-03-02 | Information Disclosure | `EditorPanel` vault branch | mitigate | Keep vault docs on body-only read/write paths and add no frontmatter/version/conflict UI. |
| T-06-03-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
T-I-088 through T-I-090 pass, local diff behavior remains covered, and vault URIs never enter local Git/file diff setup.
</success_criteria>

<output>
Create `.planning/phases/06-editor-uri-awareness-vault-badge/06-03-SUMMARY.md` when done.
</output>
