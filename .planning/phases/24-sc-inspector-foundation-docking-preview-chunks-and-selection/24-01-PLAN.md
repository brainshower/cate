---
phase: 24-sc-inspector-foundation-docking-preview-chunks-and-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/panels.ts
  - src/shared/panels.test.ts
  - src/renderer/panels/registry.ts
  - src/renderer/panels/registry.test.ts
  - src/renderer/stores/appStore.ts
  - src/renderer/stores/appStore.test.ts
  - src/renderer/panels/SemanticConnectionsPanel.tsx
  - src/renderer/panels/types.ts
  - src/renderer/lib/semanticConnections.ts
  - src/renderer/lib/semanticConnections.test.ts
autonomous: true
requirements:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-015
  - REQ-016
  - REQ-017
  - REQ-018
coverage:
  - T-U-001
  - T-U-002
  - T-U-003
  - T-U-004
  - T-U-005
  - T-U-006
  - T-U-007
  - T-U-008
  - T-I-030
must_haves:
  truths:
    - "Downstream agents MUST read the Semantic Connections Inspector requirements and test plan before implementing."
    - "`semantic-connections` is the panel type name."
    - "The SC panel must reuse Outline-style dock hosting and must not duplicate the header title inside the panel body."
    - "Embeddings-only behavior is first-class and must not be treated as an error state."
  artifacts:
    - path: "src/shared/types.ts"
      provides: "`PanelType` includes `semantic-connections`."
      contains: "semantic-connections"
    - path: "src/shared/panels.ts"
      provides: "`PANEL_DEFINITIONS.semantic-connections` shared metadata, including the locked default/minimum sizes."
      contains: "semantic-connections"
    - path: "src/renderer/panels/registry.ts"
      provides: "Lazy SC panel entry and app-store factory delegation."
      contains: "SemanticConnectionsPanel"
    - path: "src/renderer/stores/appStore.ts"
      provides: "`createSemanticConnections(workspaceId, position?, placement?)`."
      contains: "createSemanticConnections"
    - path: "src/renderer/lib/semanticConnections.ts"
      provides: "Shared semantic-connection types and pure utilities."
      contains: "arrangeForDisplay"
  key_links:
    - from: "src/renderer/panels/registry.ts"
      to: "src/renderer/stores/appStore.ts"
      via: "SC panel create delegates to app-store action"
      pattern: "createSemanticConnections"
---

<objective>
Register the `semantic-connections` panel across Cate's shared, renderer, and app-store systems, and land the shared semantic-connection utility surface needed for Phase 24 and Phase 25 work.

Purpose: Phase 24 needs a first-class panel host plus typed, testable utility foundations before dock math or preview selection behavior can safely depend on them.
Output: Panel registration plumbing, a narrow SC panel shell, and pure semantic-connection utilities with focused unit coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-RESEARCH.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md
</context>

<tasks>
<task id="24.1.1" type="execute" tdd="true">
  <name>Register the shared SC panel type and shell</name>
  <files>src/shared/types.ts, src/shared/panels.ts, src/shared/panels.test.ts, src/renderer/panels/registry.ts, src/renderer/panels/registry.test.ts, src/renderer/stores/appStore.ts, src/renderer/stores/appStore.test.ts, src/renderer/panels/SemanticConnectionsPanel.tsx, src/renderer/panels/types.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 1.3, 3.1, 3.5, 4.1, and 6.1. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.1, 3.1, 3.4, and 4.1. Use T-U-001 through T-U-003 and T-I-030 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-01 through D-10 and D-29 through D-34.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-PATTERNS.md`.
    - `src/shared/types.ts`.
    - `src/shared/panels.ts`.
    - `src/renderer/panels/registry.ts`.
    - `src/renderer/stores/appStore.ts`.
    - `src/renderer/panels/OutlinePanel.tsx`.
  </read_first>
  <action>Write the failing registration tests first. Add `semantic-connections` to the shared type/definition/registry/store chain with the locked product values: `defaultSize: { width: 360, height: 400 }`, `minimumSize: { width: 330, height: 200 }`, `canLiveOnCanvas: true`, and complete shared chrome metadata. Add `createSemanticConnections(workspaceId, position?, placement?)` following the same placement path as `createOutline`. Create a narrow `SemanticConnectionsPanel` shell that proves the body starts at scope-row or placeholder content rather than duplicating the dock title, and keep SC-specific title actions in dock chrome only. The shell may render a lightweight placeholder tied to later selection state, but it must not ship a second in-body title row.</action>
  <acceptance_criteria>
    - `src/shared/types.ts` includes `'semantic-connections'` in `PanelType`.
    - `PANEL_DEFINITIONS['semantic-connections']` matches the required default/minimum sizes and `canLiveOnCanvas: true`.
    - `PANEL_REGISTRY['semantic-connections']` lazy-loads `SemanticConnectionsPanel` and delegates creation to `createSemanticConnections`.
    - App-store tests prove main-dock and canvas placement use the ordinary placement path.
    - `SemanticConnectionsPanel` does not render a duplicate "Connections" title inside the body.
    - Tests include `T-U-001`, `T-U-002`, `T-U-003`, and `T-I-030` traceability.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>The SC panel exists as a first-class Cate panel and hosts through the standard dock system without duplicated body chrome.</done>
</task>

<task id="24.1.2" type="execute" tdd="true">
  <name>Land shared semantic-connection types and pure utilities</name>
  <files>src/renderer/lib/semanticConnections.ts, src/renderer/lib/semanticConnections.test.ts, src/renderer/panels/SemanticConnectionsPanel.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 1.3, 3.4, 3.5, and 4.4. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.1 and 3.1. Use T-U-004 through T-U-008 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-11 through D-14 and D-29 through D-34.
    - `src/renderer/lib/parseDocumentHeadings.ts` only as a local style/reference example for pure utility placement.
  </read_first>
  <action>Write failing utility tests first. Add shared semantic-connection types and pure helpers for `scEdgeLabel`, `arrangeForDisplay`, `getAllRels`, and `scCautionFlags`. The utilities must treat embeddings-only payloads as normal data, keep `rel` and `dir` optional, support sparse mixed typed/untyped data without assuming every row is typed, and keep caution counts at zero when typed caution data is absent. Wire the SC shell only lightly to prove embeddings-only mode is an expected empty/shell path, not an error path.</action>
  <acceptance_criteria>
    - Utility tests cover `T-U-004` through `T-U-008`.
    - `arrangeForDisplay` sorts score-descending in similarity mode and groups warn/caution-first in nature mode.
    - `getAllRels` ignores untyped connections.
    - `scCautionFlags` returns zero counts for embeddings-only fixtures.
    - No implementation assumes `rel` or `dir` is always present.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/semanticConnections.test.ts</automated>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts src/renderer/lib/semanticConnections.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Phase 24 has a reusable semantic-connection contract and utility layer that supports embeddings-only launch and future typed-edge data.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shared panel metadata -> renderer creation path | New panel type metadata crosses shared and renderer layers. |
| Semantic-connection payloads -> future panel/provider consumers | Renderer code will depend on shared types and utility semantics established here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-24-01-T | Tampering | panel registration | mitigate | Lock the type-definition-registry-store chain with focused tests and exact factory assertions. |
| T-24-02-T | Tampering | semantic utility semantics | mitigate | Pure utility tests lock embeddings-only, sparse mixed typed/untyped, and caution-count behavior. |
| T-24-03-D | Denial of Service | future SC shell assumptions | mitigate | Keep the shell narrow and forbid duplicate body-title chrome so later UI layers can extend it cleanly. |
| T-24-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts`
- `npx -p node@22 npm test -- src/renderer/lib/semanticConnections.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 24.1 is complete when `semantic-connections` is a registered first-class panel, the shell proves header/body ownership, and the semantic-connection utility surface is locked with focused unit tests.
</success_criteria>

<output>
Create `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-01-SUMMARY.md` when done.
</output>
