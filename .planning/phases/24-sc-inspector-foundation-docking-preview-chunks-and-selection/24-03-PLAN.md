---
phase: 24-sc-inspector-foundation-docking-preview-chunks-and-selection
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
files_modified:
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/lib/parseDocumentHeadings.ts
autonomous: true
requirements:
  - REQ-004
  - REQ-005
  - REQ-006
coverage:
  - T-I-001
  - T-I-002
  - T-I-003
must_haves:
  truths:
    - "Downstream agents MUST read the Semantic Connections Inspector requirements and test plan before implementing."
    - "Preview chunk wrappers must use `data-chunk-id` and reuse the existing heading slug logic."
    - "Chunk wrappers must refresh across preview rerenders and clear when preview mode exits."
  artifacts:
    - path: "src/renderer/panels/EditorPanel.tsx"
      provides: "Preview chunk wrapper rendering/lifecycle."
      contains: "data-chunk-id"
    - path: "src/renderer/panels/EditorPanel.test.tsx"
      provides: "Preview wrapper structure and lifecycle coverage."
      contains: "T-I-001"
---

<objective>
Make Markdown preview expose stable heading-scoped chunk wrappers that Phase 24 selection behavior and Phase 25 adapter mapping can target directly.

Purpose: Shared selection and per-section semantic scope depend on stable preview chunk wrappers, not heading-only IDs.
Output: Updated preview rendering/lifecycle and focused wrapper tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-RESEARCH.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-PATTERNS.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md
</context>

<tasks>
<task id="24.3.1" type="execute" tdd="true">
  <name>Add heading-scoped preview chunk wrappers</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 3.2, 4.1, and 6.3. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.2 and 3.2. Use T-I-001 through T-I-003 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-18 through D-20 and D-29 through D-31.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-07 through D-12 because preview heading identity already exists and must be preserved.
    - `src/renderer/panels/EditorPanel.tsx`.
    - `src/renderer/panels/EditorPanel.test.tsx`.
  </read_first>
  <action>Write failing wrapper tests first. Update preview rendering so each heading-scoped section becomes a stable wrapper element with `data-chunk-id`. The wrapper must contain the heading plus all rendered content until the next heading, preserve readable pre-heading content without inventing invalid IDs, and keep duplicate-heading IDs aligned with the existing `createHeadingIdTracker()` suffix semantics. Reuse the shared slug helpers already used for preview heading IDs instead of adding a second slugging path.</action>
  <acceptance_criteria>
    - `T-I-001` proves heading-scoped `div[data-chunk-id]` wrappers exist after preview render.
    - `T-I-002` proves a wrapper contains the heading and following body content until the next heading.
    - `T-I-003` proves duplicate headings produce stable suffixed chunk IDs matching existing heading-ID behavior.
    - No new competing slug helper is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Markdown preview exposes stable heading-scoped chunk wrappers for section-level selection and mapping.</done>
</task>

<task id="24.3.2" type="execute" tdd="true">
  <name>Refresh and clean preview chunk wrapper lifecycle</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` REQ-006 acceptance criteria. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.2 and 4.3. Reuse T-I-001 through T-I-003 coverage names where the harness naturally supports it.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-20 through D-24.
    - `src/renderer/panels/EditorPanel.tsx`.
  </read_first>
  <action>Add lifecycle coverage showing wrappers and any associated preview-only wrapper bookkeeping refresh cleanly after preview rerender and clear when preview mode exits. Avoid duplicate wrapper state or duplicate event plumbing. Preserve existing Phase 23 preview routing behavior while adding chunk wrappers as the new section-level surface.</action>
  <acceptance_criteria>
    - Wrapper state refreshes cleanly after preview content changes while preview remains active.
    - Preview-only wrapper bookkeeping clears when preview mode exits.
    - Existing preview heading ID and preview scroll behavior remain intact.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Preview chunk wrappers survive rerenders and clear safely on preview exit.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Markdown preview content -> section wrapper DOM | User-authored markdown is projected into chunk-scoped wrapper elements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-24-06-T | Tampering | wrapper identity | mitigate | Reuse shared slug helpers and lock duplicate-heading semantics with focused tests. |
| T-24-07-D | Denial of Service | rerender lifecycle | mitigate | Verify refresh and preview-exit cleanup to avoid duplicate wrapper bookkeeping. |
| T-24-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 24.3 is complete when preview rendering exposes stable `data-chunk-id` wrappers, duplicate headings stay aligned with existing slug semantics, and wrapper lifecycle remains clean across rerenders and preview exit.
</success_criteria>

<output>
Create `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-03-SUMMARY.md` when done.
</output>
