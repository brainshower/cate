---
phase: 24-sc-inspector-foundation-docking-preview-chunks-and-selection
plan: 04
type: execute
wave: 4
depends_on:
  - 24-01
  - 24-03
files_modified:
  - src/renderer/stores/previewSelectionStore.ts
  - src/renderer/stores/previewSelectionStore.test.ts
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/panels/OutlinePanel.tsx
  - src/renderer/panels/OutlinePanel.test.tsx
  - src/renderer/panels/SemanticConnectionsPanel.tsx
  - e2e/semantic-connections-preview-selection.spec.ts
autonomous: true
requirements:
  - REQ-007
  - REQ-008
  - REQ-009
  - REQ-013
  - REQ-014
coverage:
  - T-U-009
  - T-U-010
  - T-I-004
  - T-I-005
  - T-I-006
  - T-I-007
  - T-I-008
  - T-I-031
  - T-I-032
  - T-I-033
  - T-I-034
  - T-I-035
  - T-E-004
must_haves:
  truths:
    - "Downstream agents MUST read the Semantic Connections Inspector requirements and test plan before implementing."
    - "The shared selection rule is `activeChunkId = hoveredChunkId || pinnedChunkId`."
    - "Preview interaction must preserve normal text selection and must not use production `CustomEvent` bridging."
    - "Embeddings-only fixtures must show no caution decoration."
  artifacts:
    - path: "src/renderer/stores/previewSelectionStore.ts"
      provides: "Shared preview-selection surface."
      contains: "activeChunkId"
    - path: "src/renderer/panels/EditorPanel.tsx"
      provides: "Preview hover/pin/decorations wired to shared selection state."
      contains: "data-chunk-id"
    - path: "src/renderer/panels/OutlinePanel.tsx"
      provides: "Initial Outline awareness of shared selection state."
      contains: "activeChunkId"
    - path: "src/renderer/panels/SemanticConnectionsPanel.tsx"
      provides: "SC shell consumes shared selection state for scope awareness."
      contains: "Whole document"
---

<objective>
Introduce the shared preview-selection surface and wire preview hover/pin/decorations plus initial Outline and SC shell awareness through it.

Purpose: This is the Phase 24 cross-surface foundation that prevents Phase 25 from inventing a second synchronization model.
Output: Shared selection store, preview interaction wiring, decoration behavior, initial Outline/SC shell consumption, and focused automated coverage including the required E2E path.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-RESEARCH.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md
</context>

<tasks>
<task id="24.4.1" type="execute" tdd="true">
  <name>Add shared preview-selection state</name>
  <files>src/renderer/stores/previewSelectionStore.ts, src/renderer/stores/previewSelectionStore.test.ts, src/renderer/panels/SemanticConnectionsPanel.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 3.3 and 6.3. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.1, 3.1, and 4.3. Use T-U-009 and T-U-010 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-21 through D-24 and D-32 through D-34.
    - `src/renderer/lib/activeEditorRegistry.ts` only as a reference for renderer-local cross-panel state style.
  </read_first>
  <action>Write failing store tests first. Add a dedicated renderer-local preview-selection store exposing at least `hoveredChunkId`, `pinnedChunkId`, `activeChunkId`, `setHoveredChunkId`, `setPinnedChunkId`, `selectSection`, and `clearSelection`. The SC shell should begin reading this store so Phase 24 proves whole-document versus selected-section scope state without needing the full Phase 25 card UI.</action>
  <acceptance_criteria>
    - `T-U-009` proves `activeChunkId = hoveredChunkId || pinnedChunkId`.
    - `T-U-010` proves clear/reset behavior for hover, pin, and active state.
    - The store is renderer-local and does not depend on browser `CustomEvent`.
    - The SC shell can represent whole-document versus selected-section state from the shared store.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/stores/previewSelectionStore.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>The shared selection surface exists and the SC shell consumes it.</done>
</task>

<task id="24.4.2" type="execute" tdd="true">
  <name>Wire preview hover, pin, clear, and decoration behavior</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx, src/renderer/stores/previewSelectionStore.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` REQ-007, REQ-008, REQ-013, and REQ-014. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.2, 3.2, and 3.5. Use T-I-004 through T-I-008 and T-I-031 through T-I-035 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-18 through D-25.
    - `src/renderer/panels/EditorPanel.tsx`.
    - `src/renderer/panels/EditorPanel.test.tsx`.
  </read_first>
  <action>Write failing interaction tests first. Wire preview wrappers so hover, click-pin, Esc clear, empty-space clear, and whole-document clear update the shared selection store. Hit-testing must resolve nested targets to the enclosing chunk wrapper, drag text selection must not pin, and existing links must keep working. Apply active and pinned classes to the wrappers themselves. Use typed fixtures only to prove caution-decoration wiring, but ensure embeddings-only fixtures keep caution decoration absent.</action>
  <acceptance_criteria>
    - `T-I-004` proves nested targets resolve to the enclosing chunk wrapper.
    - `T-I-005` proves drag text selection does not pin.
    - `T-I-006` proves active and pinned classes land on chunk wrappers.
    - `T-I-007` proves embeddings-only fixtures produce no caution decoration.
    - `T-I-008` proves typed warn/caution fixtures show caution decoration that wins over teal selection.
    - `T-I-031` through `T-I-035` prove hover, leave, pin, hover-beats-pin, and clear semantics.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>! rg -n "CustomEvent|preview-section-select" src/renderer src/shared</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Preview wrappers drive shared selection state and wrapper-level decorations without breaking text selection or using event bridges.</done>
</task>

<task id="24.4.3" type="execute" tdd="true">
  <name>Add initial Outline awareness and focused end-to-end proof</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx, src/renderer/panels/SemanticConnectionsPanel.tsx, e2e/semantic-connections-preview-selection.spec.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 3.3, 4.1, and 6.3. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.4, 3.5, 3.6, and 3.8. Use T-I-031 through T-I-035 and T-E-004 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-26 through D-31.
    - `src/renderer/panels/OutlinePanel.tsx`.
    - `src/renderer/panels/OutlinePanel.test.tsx`.
  </read_first>
  <action>Add the initial Outline subscription to shared selection state so shared active selection can influence Outline highlight without regressing source-mode fallback behavior. Keep this work intentionally narrow and seam-oriented; final two-pass chunk matching belongs to Phase 25. Then add the required focused Electron/Playwright proof for preview hover synchronization (`T-E-004`) using the smallest realistic scenario that proves preview hover updates cross-surface state.</action>
  <acceptance_criteria>
    - Outline can react to shared active selection state without losing its existing source-mode/cursor fallback.
    - The SC shell reflects whole-document versus one-section-selected scope from the shared store.
    - `T-E-004` proves preview hover updates shared state across Preview and Outline.
    - No code introduces production `CustomEvent` bridges.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx src/renderer/stores/previewSelectionStore.test.ts</automated>
    <automated>npx -p node@22 npm run test:e2e -- e2e/semantic-connections-preview-selection.spec.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Shared selection state reaches Preview, Outline, and the SC shell, and the required E2E preview-hover path is covered.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Preview DOM interaction -> shared renderer state | Pointer and keyboard interactions mutate cross-panel selection state. |
| Shared selection state -> Outline and SC shell | Cross-panel consumers react to preview-driven state changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-24-08-T | Tampering | shared selection semantics | mitigate | Lock the store contract and hover/pin precedence with T-U-009, T-U-010, and selection-flow tests. |
| T-24-09-D | Denial of Service | preview interaction layer | mitigate | Use wrapper-level hit-testing, preserve text selection, and test clear-on-exit flows. |
| T-24-10-I | Information Disclosure | event-bridge behavior | mitigate | Forbid production `CustomEvent` bridges and grep for them during verification. |
| T-24-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/stores/previewSelectionStore.test.ts`
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx src/renderer/stores/previewSelectionStore.test.ts`
- `npx -p node@22 npm run test:e2e -- e2e/semantic-connections-preview-selection.spec.ts`
- `! rg -n "CustomEvent|preview-section-select" src/renderer src/shared`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 24.4 is complete when preview hover/pin behavior is driven by one shared selection surface, embeddings-only fixtures avoid caution decoration, Outline has the initial shared-selection seam, and the required preview-hover E2E path passes.
</success_criteria>

<output>
Create `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-04-SUMMARY.md` when done.
</output>
