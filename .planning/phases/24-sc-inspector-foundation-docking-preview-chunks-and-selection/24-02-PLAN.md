---
phase: 24-sc-inspector-foundation-docking-preview-chunks-and-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/renderer/docking/DockSplitContainer.tsx
  - src/renderer/docking/DockSplitContainer.test.tsx
  - src/shared/panels.ts
  - src/shared/types.ts
autonomous: true
requirements:
  - REQ-002
  - REQ-035
coverage:
  - T-U-013
  - T-U-014
  - T-I-027
  - T-I-028
  - T-I-029
must_haves:
  truths:
    - "Downstream agents MUST read the Semantic Connections Inspector requirements and test plan before implementing."
    - "Dock split resizing must enforce panel-specific descendant minimum sizes, including the SC panel's 330px width floor."
    - "The existing 10% ratio floor may remain only as a secondary clamp."
  artifacts:
    - path: "src/renderer/docking/DockSplitContainer.tsx"
      provides: "Recursive effective minimum-size clamping for dock resize."
      contains: "setSplitRatio"
    - path: "src/renderer/docking/DockSplitContainer.test.tsx"
      provides: "Focused dock minimum-size regression coverage."
      contains: "T-U-013"
---

<objective>
Replace the dock split ratio-only floor with descendant effective minimum-size enforcement while preserving the existing adjacent-delta transfer model.

Purpose: The SC panel's required `330px` minimum width and the broader Phase 24 dock-floor fix cannot be delivered by the current `0.1` ratio clamp.
Output: Updated dock split resize logic plus focused layout tests for horizontal, vertical, and nested cases.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-RESEARCH.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md
</context>

<tasks>
<task id="24.2.1" type="execute" tdd="true">
  <name>Add recursive effective minimum-size helpers</name>
  <files>src/renderer/docking/DockSplitContainer.tsx, src/renderer/docking/DockSplitContainer.test.tsx, src/shared/panels.ts, src/shared/types.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` sections 2.1, 3.9, 4.1, and 6.2. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 2.1 and 3.4. Use T-U-013 and T-I-027 through T-I-029 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-15 through D-17 and D-29 through D-31.
    - `src/renderer/docking/DockSplitContainer.tsx`.
    - `src/shared/panels.ts`.
    - `src/shared/types.ts`.
  </read_first>
  <action>Write failing dock tests first. Add helper logic that computes the effective minimum width/height for a dock layout subtree from descendant panel definitions. Horizontal splits should aggregate child minimum widths and take the max child minimum height; vertical splits should aggregate heights and take the max child minimum width; tab stacks should use the max minimum-size of the panels they host. Keep the helper close to dock resize logic unless an existing shared dock helper file clearly fits better.</action>
  <acceptance_criteria>
    - Tests cover single-stack, mixed-stack, and nested-layout effective minimum-size calculation for `T-U-013`.
    - Effective minimum widths/heights derive from `PANEL_DEFINITIONS[type].minimumSize`.
    - The SC panel's `330px` width minimum participates in recursive effective minimum computation.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/docking/DockSplitContainer.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Dock layouts can calculate real descendant minimum sizes rather than relying on a ratio-only floor.</done>
</task>

<task id="24.2.2" type="execute" tdd="true">
  <name>Clamp resize deltas against effective minimums</name>
  <files>src/renderer/docking/DockSplitContainer.tsx, src/renderer/docking/DockSplitContainer.test.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` section 3.9 and acceptance criteria for REQ-035. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 3.1 and 3.4. Use T-U-014 and T-I-027 through T-I-029 traceability.
    - `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-CONTEXT.md` decisions D-15 through D-17.
    - `src/renderer/docking/DockSplitContainer.tsx`.
  </read_first>
  <action>Update `handleResize` so the adjacent delta is clamped against effective minimum pixel sizes for both neighboring children before converting back to ratios. Preserve the existing invariant that only the actual clamped delta transfers between adjacent children and no full-layout renormalization occurs. Add focused tests proving horizontal SC width clamping, vertical panel height clamping, and nested-layout clamping behavior.</action>
  <acceptance_criteria>
    - `T-U-014` proves the transferred ratio delta matches the actual clamped pixel delta.
    - `T-I-027` proves a horizontal split cannot shrink the SC panel below `330px`.
    - `T-I-028` proves vertical splits enforce panel-specific minimum heights.
    - `T-I-029` proves nested descendant minimums are enforced.
    - The 10% ratio floor remains only as a secondary rule.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/docking/DockSplitContainer.test.tsx</automated>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/docking/DockSplitContainer.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Dock resizing enforces panel-specific descendant minimums and preserves adjacent clamped-delta transfer.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User drag delta -> dock layout mutation | Pointer movement mutates persistent dock ratios. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-24-04-T | Tampering | dock ratio math | mitigate | Lock effective minimum-size and clamped-delta behavior with focused tests. |
| T-24-05-D | Denial of Service | nested split layout | mitigate | Compute recursive effective minimums and verify nested cases explicitly. |
| T-24-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/docking/DockSplitContainer.test.tsx`
- `npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/docking/DockSplitContainer.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 24.2 is complete when dock resizing enforces effective descendant minimum sizes, including the SC panel width floor, and the resize delta remains locally clamped between adjacent children.
</success_criteria>

<output>
Create `.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-02-SUMMARY.md` when done.
</output>
