---
phase: 04-vault-panel-shared-chip
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/flashqueryUri.ts
  - src/shared/flashqueryUri.test.ts
  - src/main/flashquery/uri.ts
  - src/main/flashquery/uri.test.ts
  - src/shared/ipc-channels.ts
  - src/shared/electron-api.d.ts
  - src/preload/index.ts
  - src/main/ipc/flashquery.ts
  - src/main/ipc/flashquery.test.ts
  - src/renderer/stores/uiStore.ts
  - src/renderer/stores/uiStore.test.ts
autonomous: true
requirements:
  - REQ-017
  - REQ-019
  - REQ-025
must_haves:
  truths:
    - "Renderer code can build canonical `flashquery://` editor URIs without importing main-process modules."
    - "User can manually retry a disconnected FlashQuery connection from renderer UI."
    - "Phase 4 empty/disconnected actions can open the future FlashQuery connection dialog state without implementing the dialog."
  artifacts:
    - path: "src/shared/flashqueryUri.ts"
      provides: "Renderer-safe `buildVaultUri` and `parseVaultUri` helpers"
      exports: ["FlashQueryUriParts", "buildVaultUri", "parseVaultUri"]
    - path: "src/main/flashquery/uri.ts"
      provides: "Main-side compatibility re-export for existing imports"
    - path: "src/shared/ipc-channels.ts"
      provides: "`FLASHQUERY_RETRY` channel constant"
    - path: "src/shared/electron-api.d.ts"
      provides: "`flashqueryRetry(workspaceId)` renderer API contract"
    - path: "src/preload/index.ts"
      provides: "Preload bridge for manual retry"
    - path: "src/main/ipc/flashquery.ts"
      provides: "Main IPC handler delegating to `FlashQueryClientManager.retry`"
    - path: "src/renderer/stores/uiStore.ts"
      provides: "`showFlashQueryConnectionDialog` and setter action"
  key_links:
    - from: "src/main/flashquery/uri.ts"
      to: "src/shared/flashqueryUri.ts"
      via: "compatibility re-export"
      pattern: "export .* from .*shared.*flashqueryUri"
    - from: "src/preload/index.ts"
      to: "src/main/ipc/flashquery.ts"
      via: "FLASHQUERY_RETRY invoke"
      pattern: "ipcRenderer.invoke\\(FLASHQUERY_RETRY"
    - from: "src/renderer/panels/FlashQueryVaultPanel.tsx"
      to: "src/renderer/stores/uiStore.ts"
      via: "setShowFlashQueryConnectionDialog(true)"
      pattern: "setShowFlashQueryConnectionDialog\\(true\\)"
---

<objective>
Expose the narrow renderer hooks Phase 4 needs: a renderer-safe FlashQuery URI helper, manual FlashQuery retry, and future connection-dialog open state.

Purpose: REQ-017 requires document open actions to create canonical `flashquery://` editor URIs from renderer code without crossing process boundaries; REQ-025 and T-I-028 require retry from the disconnected chip/panel; REQ-019 State A/C actions require a store action to open the Phase 5 dialog without building the dialog in Phase 4.
Output: shared URI helper extraction/re-export, `flashquery:retry` IPC/preload/API bridge, main handler tests, and `uiStore` dialog visibility flag/action tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ipc-surface/03-CONTEXT.md
@.planning/phases/03-ipc-surface/03-03-SUMMARY.md
@.planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
@.planning/phases/04-vault-panel-shared-chip/04-RESEARCH.md
@.planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md

<interfaces>
From Phase 3 summary: `src/main/flashquery/clientManager.ts` already has manager retry behavior, but renderer cannot trigger it.

From existing code: `src/main/flashquery/uri.ts` currently owns `FlashQueryUriParts`, `buildVaultUri`, and `parseVaultUri`; this plan must move that implementation into `src/shared/flashqueryUri.ts` so Wave 2 renderer code can import it without violating Electron process boundaries, while `src/main/flashquery/uri.ts` re-exports it for existing main-side imports.

From `04-RESEARCH.md` pitfalls: add a narrow `flashqueryRetry(workspaceId)` IPC method and a narrow `showFlashQueryConnectionDialog` / `setShowFlashQueryConnectionDialog` UI-store hook; do not implement Phase 5 form fields, dialog component, or workspace menu entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract FlashQuery URI helpers into shared module</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/main/flashquery/uri.ts
    src/main/flashquery/uri.test.ts
    src/shared/types.ts
  </read_first>
  <files>src/shared/flashqueryUri.ts, src/shared/flashqueryUri.test.ts, src/main/flashquery/uri.ts, src/main/flashquery/uri.test.ts</files>
  <behavior>
    - REQ-017 support: renderer-safe code can import `buildVaultUri(workspaceId, vaultPath)` from `src/shared/flashqueryUri.ts`.
    - Existing main-side imports of `src/main/flashquery/uri.ts` continue to work through a re-export.
    - URI tests still prove empty path, nested path, spaces/reserved characters, percent signs/non-ASCII, leading/trailing slashes, and malformed URI parsing.
  </behavior>
  <action>Move the pure URI helper implementation from `src/main/flashquery/uri.ts` into new shared module `src/shared/flashqueryUri.ts`, exporting `FlashQueryUriParts`, `buildVaultUri`, and `parseVaultUri`. Keep `src/main/flashquery/uri.ts` as a compatibility re-export from the shared module so Phase 3 main/editor routing imports do not break. Move or mirror the current URI tests into `src/shared/flashqueryUri.test.ts` and keep `src/main/flashquery/uri.test.ts` as a compatibility test if existing imports rely on it. Do not import renderer, preload, Electron, Node, or main-process modules from the shared helper; it must be TypeScript-only pure string/URI logic.</action>
  <acceptance_criteria>
    - `src/shared/flashqueryUri.ts` contains the canonical implementation of `buildVaultUri` and `parseVaultUri`.
    - `src/main/flashquery/uri.ts` contains only a compatibility re-export or equivalent no-logic wrapper.
    - Tests cover both the shared helper contract and existing main import compatibility.
    - No renderer plan imports `src/main/flashquery/uri.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/shared/flashqueryUri.test.ts src/main/flashquery/uri.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Wave 2 renderer code can build canonical FlashQuery editor URIs from `src/shared/flashqueryUri.ts` without importing main-process code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add manual retry IPC bridge</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/03-ipc-surface/03-03-SUMMARY.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/shared/ipc-channels.ts
    src/shared/electron-api.d.ts
    src/preload/index.ts
    src/main/ipc/flashquery.ts
    src/main/ipc/flashquery.test.ts
    src/main/flashquery/clientManager.ts
  </read_first>
  <files>src/shared/ipc-channels.ts, src/shared/electron-api.d.ts, src/preload/index.ts, src/main/ipc/flashquery.ts, src/main/ipc/flashquery.test.ts</files>
  <behavior>
    - T-I-028 support: calling `window.electronAPI.flashqueryRetry(workspaceId)` reaches `FlashQueryClientManager.retry(workspaceId)`.
    - Invalid or empty workspace ID rejects with the same non-empty string validation style used by existing FlashQuery IPC handlers.
    - Handler registration remains idempotent and existing FlashQuery channel tests are updated for the extra channel.
  </behavior>
  <action>Add `FLASHQUERY_RETRY = 'flashquery:retry'` beside existing FlashQuery channel constants. Add `flashqueryRetry(workspaceId: string): Promise<void>` to `ElectronAPI`. Expose `flashqueryRetry(workspaceId)` in preload using `ipcRenderer.invoke(FLASHQUERY_RETRY, workspaceId)`. In `src/main/ipc/flashquery.ts`, import the constant, add a `retry(workspaceId: string)` helper that calls existing `requireNonEmptyString(workspaceId, 'workspaceId')` and delegates to `flashQueryClientManager.retry(workspaceId)`, then register an `ipcMain.handle(FLASHQUERY_RETRY, ...)` handler. Extend `flashquery.test.ts` to assert the channel is registered, validation rejects bad workspace IDs, and a valid call invokes the mocked manager `retry` exactly once. Do not expose any generic manager method or MCP tool executor.</action>
  <acceptance_criteria>
    - Channel constant is exactly `flashquery:retry`.
    - Renderer-facing method name is exactly `flashqueryRetry`.
    - Main handler delegates to `FlashQueryClientManager.retry`, not `setConnection`, not `connect`, and not direct HTTP/MCP code.
    - Existing `setConnection`, `listVault`, `getDocument`, `writeDocument`, and `onFlashQueryStatus` contracts remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/main/ipc/flashquery.test.ts</automated>
  </verify>
  <done>Renderer UI has the narrow retry bridge needed by the disconnected chip and Retry button.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add narrow FlashQuery dialog visibility state</name>
  <read_first>
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    .planning/phases/04-vault-panel-shared-chip/04-CONTEXT.md
    .planning/phases/04-vault-panel-shared-chip/04-PATTERNS.md
    src/renderer/stores/uiStore.ts
    src/renderer/stores/uiStore.test.ts
    src/renderer/dialogs/SavedLayoutsDialog.tsx
  </read_first>
  <files>src/renderer/stores/uiStore.ts, src/renderer/stores/uiStore.test.ts</files>
  <behavior>
    - T-I-025/T-I-029 support: Phase 4 buttons can call `useUIStore.getState().setShowFlashQueryConnectionDialog(true)`.
    - The store exposes a boolean `showFlashQueryConnectionDialog` defaulting to `false`.
    - `setShowFlashQueryConnectionDialog(false)` closes the future dialog state.
  </behavior>
  <action>Add only `showFlashQueryConnectionDialog: boolean` and `setShowFlashQueryConnectionDialog(show: boolean): void` to `uiStore`, following the existing boolean dialog state/action style. Add or extend `src/renderer/stores/uiStore.test.ts` to prove the default is `false`, setting `true` opens it, and setting `false` closes it. Do not create `FlashQueryConnectionDialog.tsx`, URL/token fields, save/remove/probe behavior, or workspace context-menu items; those belong to Phase 5.</action>
  <acceptance_criteria>
    - `useUIStore.getState().showFlashQueryConnectionDialog` is initially `false`.
    - `setShowFlashQueryConnectionDialog(true)` sets it to `true`.
    - `setShowFlashQueryConnectionDialog(false)` sets it to `false`.
    - No new dialog component or workspace menu entry is added in this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/renderer/stores/uiStore.test.ts</automated>
    <automated>npm test -- src/main/ipc/flashquery.test.ts src/renderer/stores/uiStore.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Phase 4 can open the future connection dialog state from no-connection and disconnected actions without implementing Phase 5 UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared URI helper -> renderer/main | Pure URI helpers are imported across tiers and must not pull privileged modules into renderer bundles |
| renderer -> preload -> main | Untrusted renderer requests manual retry for a workspace ID |
| renderer UI state | Components toggle future dialog visibility inside local renderer store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-00 | Elevation of Privilege | `src/shared/flashqueryUri.ts` | mitigate | Keep URI helper TypeScript-only and dependency-free; main compatibility module re-exports shared code, and renderer imports only the shared module. |
| T-04-03-01 | Tampering | `flashquery:retry` IPC | mitigate | Validate `workspaceId` as a non-empty string before delegating to manager retry. |
| T-04-03-02 | Elevation of Privilege | preload bridge | mitigate | Expose only `flashqueryRetry(workspaceId)`; renderer still cannot import Electron, Node, manager, or MCP clients. |
| T-04-03-03 | Spoofing | UI-store dialog state | accept | The flag only controls renderer visibility state for a future dialog; no credentials or persistence are touched in Phase 4. |
| T-04-03-SC | Tampering | npm installs | accept | This plan performs no package-manager installs. |
</threat_model>

<verification>
Run `npm test -- src/shared/flashqueryUri.test.ts src/main/flashquery/uri.test.ts src/main/ipc/flashquery.test.ts src/renderer/stores/uiStore.test.ts` and `npm run typecheck`. If the local default Node is outside Cate's `>=20 <23` engine, rerun through Node 22 and record the fallback.
</verification>

<success_criteria>
Shared URI construction, manual retry, and dialog-open state are available to the vault panel through renderer-safe helpers and narrow typed APIs; no Phase 5 dialog implementation or workspace menu work is added.
</success_criteria>

<output>
Create `.planning/phases/04-vault-panel-shared-chip/04-03-SUMMARY.md` when done.
</output>
