---
phase: 9
plan: 9.2
type: execute
wave: 2
depends_on:
  - 9.1
files_modified:
  - .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/*
autonomous: false
requirements:
  - REQ-024
must_haves:
  truths:
    - "Fresh Phase 9 build, typecheck, unit, and E2E evidence exists after mainline handoff."
    - "Fresh Phase 9 verification exercises the Phase 8 gap-closure tests for T-U-002, T-U-008, and T-U-017."
    - "Product acceptance smoke is rerun or explicitly carried forward with a documented evidence rationale."
  artifacts:
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/build.log"
      provides: "Post-handoff build output"
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/typecheck.log"
      provides: "Post-handoff typecheck output"
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log"
      provides: "Post-handoff unit/component output"
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test-e2e.log"
      provides: "Post-handoff E2E output"
    - path: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md"
      provides: "Command summary and T-A-010 product acceptance note"
  key_links:
    - from: ".planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md"
      to: ".planning/phases/09-upstream-sync-mainline-handoff/09-VALIDATION.md"
      via: "required Phase 9 gates"
      pattern: "T-A-010"
---

# Plan 9.2: Post-Handoff Verification

<objective>
Run and record the fresh post-handoff verification matrix required by the upstream-sync test plan so Phase 9 does not rely silently on stale Phase 8 evidence.
</objective>

<threat_model>
## Threat Model

- **High:** Mainline can differ from the already-verified sync branch after handoff. Mitigation: rerun build, typecheck, unit, and E2E on the handed-off state.
- **Medium:** Manual/product acceptance can be lost in old evidence. Mitigation: record a Phase 9 `T-A-010` pass note or explicit carry-forward rationale.
</threat_model>

<tasks>
<task id="9.2.1" type="execute">
  <name>Task 1: Run final automated matrix</name>
  <files>.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/build.log, .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/typecheck.log, .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log, .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test-e2e.log, src/shared/ipc-channels.test.ts, src/renderer/lib/session.test.ts, src/renderer/lib/__fixtures__/premerge-workspace.json, src/renderer/lib/__fixtures__/premerge-session.json, e2e/flashquery-happy-path.spec.ts</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `.planning/phases/09-upstream-sync-mainline-handoff/09-VALIDATION.md`
  - `.planning/phases/09-upstream-sync-mainline-handoff/evidence/preflight/NOTES.md`
  - `package.json`
  </read_first>
  <action>
  Run `npm run build`, `npm run typecheck`, `npm test`, and `npm run test:e2e` after handoff. Capture command text, timestamp, exit code, and output under `.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/`. Confirm the fresh unit/E2E logs include the Phase 8 gap-closure coverage for `src/shared/ipc-channels.test.ts`, `src/renderer/lib/session.test.ts`, and the `T-U-017` command-palette path in `e2e/flashquery-happy-path.spec.ts`.
  </action>
  <verify>
    <automated>mkdir -p .planning/phases/09-upstream-sync-mainline-handoff/evidence/final && bash -c 'log=.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/build.log; { echo "command: npm run build"; echo "timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"; echo "output:"; } > "$log"; npm run build >> "$log" 2>&1; code=$?; echo "exit_code: $code" >> "$log"; exit "$code"'</automated>
    <automated>bash -c 'log=.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/typecheck.log; { echo "command: npm run typecheck"; echo "timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"; echo "output:"; } > "$log"; npm run typecheck >> "$log" 2>&1; code=$?; echo "exit_code: $code" >> "$log"; exit "$code"'</automated>
    <automated>bash -c 'log=.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log; { echo "command: npm test"; echo "timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"; echo "output:"; } > "$log"; npm test >> "$log" 2>&1; code=$?; echo "exit_code: $code" >> "$log"; exit "$code"'</automated>
    <automated>bash -c 'log=.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test-e2e.log; { echo "command: npm run test:e2e"; echo "timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"; echo "output:"; } > "$log"; npm run test:e2e >> "$log" 2>&1; code=$?; echo "exit_code: $code" >> "$log"; exit "$code"'</automated>
    <automated>for log in .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/build.log .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/typecheck.log .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test-e2e.log; do grep -q '^command:' "$log" && grep -q '^timestamp:' "$log" && grep -q '^output:' "$log" && grep -q '^exit_code: 0$' "$log" || exit 1; done</automated>
    <automated>grep -q 'src/shared/ipc-channels.test.ts' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log && grep -q 'src/renderer/lib/session.test.ts' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test.log</automated>
    <automated>grep -q 'T-U-017 opens a FlashQuery Vault from the command palette' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/test-e2e.log</automated>
  </verify>
  <done>Fresh Phase 9 logs prove `T-A-003`, `T-A-004`, `REQ-024`, `T-E-001..T-E-005`, and the Phase 8 gap-closure coverage for `T-U-002`, `T-U-008`, and `T-U-017` on the handed-off state.</done>
</task>

<task id="9.2.2" type="execute">
  <name>Task 2: Record product acceptance smoke</name>
  <files>.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md</files>
  <read_first>
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Requirements.md`
  - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Upstream Sync Report (30-May-2026)/Upstream Sync Test Plan.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/08-UAT.md`
  - `.planning/phases/08-upstream-sync-v1-1-0/08-VERIFICATION.md`
  - `.planning/phases/09-upstream-sync-mainline-handoff/evidence/final/`
  </read_first>
  <action>
  Record the Phase 9 `T-A-010` product acceptance status in `evidence/final/NOTES.md`: connect, test connection, open vault, browse docs, edit/save, disconnect/retry, restart, command palette "New FlashQuery Vault", and workspace menu connection action. If any manual step is carried forward from Phase 8, cite the exact Phase 8 evidence and explain why the handoff did not change that surface.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md && grep -q 'T-A-010' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md</automated>
    <automated>grep -E 'PASS|carried forward' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md</automated>
    <automated>! grep -E '\\b(FAIL|BLOCKED)\\b' .planning/phases/09-upstream-sync-mainline-handoff/evidence/final/NOTES.md</automated>
  </verify>
  <done>`T-A-010` has a Phase 9 pass note or explicit Phase 8 evidence carry-forward rationale, with no failed or blocked acceptance status.</done>
</task>
</tasks>

<verification>
- `npm run build`
- `npm run typecheck`
- `npm test`
- `npm run test:e2e`
- `T-A-010` acceptance note in final evidence
</verification>

<success_criteria>
- Fresh post-handoff automated verification passes.
- Product acceptance is not silently assumed.
- Any verification failure blocks Phase 9 closeout.
</success_criteria>
