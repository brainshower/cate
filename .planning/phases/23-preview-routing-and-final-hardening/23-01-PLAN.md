---
phase: 23-preview-routing-and-final-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/lib/parseDocumentHeadings.ts
  - src/renderer/lib/parseDocumentHeadings.test.ts
autonomous: true
requirements:
  - REQ-017
coverage:
  - T-U-015
  - T-U-016
must_haves:
  truths:
    - "Downstream agents MUST read the Document Outline Requirements and Test Plan before implementing."
    - "Markdown preview heading slugs use the exact REQ-017 regex semantics."
    - "Duplicate heading IDs are deterministic and render-order based."
  artifacts:
    - path: "src/renderer/lib/parseDocumentHeadings.ts"
      provides: "Shared inline stripping, slug generation, and duplicate heading ID helper."
      contains: "slugifyHeading"
    - path: "src/renderer/lib/parseDocumentHeadings.test.ts"
      provides: "T-U-015 and T-U-016 unit coverage."
      contains: "T-U-015"
  key_links:
    - from: "src/renderer/lib/parseDocumentHeadings.ts"
      to: "src/renderer/panels/EditorPanel.tsx"
      via: "exported slug/duplicate helpers imported by MarkdownPreview in Plan 23-02"
      pattern: "slugifyHeading"
---

<objective>
Add the shared slug and duplicate-ID helpers required by Markdown preview heading IDs.

Purpose: REQ-017 needs one deterministic helper shared by preview rendering and preview scroll lookup so Outline navigation targets the same IDs the preview emits.
Output: Pure utility exports plus unit tests T-U-015 and T-U-016.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md

<interfaces>
From src/renderer/lib/parseDocumentHeadings.ts:
- `export interface DocumentHeading { line: number; level: number; text: string }`
- `export interface HeadingTextModel { getLineCount: () => number; getLineContent: (lineNumber: number) => string }`
- `export function stripMarkdownInlineFormatting(text: string): string`
- `export function parseDocumentHeadings(model: HeadingTextModel, maxDepth: number): DocumentHeading[]`
</interfaces>
</context>

<tasks>
<task id="23.1.1" type="execute" tdd="true">
  <name>Add shared slug helper</name>
  <files>src/renderer/lib/parseDocumentHeadings.ts, src/renderer/lib/parseDocumentHeadings.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.17 and 2.18. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.4, 3.9, and 4.1. Use its T-U-015 naming/traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-01 through D-09 and D-17.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md`.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/lib/parseDocumentHeadings.test.ts`.
  </read_first>
  <action>Write the failing T-U-015 test first, then export `slugifyHeading(text: string): string` from `parseDocumentHeadings.ts`. The helper must call `stripMarkdownInlineFormatting(text)` before slug normalization, lowercase the result, remove non-word characters while preserving whitespace and hyphens with `/[^\w\s-]/g`, trim the intermediate string, collapse one or more whitespace characters to `-`, and trim leading/trailing hyphens. Do not use a different regex and do not mutate `DocumentHeading` output.</action>
  <acceptance_criteria>
    - `slugifyHeading('Hello World')` returns `hello-world`.
    - Punctuation such as `Hello, World!` is removed with `/[^\w\s-]/g`.
    - Existing hyphens are preserved, whitespace collapses to single hyphens, and leading/trailing hyphens are trimmed.
    - Inline Markdown examples, including links, images, bold, italic, strike, nested bold-italic, and inline code, slug from stripped text.
    - Test name or comment includes `T-U-015` and `Covers REQ-017`.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>`slugifyHeading()` exactly satisfies REQ-017 slug semantics and is covered by T-U-015.</done>
</task>

<task id="23.1.2" type="execute" tdd="true">
  <name>Add duplicate heading ID helper</name>
  <files>src/renderer/lib/parseDocumentHeadings.ts, src/renderer/lib/parseDocumentHeadings.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` section 2.17. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.4 and 3.9. Use its T-U-016 naming/traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-07 through D-09, D-17, and D-22.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/lib/parseDocumentHeadings.test.ts`.
  </read_first>
  <action>Write the failing T-U-016 test first, then export a pure duplicate-ID helper, such as `createHeadingIdTracker()` returning `(text: string) => string`, or `headingIdForText(text, counts)` if that fits the code better. It must use `slugifyHeading(text)` for the base ID and append no suffix for the first occurrence, `-1` for the second, `-2` for the third, and so on. The duplicate tracker is render-order based and must not alter parser headings or Outline state.</action>
  <acceptance_criteria>
    - Duplicate plain headings return `section`, `section-1`, `section-2`.
    - Duplicate formatted headings that strip to the same text return the same suffix sequence.
    - Distinct headings keep independent counts.
    - Test name or comment includes `T-U-016` and `Covers REQ-017`.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Duplicate preview heading ID generation is deterministic and covered by T-U-016.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Markdown document text -> preview DOM ID | Untrusted document content is normalized into DOM IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-23-01-T | Tampering | slug helper | mitigate | Lock exact regex and duplicate suffix behavior with T-U-015/T-U-016 tests. |
| T-23-02-D | Denial of Service | slug helper | mitigate | Keep helper linear string replacement only; no DOM parsing or recursive processing. |
| T-23-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 23.1 is complete when T-U-015 and T-U-016 pass and downstream preview code can import shared slug/duplicate-ID helpers.
</success_criteria>

<output>
Create `.planning/phases/23-preview-routing-and-final-hardening/23-01-SUMMARY.md` when done.
</output>
