---
phase: 23-preview-routing-and-final-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/lib/activeEditorRegistry.ts
autonomous: true
requirements:
  - REQ-017
  - REQ-018
coverage:
  - T-I-023
  - T-I-024
  - T-I-025
  - T-I-028
  - T-I-029
  - T-I-030
must_haves:
  truths:
    - "Markdown preview headings render deterministic IDs from shared helpers."
    - "Preview scroll accepts Outline heading text, scrolls smoothly, and flashes blue for 1.5s."
    - "Preview scroll does not dispatch Graph Explorer `preview-section-select` events."
  artifacts:
    - path: "src/renderer/panels/EditorPanel.tsx"
      provides: "MarkdownPreview heading IDs and preview scroll callback registration."
      contains: "MarkdownPreview"
    - path: "src/renderer/lib/activeEditorRegistry.ts"
      provides: "Active editor preview-mode/scroll bridge for OutlinePanel."
      contains: "scrollPreviewToHeading"
    - path: "src/renderer/panels/EditorPanel.test.tsx"
      provides: "T-I-023, T-I-024, T-I-025, T-I-028, T-I-029, T-I-030 component coverage."
      contains: "T-I-023"
  key_links:
    - from: "src/renderer/panels/EditorPanel.tsx"
      to: "src/renderer/lib/activeEditorRegistry.ts"
      via: "registerActiveEditor/update path includes markdownPreview and preview scroll callback"
      pattern: "registerActiveEditor"
---

<objective>
Render Markdown preview heading IDs and expose a renderer-local preview scroll API for Outline navigation.

Purpose: REQ-017 and REQ-018 require the rendered preview to own stable heading IDs and a testable scroll/flash behavior without Electron IPC or Graph Explorer selection dispatch.
Output: Updated `MarkdownPreview`, active-editor registry bridge, and focused EditorPanel tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-01-SUMMARY.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md

<interfaces>
From src/renderer/lib/activeEditorRegistry.ts:
- `export interface ActiveEditorLike`
- `export interface ActiveEditorSnapshot { panelId: string | null; editor: ActiveEditorLike | null; model: ActiveEditorModelLike | null }`
- `export function registerActiveEditor(workspaceId: string, panelId: string, editor: ActiveEditorLike): void`
- `export function updateActiveEditorModel(workspaceId: string, panelId: string): void`
- `export function getActiveEditorSnapshot(workspaceId: string): ActiveEditorSnapshot`
- `export function subscribeActiveEditor(workspaceId: string, listener: () => void): () => void`

From src/renderer/panels/EditorPanel.tsx:
- Local `MarkdownPreview({ content }: { content: string })` currently renders `ReactMarkdown` with h1-h4 component overrides and `data-testid="markdown-preview-body"`.
- `markdownPreview` state is read from `PanelState.markdownPreview` and toggled with `setPanelMarkdownPreview(workspaceId, panelId, next)`.
</interfaces>
</context>

<tasks>
<task id="23.2.1" type="execute" tdd="true">
  <name>Add MarkdownPreview heading IDs</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` section 2.17. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.9 and 4.1. Use T-I-023 through T-I-025 names/traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-01 through D-09, D-17, D-18, and D-22.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md`.
    - `src/renderer/lib/parseDocumentHeadings.ts` from Plan 23-01.
    - `src/renderer/panels/EditorPanel.tsx` local `MarkdownPreview`.
    - `src/renderer/panels/EditorPanel.test.tsx`.
  </read_first>
  <action>Write failing T-I-023, T-I-024, and T-I-025 tests first. Update local `MarkdownPreview` to import the Plan 23-01 helpers and assign deterministic `id` attributes for h1 through h6 rendered by `ReactMarkdown`. Use a per-render duplicate tracker so the first duplicate uses the base ID and later duplicates append `-1`, `-2`, etc. Generate IDs from stripped/rendered heading text consistently with `slugifyHeading()`. Preserve current heading typography/classes and existing Preview/Source behavior.</action>
  <acceptance_criteria>
    - T-I-023 proves Markdown headings render IDs such as `# Intro` -> `id="intro"`.
    - T-I-024 proves duplicate headings render `intro`, `intro-1`, `intro-2`.
    - T-I-025 proves formatted heading text uses the same stripping/slug helper semantics as `slugifyHeading()`.
    - h1 through h6 receive IDs; current h1-h4 styling remains intact and h5/h6 use Cate-compatible heading styling.
    - Existing EditorPanel Preview tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>`MarkdownPreview` renders deterministic heading IDs for REQ-017 and T-I-023 through T-I-025.</done>
</task>

<task id="23.2.2" type="execute" tdd="true">
  <name>Expose preview scroll and flash behavior</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx, src/renderer/lib/activeEditorRegistry.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.18 and 2.19. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.10 and 5.2. Use T-I-028, T-I-029, and T-I-030 traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-10 through D-12, D-18, D-21, and D-23.
    - `src/renderer/lib/activeEditorRegistry.ts`.
    - `src/renderer/panels/EditorPanel.tsx`.
    - `src/renderer/panels/EditorPanel.test.tsx`.
  </read_first>
  <action>Write failing tests first for smooth scroll, fake-timer flash removal, and non-dispatch. Extend the active-editor registry snapshot/entry narrowly with preview routing data: active panel preview mode and a `scrollPreviewToHeading(headingText: string) => void` callback or equivalent. In `EditorPanel.tsx`, implement the callback inside the renderer by stripping/sluggifying the supplied heading text, finding the matching rendered heading by ID within `markdown-preview-body`, calling `scrollIntoView({ behavior: 'smooth', block: 'start' })`, applying a standalone blue flash equivalent to `rgba(0,122,204,0.2)`, and removing it after 1.5 seconds. No-target headings must return safely without throwing and without source-mode fallback from this API. Do not dispatch `preview-section-select` or any Graph Explorer event.</action>
  <acceptance_criteria>
    - T-I-028 proves `scrollIntoView({ behavior: 'smooth', block: 'start' })` is called on the matching heading.
    - T-I-029 proves blue flash is applied and removed after 1.5 seconds with fake timers.
    - T-I-030 proves `window.dispatchEvent`, document events, or custom events are not called with `preview-section-select`.
    - No-target scroll attempts do not throw.
    - Preview scroll API uses the same slug helper as heading ID render.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>! rg -n "preview-section-select" src/renderer src/shared</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Preview scroll/flash behavior satisfies REQ-018 without Graph Explorer dispatch.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Markdown content -> rendered preview DOM | Untrusted markdown creates heading elements and IDs in the renderer. |
| Outline text -> preview lookup | Outline heading text selects a rendered heading element by normalized ID. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-23-03-T | Tampering | MarkdownPreview IDs | mitigate | Use the shared slug helper from Plan 23-01 and lock IDs with T-I-023 through T-I-025. |
| T-23-04-D | Denial of Service | preview flash timers | mitigate | One bounded 1.5s timer per scroll target; tests verify removal with fake timers. |
| T-23-05-I | Information Disclosure | Graph Explorer dispatch | mitigate | Do not dispatch `preview-section-select`; grep and T-I-030 verify absence. |
| T-23-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `! rg -n "preview-section-select" src/renderer src/shared`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 23.2 is complete when preview headings have deterministic IDs, preview scroll/flash tests pass, and no Graph Explorer dispatch exists.
</success_criteria>

<output>
Create `.planning/phases/23-preview-routing-and-final-hardening/23-02-SUMMARY.md` when done.
</output>
