---
phase: 23-preview-routing-and-final-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - src/renderer/panels/OutlinePanel.tsx
  - src/renderer/panels/OutlinePanel.test.tsx
  - src/renderer/lib/activeEditorRegistry.ts
autonomous: true
requirements:
  - REQ-019
coverage:
  - T-I-026
  - T-I-027
  - T-I-030
must_haves:
  truths:
    - "Outline row clicks route to preview when Markdown preview is active."
    - "Outline Enter-to-cycle routes to preview when Markdown preview is active."
    - "Source mode still uses Monaco reveal/setPosition/focus."
    - "Preview toggling does not unnecessarily reset Outline state."
  artifacts:
    - path: "src/renderer/panels/OutlinePanel.tsx"
      provides: "Preview-aware Outline navigation branch."
      contains: "navigateToHeading"
    - path: "src/renderer/panels/OutlinePanel.test.tsx"
      provides: "T-I-026, T-I-027, and routing non-regression coverage."
      contains: "T-I-026"
    - path: "src/renderer/lib/activeEditorRegistry.ts"
      provides: "Preview state/callback fields consumed by Outline."
      contains: "scrollPreviewToHeading"
  key_links:
    - from: "src/renderer/panels/OutlinePanel.tsx"
      to: "src/renderer/lib/activeEditorRegistry.ts"
      via: "getActiveEditorSnapshot(workspaceId)"
      pattern: "getActiveEditorSnapshot"
---

<objective>
Route Outline navigation to Markdown preview when preview mode is active, while preserving Phase 22 source-mode behavior and panel state.

Purpose: REQ-019 makes Outline row clicks and Enter-to-cycle mode-sensitive. This plan consumes the preview bridge from Plan 23-02 and keeps the existing Monaco path unchanged when preview is inactive.
Output: Updated Outline navigation branch plus focused component tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-03-SUMMARY.md
@.planning/phases/23-preview-routing-and-final-hardening/23-02-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md

<interfaces>
From src/renderer/panels/OutlinePanel.tsx:
- `navigateToHeading(heading: DocumentHeading)` currently guards usable editor/model and calls `editor.revealLineInCenter(heading.line)`, `editor.setPosition({ lineNumber: heading.line, column: 1 })`, and `editor.focus()`.
- `cycleSearch()` advances `searchMatchIdx`, then calls `navigateToHeading(headings[headingIndex])`.
- State already includes `headings`, `maxDepth`, `searchQuery`, `searchMatchIdx`, and derived active heading index.

From Plan 23-02 expected registry shape:
- `ActiveEditorSnapshot` exposes active editor panel id, editor/model, preview mode, and `scrollPreviewToHeading(headingText: string) => void` or equivalent.
</interfaces>
</context>

<tasks>
<task id="23.3.1" type="execute" tdd="true">
  <name>Route row clicks through preview mode</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx, src/renderer/lib/activeEditorRegistry.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.12, 2.18, 2.19, and 2.22. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.7 and 3.10. Use T-I-026 and T-I-030 traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-05, D-06, D-10 through D-16, D-18, and D-23.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-PATTERNS.md`.
    - `src/renderer/lib/activeEditorRegistry.ts` after Plan 23-02.
    - `src/renderer/panels/OutlinePanel.tsx`.
    - `src/renderer/panels/OutlinePanel.test.tsx`.
  </read_first>
  <action>Write the failing T-I-026 test first. Update `navigateToHeading` so when the active editor snapshot says Markdown preview is active and a preview scroll callback exists, row clicks call the preview callback with `heading.text` and do not call `revealLineInCenter`, `setPosition`, or `focus`. When preview is inactive, keep the exact Phase 22 source-mode calls from decision D-15. If preview mode is active but the callback/target is unavailable, degrade safely with no throw and do not dispatch Graph Explorer events or source fallback unless explicitly documented and tested per D-12.</action>
  <acceptance_criteria>
    - T-I-026 proves clicking a heading in preview mode calls preview scroll with the parsed heading text.
    - The same test proves Monaco `revealLineInCenter`, `setPosition`, and `focus` are not called in preview mode.
    - Existing source-mode click test still proves Monaco navigation when preview is inactive.
    - T-I-030 or a companion assertion proves no `preview-section-select` dispatch occurs from Outline routing.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>! rg -n "preview-section-select" src/renderer src/shared</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline row clicks satisfy REQ-019 preview routing while preserving source-mode navigation.</done>
</task>

<task id="23.3.2" type="execute" tdd="true">
  <name>Route Enter-to-cycle through preview and preserve state</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.16, 2.19, and 2.20. If any conflict exists, this product doc wins unless local code makes it impossible.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.7 and 3.10. Use T-I-027 traceability.
    - `.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md` decisions D-14 through D-16 and D-18.
    - `src/renderer/panels/OutlinePanel.tsx`.
    - `src/renderer/panels/OutlinePanel.test.tsx`.
  </read_first>
  <action>Write the failing T-I-027 test first. Ensure `cycleSearch()` reuses the same preview-aware `navigateToHeading` path used by row clicks. Add coverage showing repeated Enter cycles through matches, calls preview scroll with each matched heading text when preview mode is active, wraps at the end, and leaves `searchQuery`, `searchMatchIdx`, `maxDepth`, and parsed `headings` intact across preview on/off snapshot updates except where normal query/depth changes require updates.</action>
  <acceptance_criteria>
    - T-I-027 proves Enter-to-cycle in preview mode calls preview scroll for each cycled match.
    - Enter cycling wraps and keeps search-focus styling behavior from Phase 22.
    - Source-mode Enter-to-cycle still calls Monaco navigation.
    - Toggling preview state through the active-editor snapshot does not clear `searchQuery`, reset `maxDepth`, or unnecessarily drop parsed headings.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline Enter-to-cycle satisfies REQ-019 and preserves Phase 22 Outline state behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Outline component -> active editor adapter | Outline consumes renderer-local active editor state and callback references. |
| Outline heading text -> preview scroll callback | Parsed document text is passed into preview lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-23-06-T | Tampering | Outline routing branch | mitigate | Tests prove preview mode does not accidentally call source navigation and source mode still does. |
| T-23-07-D | Denial of Service | stale editor/callback references | mitigate | Preserve existing disposed editor/model guards and safe no-op behavior for missing preview callbacks. |
| T-23-08-I | Information Disclosure | excluded Graph Explorer events | mitigate | Grep and T-I-030 verify no `preview-section-select` dispatch. |
| T-23-SC | Tampering | npm installs | accept | This plan installs no packages. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx`
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx src/renderer/panels/OutlinePanel.test.tsx`
- `! rg -n "preview-section-select" src/renderer src/shared`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 23.3 is complete when row clicks and Enter-to-cycle route through preview only when preview is active and all Phase 22 source-mode navigation tests still pass.
</success_criteria>

<output>
Create `.planning/phases/23-preview-routing-and-final-hardening/23-03-SUMMARY.md` when done.
</output>
