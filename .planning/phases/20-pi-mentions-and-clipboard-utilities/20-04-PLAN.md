---
phase: 20-pi-mentions-and-clipboard-utilities
plan: 04
type: execute
wave: 3
depends_on:
  - 20-02
  - 20-03
files_modified:
  - e2e/fixtures/flashquery-server.ts
  - e2e/flashquery-pi-mentions.spec.ts
  - e2e/flashquery-vault-search.spec.ts
  - src/renderer/lib/e2eHarness.ts
  - .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md
autonomous: false
requirements:
  - REQ-018
  - REQ-019
must_haves:
  truths:
    - "Downstream implementation agents read the Milestone 2 Requirements and Milestone 2 Test Plan before resolving scope or acceptance questions."
    - "E2E evidence proves Pi chat `@` insertion, workspace-switch cache replacement, disconnect clearing, and no extra footer UI."
    - "E2E/search evidence preserves document-row copy behavior and memory-row no-menu behavior."
    - "UAT records T-M-004 native macOS clipboard verification steps and either passed evidence or an explicit blocker."
  artifacts:
    - path: "e2e/flashquery-pi-mentions.spec.ts"
      provides: "T-E-004 Pi mention E2E coverage"
      contains: "T-E-004"
    - path: "e2e/flashquery-vault-search.spec.ts"
      provides: "Preserved T-E-003 document copy/search behavior"
      contains: "Copy as reference"
    - path: ".planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md"
      provides: "T-M-004 manual native clipboard evidence and source audit"
      contains: "T-M-004"
  key_links:
    - from: "e2e/fixtures/flashquery-server.ts"
      to: "e2e/flashquery-pi-mentions.spec.ts"
      via: "fixture vault-index data"
      pattern: "vault index|list_all|documents"
    - from: "src/renderer/lib/e2eHarness.ts"
      to: "e2e/flashquery-pi-mentions.spec.ts"
      via: "agent panel key discovery and optional workspace/status helpers"
      pattern: "agentPanelIds|dispatchAgentEvent"
---

<objective>
Add Phase 20 E2E and manual evidence traceability for REQ-018 and REQ-019.

Purpose: The phase is not complete until automated Electron coverage proves the user flows and manual UAT captures native clipboard behavior that component tests cannot fully prove.
Output: T-E-004 E2E, preserved T-E-003 checks, and `20-UAT.md` with T-M-004 evidence.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-CONTEXT.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Test Plan.md
@.planning/phases/19-pi-toolcard-observability-rendering/19-02-SUMMARY.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-02-SUMMARY.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-03-SUMMARY.md

<interfaces>
Required source-of-truth docs: before changing tests, evidence, or acceptance, read the two product docs listed above. Per D-01, D-02, and D-03 they define the canonical coverage IDs and pass/fail criteria.

Existing E2E contracts:
- `src/renderer/lib/e2eHarness.ts` already exposes `dispatchAgentEvent(panelId, event)` and `agentPanelIds()` from Phase 19. After the Phase 19 gap fixes, E2E should create an Agent panel, use `agentPanelIds()` to find the active `agent-${panelId}-...` store key, and dispatch mocked events to that active key rather than assuming the dock panel id is the rendered agent store id.
- `e2e/fixtures/flashquery-server.ts` is the FlashQuery fixture source for search/list-all behavior.
- `e2e/flashquery-vault-search.spec.ts` covers T-E-003 search behavior and should be extended only where REQ-019 requires preservation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add T-E-004 Pi mention E2E coverage</name>
  <files>e2e/fixtures/flashquery-server.ts, e2e/flashquery-pi-mentions.spec.ts, src/renderer/lib/e2eHarness.ts</files>
  <read_first>
    - Product requirements §6.18 and §6.20 cache-related acceptance.
    - Product test plan T-E-004 and T-E-007 REQ-018 portions.
    - `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-CONTEXT.md` decisions D-33 through D-35.
    - `.planning/phases/19-pi-toolcard-observability-rendering/19-02-SUMMARY.md` for agent E2E harness behavior.
    - `e2e/fixtures/flashquery-server.ts`, `e2e/flashquery-pi-diagnostics.spec.ts`, and `src/renderer/lib/e2eHarness.ts`.
  </read_first>
  <behavior>
    - Test 1: fixture vault index includes at least two documents whose filenames prove filename-only filtering and fullPath sorting.
    - Test 2: open an Agent panel, type `@`, select a document, and verify the textarea contains literal `{{ref:<fullPath>}}`.
    - Test 3: workspace switch or fixture rebinding replaces cache data so old-workspace documents are not offered.
    - Test 4: simulated disconnect clears cache and prevents stale document insertion; reconnect can repopulate.
    - Test 5: no extra chat footer UI, footer pills, or rich-input chips appear.
  </behavior>
  <action>Create `e2e/flashquery-pi-mentions.spec.ts` for T-E-004. Extend `flashquery-server.ts` only as needed to seed/list vault-index document entries through the existing FlashQuery list-all/search fixture path. Use existing Electron/Playwright fixture helpers and the Phase 19 E2E harness; after creating an Agent panel, resolve the active `agent-${panelId}-...` store key with `agentPanelIds()` before inspecting/dispatching agent state, matching the post-gap-fix diagnostics E2E pattern. Add only minimal harness helpers if the test cannot reach the active Agent input or simulate workspace/status changes through existing helpers. Keep the test on real renderer/main/preload behavior, not a DOM shortcut. Run `npm run build` before E2E when harness or renderer files change because Playwright launches the built Electron bundle.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run test:e2e -- e2e/flashquery-pi-mentions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - T-E-004 passes and asserts literal `{{ref:<fullPath>}}` insertion.
    - Test proves workspace-switch replacement and disconnect clearing for the mention cache.
    - Test includes a negative assertion for no footer/chip additions.
  </acceptance_criteria>
  <done>T-E-004 automated Electron evidence exists for Pi `@` mention behavior and cache lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve search clipboard E2E and write UAT evidence</name>
  <files>e2e/flashquery-vault-search.spec.ts, .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md</files>
  <read_first>
    - Product requirements §6.19.
    - Product test plan T-E-003 and T-M-004.
    - `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-VALIDATION.md` manual-only verification table.
    - `e2e/flashquery-vault-search.spec.ts` existing document copy/search behavior.
    - Plans 20-02 and 20-03 summaries to capture exact implemented surfaces.
  </read_first>
  <behavior>
    - Test 1: T-E-003 still proves search document row `Copy vault path` and `Copy as reference`.
    - Test 2: T-E-003 still proves memory row inspector behavior without a formal memory row context menu.
    - Test 3: `20-UAT.md` maps REQ-018 and REQ-019 to T-U-006, T-U-011, T-U-012, T-U-020, T-E-003, T-E-004, and T-M-004 evidence.
    - Test 4: `20-UAT.md` includes native macOS clipboard steps for vault tree, search row, and editor title actions and records pass/blocker status.
  </behavior>
  <action>Extend `e2e/flashquery-vault-search.spec.ts` only if current T-E-003 does not assert the REQ-019 clipboard behavior from Plan 20-03. Create `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md` with sections for source docs read, automated evidence commands/results, T-M-004 manual steps, expected clipboard strings, and explicit confirmation that no section-anchor or markdown-link variants appear. If live native clipboard verification cannot run in the current environment, mark T-M-004 blocked with concrete missing prerequisites; do not mark it passed without actual manual evidence.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test -- src/main/flashquery/clientManager.test.ts src/main/ipc/flashquery.test.ts src/agent/renderer/agentStore.test.ts src/agent/renderer/AgentChatInput.atMention.test.tsx src/renderer/panels/FlashQueryVaultPanel.test.tsx src/renderer/panels/FlashQueryVaultSearchPanel.test.tsx src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npm run test:e2e -- e2e/flashquery-vault-search.spec.ts e2e/flashquery-pi-mentions.spec.ts</automated>
    <automated>test -f .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md && rg -n "REQ-018|REQ-019|T-U-006|T-U-011|T-U-012|T-U-020|T-E-003|T-E-004|T-M-004|section-anchor|markdown-link" .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - T-E-003 and T-E-004 commands pass or failures are fixed before summary.
    - `20-UAT.md` contains traceability for every Phase 20 targeted test ID and a truthful T-M-004 status.
    - UAT confirms or explicitly blocks native macOS clipboard checks for vault tree, search row, and editor title.
  </acceptance_criteria>
  <done>Automated E2E evidence and manual clipboard UAT traceability close Phase 20 verification requirements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human native clipboard verification checkpoint</name>
  <files>.planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Requirements.md` §6.19.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Test Plan.md` T-M-004.
    - `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md` for the exact evidence template and current automated evidence status.
  </read_first>
  <what-built>Plans 20-01 through 20-03 automated the vault-index cache, Pi mention autocomplete, and clipboard actions. Task 2 records `20-UAT.md` with exact T-M-004 native macOS clipboard steps.</what-built>
  <action>Pause for human verification of T-M-004 after automated tests and `20-UAT.md` setup are complete. The implementation agent must update `20-UAT.md` with the human result before marking the checkpoint done; passed evidence requires pasted clipboard values from vault tree, search row, and editor title actions, while blocked evidence must name the missing runtime, workspace, or native clipboard prerequisite.</action>
  <how-to-verify>
    1. Open Cate on macOS with a workspace connected to FlashQuery fixture or live FlashQuery.
    2. From a vault tree document row, use `Copy vault path`, paste into a plain text target, and verify exact forward-slash path such as `Docs/Plan.md`.
    3. From the same vault tree row, use `Copy as reference`, paste, and verify exact `{{ref:Docs/Plan.md}}`.
    4. Repeat path/reference copy from a search document row and from a FlashQuery editor title Clipboard menu.
    5. Confirm pasted values do not contain `flashquery://`, `%20` for spaces, section anchors, block refs, or markdown-link syntax.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md && rg -n "T-M-004|vault tree|search row|editor title|Copy vault path|Copy as reference|blocked|passed" .planning/phases/20-pi-mentions-and-clipboard-utilities/20-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `20-UAT.md` contains T-M-004 evidence for vault tree, search row, and editor title clipboard actions.
    - Passed evidence includes exact pasted strings; blocked evidence identifies concrete missing prerequisites.
    - Evidence confirms no `flashquery://`, `%20`, section-anchor, block-reference, or markdown-link variant is copied.
  </acceptance_criteria>
  <done>`20-UAT.md` records T-M-004 as passed with pasted native clipboard values for all three surfaces or blocked with concrete prerequisites; no unverified pass is recorded.</done>
  <resume-signal>Type "approved" after T-M-004 evidence is recorded in `20-UAT.md`, or describe the failing surface and copied value.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E fixture to app | Fixture data simulates workspace-scoped FlashQuery responses and must not hide stale-cache failures. |
| renderer to native clipboard | Manual verification proves OS clipboard contents from native menu-driven flows. |
| workspace switch/disconnect to renderer cache | E2E verifies stale async/cache state does not leak between workspaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Information Disclosure | E2E cache fixture | mitigate | T-E-004 must assert old-workspace documents disappear after switch/disconnect before accepting a mention. |
| T-20-14 | Tampering | clipboard/reference evidence | mitigate | T-M-004 requires pasted values exactly match forward-slash paths or `{{ref:path.md}}`; no anchors or markdown links. |
| T-20-15 | Repudiation | UAT traceability | mitigate | `20-UAT.md` records commands, outcomes, blockers, and canonical test IDs for REQ-018/REQ-019. |
| T-20-16 | Information Disclosure | renderer token boundary | mitigate | E2E uses existing app IPC and fixture server; tests must not add renderer token exposure or direct MCP client calls. |
| T-20-SC | Tampering | npm installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run final focused validation:
- `npm run build`
- `npm test -- src/main/flashquery/clientManager.test.ts src/main/ipc/flashquery.test.ts src/agent/renderer/agentStore.test.ts src/agent/renderer/AgentChatInput.atMention.test.tsx src/renderer/panels/FlashQueryVaultPanel.test.tsx src/renderer/panels/FlashQueryVaultSearchPanel.test.tsx src/renderer/panels/EditorPanel.test.tsx`
- `npm run test:e2e -- e2e/flashquery-vault-search.spec.ts e2e/flashquery-pi-mentions.spec.ts`
- `npm run typecheck`
</verification>

<success_criteria>
REQ-018 and REQ-019 have automated and manual/UAT traceability for T-U-006, T-U-011, T-U-012, T-U-020, T-E-003, T-E-004, and T-M-004.
</success_criteria>

<output>
Create `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-04-SUMMARY.md` when done.
</output>
