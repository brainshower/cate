---
phase: 20-pi-mentions-and-clipboard-utilities
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/agent/renderer/AgentChatInput.tsx
  - src/agent/renderer/AgentChatInput.atMention.test.tsx
  - src/agent/renderer/AgentPanel.tsx
autonomous: true
requirements:
  - REQ-018
must_haves:
  truths:
    - "Downstream implementation agents read the Milestone 2 Requirements and Milestone 2 Test Plan before resolving scope or acceptance questions."
    - "User can type `@` in Pi chat and see a dropdown above the textarea using the existing portal pattern."
    - "Autocomplete filters by filename only, sorts by full path, and inserts plain literal `{{ref:<fullPath>}}` text."
    - "The active `@<filter>` segment has a typing-time visual highlight while the mention popover is open and the highlight drops on dismiss, pick, or literal fallback."
    - "Slash-command behavior, footer UI, and rich-input/chip-free text input behavior remain unchanged."
  artifacts:
    - path: "src/agent/renderer/AgentChatInput.tsx"
      provides: "Active `@` segment detection, mention dropdown, keyboard handling, and literal insertion"
      contains: "Loading vault..."
    - path: "src/agent/renderer/AgentChatInput.atMention.test.tsx"
      provides: "T-U-020 component coverage"
      contains: "REQ-018"
    - path: "src/agent/renderer/AgentPanel.tsx"
      provides: "Passes `vaultIndex` and `vaultIndexLoading` from store into ChatInput"
      contains: "vaultIndexLoading"
  key_links:
    - from: "src/agent/renderer/AgentPanel.tsx"
      to: "src/agent/renderer/AgentChatInput.tsx"
      via: "props"
      pattern: "vaultIndex"
    - from: "src/agent/renderer/AgentChatInput.tsx"
      to: "src/agent/renderer/agentStore.ts"
      via: "read-only entries from props"
      pattern: "FlashQueryVaultIndexEntry"
---

<objective>
Add the Pi chat `@` mention UI that consumes the workspace-scoped vault-index cache from Plan 20-01.

Purpose: REQ-018 becomes user-visible only when the composer can detect active mention segments, show valid matches, and insert literal FlashQuery references without changing Pi chat chrome.
Output: Component implementation plus T-U-020 coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-CONTEXT.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-RESEARCH.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Continued FQ integration (milestone 2)/Milestone 2 Test Plan.md
@.planning/phases/20-pi-mentions-and-clipboard-utilities/20-01-SUMMARY.md

<interfaces>
Required source-of-truth docs: before changing code or judging acceptance, read the two product docs listed above. Per D-01, D-02, and D-03 they define REQ-018 and T-U-020.

Current composer contracts:
- `AgentChatInput.tsx` imports `createPortal`, defines `useNodePortalTarget`, derives `slashMatch`, and renders `SlashPopup`.
- Existing slash popup keyboard handling uses ArrowUp, ArrowDown, Enter, Tab, and Escape.
- Plan 20-01 adds `vaultIndex: FlashQueryVaultIndexEntry[]` and `vaultIndexLoading: boolean` to the active `AgentPanel` store slice.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add T-U-020 mention component tests</name>
  <files>src/agent/renderer/AgentChatInput.atMention.test.tsx, src/agent/renderer/AgentChatInput.tsx</files>
  <read_first>
    - Product requirements §6.18 acceptance items 1-4 and 8-14.
    - Product test plan T-U-020.
    - `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-CONTEXT.md` decisions D-05 through D-16 and D-34.
    - `src/agent/renderer/AgentChatInput.tsx` slash command detection, portal target hook, keydown handler, footer button row, and popup rendering.
  </read_first>
  <behavior>
    - Test 1: typing `@pla` with entries for `Plan.md` and `Notes.md` opens a portal dropdown above the textarea and shows filename plus fullPath rows.
    - Test 2: matching uses case-insensitive substring on `filename` only and sorts matches by `fullPath`.
    - Test 3: ArrowUp/ArrowDown changes selection; Enter and Tab accept; Esc dismisses.
    - Test 4: accepting replaces only the active `@<filter>` segment with `{{ref:<fullPath>}}`.
    - Test 5: no-match plus space keeps literal text and dismisses; slash popup behavior keeps priority and is unchanged.
    - Test 6: typing-time visual highlight appears on the active `@<filter>` segment while the popover is open and drops on dismiss, pick, and literal fallback.
    - Test 7: loading state text is exactly `Loading vault...`; no footer buttons, footer pills, or rich-input chips are added.
  </behavior>
  <action>Create `AgentChatInput.atMention.test.tsx` or extend an existing co-located test only if one already covers this component. Mock only component props and DOM events; do not add IPC or store dependencies to the component test. Add test IDs only where needed for stable assertions. Include `REQ-018` and `T-U-020` in test names or nearby comments for traceability. Require assertions that the active `@<filter>` segment receives a typing-time visual highlight while the mention popover is open and that the highlight is removed on Escape dismiss, successful pick, and no-match space literal fallback.</action>
  <verify>
    <automated>npm test -- src/agent/renderer/AgentChatInput.atMention.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation and cover every behavior listed in T-U-020, including active-segment typing highlight appearance and removal.
    - Test assertions include absence of new footer buttons/pills/chips.
  </acceptance_criteria>
  <done>T-U-020 component tests exist and describe the required mention behavior before production code is finished.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mention detection, dropdown, and literal insertion</name>
  <files>src/agent/renderer/AgentChatInput.tsx, src/agent/renderer/AgentPanel.tsx, src/agent/renderer/AgentChatInput.atMention.test.tsx</files>
  <read_first>
    - Product requirements §6.18 acceptance items 1-18.
    - Product test plan T-U-020 and T-E-004.
    - `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-PATTERNS.md` `AgentChatInput` and `AgentPanel` sections.
    - `src/agent/renderer/AgentPanel.tsx` ChatInput prop pass-through.
  </read_first>
  <action>Extend `ChatInput` props with `vaultIndex` and `vaultIndexLoading` using the existing `FlashQueryVaultIndexEntry` type. In `AgentPanel.tsx`, pass active slice values from Plan 20-01. In `AgentChatInput.tsx`, derive an active mention segment from `textarea.selectionStart`: segment starts at the nearest `@` before the cursor, has no whitespace/newline between `@` and cursor, and dismisses when the cursor moves before the segment. Reuse `useNodePortalTarget` and `createPortal` so the dropdown appears above the textarea. When loading, render exactly `Loading vault...`. When not loading, filter entries by `entry.filename.toLowerCase().includes(filter.toLowerCase())`, sort by `entry.fullPath.localeCompare`, and show both filename and fullPath. Implement ArrowUp/ArrowDown, Enter/Tab accept, Escape dismiss, mouse hover/pick, and no-match space literal fallback. Implement typing-time visual highlighting for the active `@<filter>` segment while the mention popover is open; remove that highlight on Escape dismiss, successful pick, cursor exit, and no-match space literal fallback. The highlight must be visual only and must not create rich chips, editable tokens, footer UI, or a second input model. Preserve slash popup priority, existing submit behavior, and the current footer row exactly; add no footer buttons, pills, chips, or new chat message types.</action>
  <verify>
    <automated>npm test -- src/agent/renderer/AgentChatInput.atMention.test.tsx</automated>
    <automated>npm test -- src/agent/renderer/agentStore.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All T-U-020 assertions pass, including typing-time visual highlight appearance while open and removal on dismiss/pick/literal fallback.
    - `rg -n "Loading vault\\.\\.\\.|\\{\\{ref:" src/agent/renderer/AgentChatInput.tsx src/agent/renderer/AgentChatInput.atMention.test.tsx` finds the required literal behavior.
    - `rg -n "chip|footer pill|document-reference" src/agent/renderer/AgentChatInput.tsx | grep -v '^#'` finds no new rich reference UI introduced by this plan.
  </acceptance_criteria>
  <done>Pi chat mention autocomplete inserts whole-document references as plain text and preserves existing slash/footer behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache state to composer UI | Workspace-scoped document names become visible in Pi composer suggestions. |
| keyboard input to prompt text | User text is transformed by accept/fallback handlers and must preserve literal reference integrity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | mention dropdown | mitigate | Consume only Plan 20-01 active-workspace cache; do not fetch tokens or list vault directly in `AgentChatInput.tsx`. |
| T-20-06 | Tampering | literal insertion | mitigate | Replace only the active `@<filter>` segment and insert exactly `{{ref:<fullPath>}}`; no anchors, markdown links, or rich chips. |
| T-20-07 | Denial of Service | keyboard event handling | mitigate | Give slash popup priority and handle mention keys only while mention dropdown is active; preserve Enter submit behavior otherwise. |
| T-20-08 | Information Disclosure | stale async responses | mitigate | Depend on Plan 20-01 last-fetch-wins cache; this plan must not introduce a second cache or direct fetch path. |
| T-20-SC | Tampering | npm installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run focused validation:
- `npm test -- src/agent/renderer/AgentChatInput.atMention.test.tsx`
- `npm test -- src/agent/renderer/agentStore.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
REQ-018 mention UI is complete per D-05 through D-16 and D-34, backed by Plan 20-01 cache state and without new chat footer/chip UI.
</success_criteria>

<output>
Create `.planning/phases/20-pi-mentions-and-clipboard-utilities/20-02-SUMMARY.md` when done.
</output>
