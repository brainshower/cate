---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/types.test.ts
  - src/main/workspaceManager.ts
  - src/main/workspaceManager.test.ts
  - src/main/projectWorkspaceStore.ts
  - src/renderer/stores/appStore.ts
  - src/renderer/lib/session.ts
autonomous: true
requirements:
  - REQ-001
requirements_addressed: [REQ-001]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03: Downstream implementation reads the external Cate-FlashQuery Requirements and Test Plan before editing or asking questions."
    - "D-04: This plan implements only REQ-001 from the Phase 1 scope."
    - "D-07 D-08: WorkspaceInfo includes optional flashqueryConnection metadata with the v1 HTTP FlashQueryConnection shape and without making FlashQuery required for existing workspaces."
    - "D-09: Workspace persistence and restore preserve a valid HTTP FlashQuery connection and treat malformed connection data as absent."
    - "D-05 D-06: This plan does not add network behavior, MCP clients, Supabase/vault access, IPC handlers, preload APIs, renderer UI, or FlashQuery runtime ownership."
    - Renderer workspace/session merge paths preserve flashqueryConnection when WorkspaceInfo crosses window or session boundaries.
  artifacts:
    - src/shared/types.ts defines FlashQueryConnection and WorkspaceInfo.flashqueryConnection.
    - src/main/workspaceManager.test.ts covers T-U-002, T-U-003, and T-U-004.
    - src/shared/types.test.ts or an equivalent compile/runtime type test covers T-U-001.
  key_links:
    - src/main/workspaceManager.ts returns WorkspaceInfo values whose flashqueryConnection shape matches src/shared/types.ts.
    - src/renderer/stores/appStore.ts preserves flashqueryConnection from main-process WorkspaceInfo broadcasts.
---

<objective>
Add the workspace-level FlashQuery connection contract and preserve it through Cate's existing workspace metadata paths. Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, and D-09.

Purpose: Phase 1 needs optional per-workspace connection metadata before credentials, manager lifecycle, IPC, or UI can safely build on it.
Output: Shared FlashQuery connection types, sanitized workspace persistence behavior, and unit coverage for T-U-001 through T-U-004.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
@src/shared/types.ts
@src/main/workspaceManager.ts
@src/main/projectWorkspaceStore.ts
@src/renderer/stores/appStore.ts
@src/renderer/lib/session.ts
</context>

<threat_model>
Trust boundaries:
- Renderer and persisted project/session JSON are untrusted inputs.
- Main process owns authoritative WorkspaceInfo validation and workspace metadata broadcasts.

STRIDE register:
- threat_id: FQ-P1-01-TOKEN-IN-WORKSPACE
  category: Information disclosure
  component: WorkspaceInfo / WorkspaceState persistence
  disposition: mitigate
  mitigation_plan: Do not require or seed raw bearer tokens in project fixtures or renderer state. The type may include auth for the future IPC contract, but Phase 1 tests must not persist real secrets.
- threat_id: FQ-P1-02-MALFORMED-CONFIG
  category: Tampering / Denial of service
  component: project workspace restore and workspaceManager update paths
  disposition: mitigate
  mitigation_plan: Add a narrow FlashQueryConnection sanitizer/type guard that treats malformed persisted data as undefined instead of crashing restore.
- threat_id: FQ-P1-03-SCOPE-CREEP-IPC
  category: Elevation of privilege
  component: Electron main/preload boundary
  disposition: avoid
  mitigation_plan: Do not add flashquery IPC channels, preload APIs, network calls, or renderer UI in this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add FlashQuery connection shared contract</name>
  <files>src/shared/types.ts, src/shared/types.test.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, .planning/phases/01-foundation/01-CONTEXT.md, src/shared/types.ts</read_first>
  <action>Add exported FlashQueryConnection as a discriminated union over transport with the v1 HTTP variant `{ transport: 'http'; url: string; auth?: { type: 'bearer'; token: string } }`. Add optional `flashqueryConnection?: FlashQueryConnection` to `WorkspaceInfo`. Keep the change additive: existing WorkspaceInfo object literals with id/name/color/rootPath remain valid. Add a colocated type/runtime test that proves `flashqueryConnection?` is optional and absent by default for a minimal WorkspaceInfo object.</action>
  <verify>npm test -- src/shared/types.test.ts</verify>
  <acceptance_criteria>
    - `src/shared/types.ts` contains `export type FlashQueryConnection`.
    - `src/shared/types.ts` contains `flashqueryConnection?: FlashQueryConnection` inside `WorkspaceInfo`.
    - Test coverage includes `T-U-001` by asserting a WorkspaceInfo without `flashqueryConnection` remains valid and has `flashqueryConnection === undefined`.
  </acceptance_criteria>
  <done>T-U-001 passes and no existing WorkspaceInfo construction needs a required FlashQuery field.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve and sanitize connection metadata in workspace persistence</name>
  <files>src/main/workspaceManager.ts, src/main/workspaceManager.test.ts, src/main/projectWorkspaceStore.ts, src/renderer/lib/session.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md, .planning/phases/01-foundation/01-RESEARCH.md, src/main/workspaceManager.ts, src/main/projectWorkspaceStore.ts, src/renderer/lib/session.ts</read_first>
  <action>Add a small FlashQueryConnection sanitizer/type guard in the closest shared-safe location that avoids main-to-renderer import cycles. Use it in workspace create/update/restore paths that read persisted or renderer-originated workspace data. Valid data must preserve `transport: 'http'`, `url`, and optional bearer auth shape. Invalid, malformed, or non-object `flashqueryConnection` values must become `undefined` and must not fail workspace restore. Extend or create workspaceManager/project persistence tests for T-U-002, T-U-003, and T-U-004.</action>
  <verify>npm test -- src/main/workspaceManager.test.ts</verify>
  <acceptance_criteria>
    - A WorkspaceInfo with `flashqueryConnection: { transport: 'http', url: 'http://127.0.0.1:8787', auth: { type: 'bearer', token: 'test-token' } }` round-trips through the tested persistence path unchanged for transport/url/auth shape.
    - A WorkspaceInfo with no `flashqueryConnection` persists/restores with `flashqueryConnection === undefined`.
    - Malformed persisted values such as `{ transport: 'ftp', url: 123 }`, `null`, and a string are handled as absent connection metadata.
    - No test fixture stores a real secret; use literal dummy values like `test-token`.
  </acceptance_criteria>
  <done>T-U-002, T-U-003, and T-U-004 pass.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve connection metadata in renderer workspace sync</name>
  <files>src/renderer/stores/appStore.ts, src/renderer/lib/session.ts</files>
  <read_first>/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md, .planning/phases/01-foundation/01-RESEARCH.md, src/renderer/stores/appStore.ts, src/renderer/lib/session.ts</read_first>
  <action>Update renderer workspace state application and cross-window merge logic so `flashqueryConnection` from WorkspaceInfo is copied into the renderer workspace state when present and preserved when unchanged. Ensure session serialization does not accidentally drop the optional field for workspaces that have it. Keep this as metadata preservation only; do not add UI, dialogs, IPC methods, or network behavior.</action>
  <verify>npm test -- src/renderer/stores/appStore.test.ts src/renderer/lib/session.test.ts</verify>
  <acceptance_criteria>
    - `applyWorkspaceInfo` or its equivalent copies `info.flashqueryConnection` into the renderer workspace state.
    - `mergeWorkspaceInfos` considers `flashqueryConnection` when deciding whether to update existing workspace metadata.
    - Renderer session code preserves the optional field without requiring it for old sessions.
    - No `window.electronAPI.flashquery*` API is introduced in this plan.
  </acceptance_criteria>
  <done>Renderer metadata sync preserves the Phase 1 connection field and does not introduce Phase 3 IPC work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/shared/types.test.ts src/main/workspaceManager.test.ts`
- [ ] `npm run typecheck`
- [ ] `rg -n "flashqueryConnection\\?: FlashQueryConnection|export type FlashQueryConnection" src/shared/types.ts`
- [ ] `rg -n "flashquery:" src/preload src/shared/ipc-channels.ts src/main/ipc` shows no new `flashquery:*` IPC channels from this plan.
</verification>

<success_criteria>
- REQ-001 is fully represented in source and tests.
- T-U-001, T-U-002, T-U-003, and T-U-004 pass.
- Existing workspaces without FlashQuery metadata continue to load.
- Valid FlashQuery connection metadata survives workspace persistence and renderer sync.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
