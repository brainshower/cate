---
phase: 25-inspector-ui-adapter-boundary-outline-sync-e2e-and-acceptance-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - src/renderer/panels/OutlinePanel.tsx
  - src/renderer/panels/OutlinePanel.test.tsx
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/panels/SemanticConnectionsPanel.tsx
  - src/renderer/panels/SemanticConnectionsPanel.test.tsx
  - src/renderer/lib/activeEditorRegistry.ts
  - src/renderer/lib/activeEditorRegistry.test.ts
  - src/renderer/lib/semanticConnectionsProvider.ts
autonomous: true
requirements:
  - REQ-010
  - REQ-011
  - REQ-012
  - REQ-036
  - REQ-037
coverage:
  - T-I-036
  - T-I-037
  - T-I-038
  - T-I-039
  - T-E-004
  - T-E-006
must_haves:
  truths:
    - "Outline shared-selection highlight must preserve cursor fallback and search behavior."
    - "Two-pass matching is slug/occurrence first, preview DOM fallback second."
    - "Card open actions must navigate only when metadata is sufficient."
  artifacts:
    - path: "src/renderer/panels/OutlinePanel.tsx"
      provides: "Final Outline bidirectional selection behavior."
      contains: "activeChunkId"
    - path: "src/renderer/panels/SemanticConnectionsPanel.tsx"
      provides: "Card open action behavior."
      contains: "Open"
---

<objective>
Complete bidirectional Outline synchronization and implement safe Semantic Connections card open/deep-link behavior.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-inspector-ui-adapter-boundary-outline-sync-e2e-and-acceptance-polish/25-CONTEXT.md
@.planning/phases/25-inspector-ui-adapter-boundary-outline-sync-e2e-and-acceptance-polish/25-RESEARCH.md
@.planning/phases/25-inspector-ui-adapter-boundary-outline-sync-e2e-and-acceptance-polish/25-PATTERNS.md
@.planning/phases/24-sc-inspector-foundation-docking-preview-chunks-and-selection/24-04-SUMMARY.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/23-preview-routing-and-final-hardening/23-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md
</context>

<tasks>
<task id="25.3.1" type="execute" tdd="true">
  <name>Finish Outline bidirectional selection sync</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx, src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx, src/renderer/lib/activeEditorRegistry.ts, src/renderer/lib/activeEditorRegistry.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` REQ-010, REQ-011, and REQ-012.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 3.6 and 4.5.
    - `src/renderer/panels/OutlinePanel.tsx`.
    - `src/renderer/panels/EditorPanel.tsx`.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/stores/previewSelectionStore.ts`.
  </read_first>
  <action>Write failing Outline tests first. Preserve Phase 24's shared-selection highlight, then add final behavior: when `activeChunkId` exists, Outline highlights the matching heading; when it does not, existing cursor-derived active heading remains; search-focused styling remains distinct and Enter-to-cycle behavior stays unchanged. Outline clicks in preview mode must call `scrollPreviewToHeading(heading.text, occurrenceIndex)` and pin the matching chunk through `usePreviewSelectionStore.getState().selectSection(chunkId)`. Add two-pass matching so slug/occurrence matching is attempted first and preview DOM fallback can resolve `[data-chunk-id]` by reading the first heading text inside the chunk and matching heading text plus occurrence.</action>
  <acceptance_criteria>
    - `T-I-036` proves shared active selection highlight and cursor fallback.
    - `T-I-037` proves Outline click in preview mode scrolls and pins, while source mode reveal/cursor behavior remains unchanged.
    - `T-I-038` proves slug matching and DOM fallback both resolve the correct row.
    - `T-I-039` proves Outline search Enter cycling remains functional.
    - `T-E-004` remains passing after the changes.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx src/renderer/lib/activeEditorRegistry.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline selection, preview pinning, search, and source-mode navigation satisfy the final sync contract.</done>
</task>

<task id="25.3.2" type="execute" tdd="true">
  <name>Implement safe card open behavior</name>
  <files>src/renderer/panels/SemanticConnectionsPanel.tsx, src/renderer/panels/SemanticConnectionsPanel.test.tsx, src/renderer/panels/EditorPanel.tsx, src/renderer/lib/activeEditorRegistry.ts, src/renderer/lib/semanticConnectionsProvider.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Requirements.md` REQ-036 and REQ-037.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Semantic Connections Inspector Test Plan.md` sections 3.2, 3.8, and 3.9.
    - `src/renderer/panels/SemanticConnectionsPanel.tsx`.
    - `src/renderer/stores/appStore.ts`.
    - `src/renderer/lib/activeEditorRegistry.ts`.
    - `src/shared/flashqueryUri.ts`.
  </read_first>
  <action>Write failing tests first. Add card open actions that are focusable buttons and do not toggle expansion. For same-document targets with heading/chunk metadata, open should scroll the current preview to the target heading/chunk and select/pin the target chunk. For cross-document targets with enough path and heading metadata, open should create or focus the target editor in preview mode when available through existing app-store/editor routing, then scroll to the heading/chunk. If metadata is incomplete, disable the button or show a recoverable inline error and never navigate to a guessed target.</action>
  <acceptance_criteria>
    - Same-document open scrolls to target heading/chunk and updates shared selection when metadata is sufficient.
    - Cross-document open uses existing editor creation/routing paths and scrolls after the document is available.
    - Open button is disabled or recoverably errors when `documentPath`, heading, or chunk metadata is insufficient.
    - Open button has an accessible label that names the target document/heading.
    - Card keyboard behavior remains compatible with `T-I-013` and `T-E-008`.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/SemanticConnectionsPanel.test.tsx src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Connection cards navigate safely when metadata permits and fail safely when it does not.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Preview DOM -> Outline matching fallback | DOM-derived heading text influences Outline row selection. |
| Connection metadata -> editor routing | Provider target metadata drives same-document and cross-document navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-25-08-T | Tampering | Outline matching | mitigate | Prefer slug/occurrence matching, test DOM fallback, fail quietly when unresolved. |
| T-25-09-S | Spoofing | card open metadata | mitigate | Require sufficient document/heading metadata before navigation; disable otherwise. |
| T-25-10-D | Denial of Service | search and source-mode regressions | mitigate | Keep explicit regression tests for search cycling and source reveal. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx src/renderer/panels/SemanticConnectionsPanel.test.tsx src/renderer/lib/activeEditorRegistry.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 25.3 is complete when Outline sync satisfies REQ-010 through REQ-012, card open actions satisfy REQ-036, and accessibility/keyboard behavior remains intact.
</success_criteria>

<output>
Create `.planning/phases/25-inspector-ui-adapter-boundary-outline-sync-e2e-and-acceptance-polish/25-03-SUMMARY.md` when done.
</output>
