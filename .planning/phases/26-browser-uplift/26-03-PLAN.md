---
phase: 26-browser-uplift
plan: 03
type: execute
wave: 3
depends_on:
  - 26-01
files_modified:
  - src/main/ipc/capture.ts
  - src/main/ipc/capture.test.ts
  - src/main/index.ts
  - src/shared/ipc-channels.ts
  - src/preload/index.ts
  - src/shared/electron-api.d.ts
  - src/renderer/panels/BrowserPanel.tsx
  - e2e/browser-uplift.spec.ts
autonomous: true
requirements:
  - REQ-010
must_haves:
  truths:
    - "Screenshot/page capture handlers live in a focused main IPC module."
    - "WEBVIEW_SCREENSHOT keeps Desktop PNG plus filePath/dataUrl result and guest ownership validation."
    - "No proxy, extraction, vault write, FlashQuery server, or cate_browser handlers are registered."
  artifacts:
    - path: src/main/ipc/capture.ts
      provides: registerCaptureHandlers for capture/screenshot/native drag
    - path: src/main/index.ts
      provides: single registration call
    - path: src/main/ipc/capture.test.ts
      provides: capture ownership and negative registration tests
  key_links:
    - from: src/main/index.ts
      to: src/main/ipc/capture.ts
      via: registerCaptureHandlers()
      pattern: registerCaptureHandlers
    - from: src/preload/index.ts
      to: src/shared/electron-api.d.ts
      via: unchanged webviewScreenshot contract
      pattern: webviewScreenshot
---

<objective>
Relocate screenshot and page capture IPC without changing renderer-facing behavior.

Purpose: Satisfy REQ-010 and create a clean future capture home while excluding extraction/proxy work.
Output: registerCaptureHandlers(), removed inline main/index handlers, IPC tests, and screenshot E2E.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
MANDATORY FIRST READ, before implementation or scope decisions, per D-01 and D-02:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Test Plan.md

Then read:
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-browser-uplift/26-CONTEXT.md
@.planning/phases/26-browser-uplift/26-RESEARCH.md
@.planning/phases/26-browser-uplift/26-VALIDATION.md
@.planning/phases/26-browser-uplift/26-PATTERNS.md
</context>
<context>
@src/main/index.ts
@src/shared/ipc-channels.ts
@src/preload/index.ts
@src/shared/electron-api.d.ts
@src/renderer/panels/BrowserPanel.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Capture IPC module and ownership tests</name>
  <files>src/main/ipc/capture.ts, src/main/ipc/capture.test.ts, src/main/index.ts</files>
  <behavior>
    - T-I-004 authorized webview screenshot writes PNG and returns filePath/dataUrl result.
    - T-I-005 unauthorized webContentsId is denied without capture.
    - T-I-006 registration does not include proxy or extraction handlers.
  </behavior>
  <action>Move CAPTURE_PAGE, WEBVIEW_SCREENSHOT, and NATIVE_FILE_DRAG handler registration from src/main/index.ts into src/main/ipc/capture.ts with exported registerCaptureHandlers(). Preserve current Desktop PNG naming, nativeImage/dataUrl behavior, and webview guest ownership validation using BrowserWindow/fromWebContents and hostWebContents checks per D-12. Register exactly once from startup and remove inline handler bodies. Do not import upstream proxy, DOM extraction, Readability, Turndown, PDF/DOCX, vault write, cate_browser, or FlashQuery code per D-06.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/main/ipc/capture.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-I-004, T-I-005, and T-I-006 pass; capture registration is modular and single-registered.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Preserve preload/API screenshot contract and E2E</name>
  <files>src/shared/ipc-channels.ts, src/preload/index.ts, src/shared/electron-api.d.ts, src/renderer/panels/BrowserPanel.tsx, e2e/browser-uplift.spec.ts</files>
  <behavior>
    - T-E-016 screenshot button still creates draggable screenshot thumbnail from a local page.
  </behavior>
  <action>Keep existing channel names and preload methods for capturePage(), webviewScreenshot(), and nativeFileDrag() unchanged unless type declarations require relocation cleanup. Ensure BrowserPanel screenshot button still calls window.electronAPI.webviewScreenshot(webContentsId) and consumes filePath/dataUrl result | null. Add local-page E2E coverage for the screenshot thumbnail path.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/main/ipc/capture.test.ts</automated>
    <automated>npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-E-016 passes and renderer-facing screenshot behavior is unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload/main | Renderer requests privileged browser operations through typed preload APIs. |
| webview guest -> host app | Untrusted web content runs in Electron guest webContents. |
| workspaceId -> browser storage | Workspace identifiers select persistent browser state and Chromium partitions. |
| browser state -> FlashQuery state | Browser state shares workspaceId but must not touch FlashQuery credentials, vaults, indexes, clients, or MCP sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-S | Tampering | WEBVIEW_SCREENSHOT webContentsId | mitigate | Validate host/caller ownership before capture. |
| T-26-03-I | Tampering | Desktop PNG/dataUrl | mitigate | Deny unrelated webContents and preserve safe null/error behavior. |
| T-26-03-E | Tampering | capture module scope | mitigate | Negative registration test forbids proxy/extraction/native guest expansion. |
| T-26-03-SC | Tampering | npm installs | accept | No new packages are planned; Package Legitimacy Audit says installs are not applicable. |
</threat_model>

<verification>
Run npx -p node@22 npm test -- src/main/ipc/capture.test.ts first, then npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts and npx -p node@22 npm run typecheck.
</verification>
<success_criteria>REQ-010 and T-I-004, T-I-005, T-I-006, T-E-016 are implemented in this plan without changing existing screenshot contracts or adding excluded capture/extraction features.</success_criteria>
<source_audit>
GOAL: Browser Foundation modular screenshot IPC.
REQ: REQ-010 covered.
RESEARCH: Main boundary owns privileged capture; handler double-registration pitfall addressed.
CONTEXT: Implements D-01, D-02, D-06, D-12, D-26, D-28, D-30.
TESTS: T-I-004, T-I-005, T-I-006, T-E-016.
</source_audit>
<output>Create .planning/phases/26-browser-uplift/26-03-SUMMARY.md when done. The summary must state that the two product docs were read first.</output>
