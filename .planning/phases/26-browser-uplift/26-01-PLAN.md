---
phase: 26-browser-uplift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/panels/browserPartition.ts
  - src/renderer/panels/browserPartition.test.ts
  - src/renderer/panels/BrowserPanel.tsx
  - src/renderer/panels/BrowserPanel.test.tsx
  - src/renderer/shells/PanelWindowShell.tsx
  - src/renderer/shells/DockWindowShell.tsx
  - src/renderer/canvas/CanvasNode.tsx
  - e2e/browser-uplift.spec.ts
  - e2e/fixtures/electron-app.ts
autonomous: true
requirements:
  - REQ-001
must_haves:
  truths:
    - "Browser webviews use non-empty persist:browser-ws-${workspaceId} partitions, never panel-scoped partitions."
    - "Canvas, dock, detached panel, detached dock, same-workspace multi-window, and new workspace mounts all thread a real workspaceId."
    - "Local cookie/session state persists in one workspace and remains isolated from other workspaces."
  artifacts:
    - path: src/renderer/panels/browserPartition.ts
      provides: tested browserPartitionForWorkspace helper
    - path: src/renderer/panels/BrowserPanel.tsx
      provides: partition fail-closed wiring
    - path: e2e/browser-uplift.spec.ts
      provides: T-E-001/002/003/020/021 partition coverage
  key_links:
    - from: src/renderer/panels/BrowserPanel.tsx
      to: src/renderer/panels/browserPartition.ts
      via: browserPartitionForWorkspace(workspaceId)
      pattern: browserPartitionForWorkspace
    - from: src/renderer/shells/PanelWindowShell.tsx
      to: src/renderer/panels/BrowserPanel.tsx
      via: real workspaceId prop or withheld rendering
      pattern: workspaceId
---

<objective>
Implement the workspace-scoped browser partition foundation.

Purpose: Satisfy REQ-001 before state, cleanup, or UI features rely on browser storage boundaries.
Output: Partition helper, BrowserPanel mount threading, focused unit tests, and partition E2E coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
MANDATORY FIRST READ, before implementation or scope decisions, per D-01 and D-02:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Test Plan.md

Then read:
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-browser-uplift/26-CONTEXT.md
@.planning/phases/26-browser-uplift/26-RESEARCH.md
@.planning/phases/26-browser-uplift/26-VALIDATION.md
@.planning/phases/26-browser-uplift/26-PATTERNS.md
</context>

<context>
@src/renderer/panels/BrowserPanel.tsx
@src/renderer/panels/browserUrl.ts
@src/renderer/panels/BrowserPanel.test.ts
@src/renderer/shells/PanelWindowShell.tsx
@src/renderer/shells/DockWindowShell.tsx
@src/renderer/canvas/CanvasNode.tsx
@e2e/fixtures/electron-app.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Partition helper and BrowserPanel fail-closed mount</name>
  <files>src/renderer/panels/browserPartition.ts, src/renderer/panels/browserPartition.test.ts, src/renderer/panels/BrowserPanel.tsx, src/renderer/panels/BrowserPanel.test.tsx</files>
  <behavior>
    - T-U-001: browserPartitionForWorkspace('abc') returns exactly persist:browser-ws-abc and never includes panelId.
    - T-U-002: distinct workspace IDs produce distinct partition strings.
    - T-U-029: empty or whitespace workspaceId fails closed and never returns persist:browser-ws-.
  </behavior>
  <action>Create browserPartitionForWorkspace(workspaceId: string) per D-08/D-09. It trims, rejects empty IDs, and returns exactly persist:browser-ws-${workspaceId}. Wire BrowserPanel to use this helper before rendering the webview; when workspaceId is missing, render an explicit non-webview error state and do not mount a webview. Do not implement old per-panel cookie migration, tabs, start page, autocomplete, proxy, extraction, FlashQuery server changes, or browser-to-FlashQuery state coupling per D-06/D-07.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/browserPartition.test.ts src/renderer/panels/BrowserPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-U-001, T-U-002, and T-U-029 pass; BrowserPanel cannot mount a panel-scoped or empty workspace browser partition.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: WorkspaceId threading and partition E2E</name>
  <files>src/renderer/shells/PanelWindowShell.tsx, src/renderer/shells/DockWindowShell.tsx, src/renderer/canvas/CanvasNode.tsx, e2e/browser-uplift.spec.ts, e2e/fixtures/electron-app.ts</files>
  <behavior>
    - T-E-001: local cookie/session persists across restart and browser-panel recreation in the same workspace.
    - T-E-002: workspace A and workspace B do not share local cookie/session state.
    - T-E-003: same workspace opened in two windows shares local cookie/session state.
    - T-E-020: detached browser window uses the same workspace partition and never an empty partition.
    - T-E-021: brand-new pre-first-save workspace gets a stable partition reused after restart.
  </behavior>
  <action>Fix canvas, dock, detached panel, and detached dock mount sites to pass the owning workspaceId to BrowserPanel; specifically remove the detached fallback that can pass workspaceId ?? ''. Add CATE_E2E-gated harness helpers only as needed for deterministic browser-panel creation, detachment, restart, and local cookie/localStorage checks. Keep E2E local-HTTP based per D-28.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/browserPartition.test.ts src/renderer/panels/BrowserPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-E-001, T-E-002, T-E-003, T-E-020, and T-E-021 prove durable workspace partitions across restart/window/detach/new-workspace paths.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload/main | Renderer requests privileged browser operations through typed preload APIs. |
| webview guest -> host app | Untrusted web content runs in Electron guest webContents. |
| workspaceId -> browser storage | Workspace identifiers select persistent browser state and Chromium partitions. |
| browser state -> FlashQuery state | Browser state shares workspaceId but must not touch FlashQuery credentials, vaults, indexes, clients, or MCP sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-T | Tampering | partition helper | mitigate | Reject empty workspace IDs and use one tested helper for renderer partition strings. |
| T-26-01-I | Tampering | workspace browser storage | mitigate | E2E proves same-workspace reuse and cross-workspace isolation. |
| T-26-01-D | Tampering | BrowserPanel mount | mitigate | Fail closed without mounting a webview when workspaceId is missing. |
| T-26-01-SC | Tampering | npm installs | accept | No new packages are planned; Package Legitimacy Audit says installs are not applicable. |
</threat_model>

<verification>
Run fast checks first: npx -p node@22 npm test -- src/renderer/panels/browserPartition.test.ts src/renderer/panels/BrowserPanel.test.tsx. Then run npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts and npx -p node@22 npm run typecheck.
</verification>

<success_criteria>
REQ-001 and T-U-001, T-U-002, T-U-029, T-E-001, T-E-002, T-E-003, T-E-020, T-E-021 are implemented in this plan. Out-of-scope exclusions remain absent.
</success_criteria>

<source_audit>
GOAL: Browser Foundation durable workspace sessions.
REQ: REQ-001 covered.
RESEARCH: Partition helper first; empty workspace partition pitfall; Electron persistent partition semantics.
CONTEXT: Implements D-01, D-02, D-06, D-07, D-08, D-09, D-10, D-26, D-28, D-30.
TESTS: T-U-001, T-U-002, T-U-029, T-E-001, T-E-002, T-E-003, T-E-020, T-E-021.
</source_audit>

<output>Create .planning/phases/26-browser-uplift/26-01-SUMMARY.md when done. The summary must state that the two product docs were read first.</output>
