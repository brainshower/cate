---
phase: 26-browser-uplift
plan: 05
type: execute
wave: 5
depends_on:
  - 26-04
files_modified:
  - src/renderer/stores/browserStore.ts
  - src/renderer/stores/browserStore.test.ts
  - src/renderer/panels/BookmarksBar.tsx
  - src/renderer/panels/BookmarksBar.test.tsx
  - src/renderer/panels/BrowserPanel.tsx
  - src/renderer/panels/BrowserPanel.test.tsx
  - e2e/browser-uplift.spec.ts
autonomous: true
requirements:
  - REQ-003
  - REQ-007
must_haves:
  truths:
    - "Renderer browser store selectors/actions are parameterized by workspaceId."
    - "Bookmarks bar and star toggle operate only on the active panel workspace."
    - "Bookmarks persist across restart and do not bleed across workspaces."
  artifacts:
    - path: src/renderer/stores/browserStore.ts
      provides: workspace-scoped renderer browser store
    - path: src/renderer/panels/BookmarksBar.tsx
      provides: bookmarks bar UI
    - path: src/renderer/panels/BrowserPanel.tsx
      provides: star toggle and visit recording wiring
  key_links:
    - from: src/renderer/panels/BrowserPanel.tsx
      to: src/renderer/stores/browserStore.ts
      via: workspaceId selectors/actions
      pattern: workspaceId
    - from: src/renderer/stores/browserStore.ts
      to: src/preload/index.ts
      via: browser history/bookmark preload methods
      pattern: browserBookmarks
---

<objective>
Wire renderer browser state, bookmarks bar, and star toggle.

Purpose: Deliver user-facing REQ-003/REQ-007 affordances on top of the main contracts from Plan 26-04.
Output: Renderer store, BookmarksBar, BrowserPanel star/recording wiring, and tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
MANDATORY FIRST READ, before implementation or scope decisions, per D-01 and D-02:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Test Plan.md

Then read:
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-browser-uplift/26-CONTEXT.md
@.planning/phases/26-browser-uplift/26-RESEARCH.md
@.planning/phases/26-browser-uplift/26-VALIDATION.md
@.planning/phases/26-browser-uplift/26-PATTERNS.md
</context>
<context>
@src/renderer/stores/appStore.ts
@src/renderer/stores/browserStore.ts
@src/renderer/panels/BrowserPanel.tsx
@src/preload/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Workspace-scoped renderer browser store</name>
  <files>src/renderer/stores/browserStore.ts, src/renderer/stores/browserStore.test.ts, src/renderer/panels/BrowserPanel.tsx, src/renderer/panels/BrowserPanel.test.tsx</files>
  <behavior>
    - T-U-008 store refreshes only matching workspace invalidations.
    - T-U-017 star toggle calls add/remove bookmark with current workspaceId.
    - T-U-030 selectors return bookmarks/history for the queried workspaceId and do not rely on a global active workspace.
  </behavior>
  <action>Adapt upstream renderer browserStore to keep state by workspaceId and expose workspace-parameterized selectors/actions for history, bookmarks, current-page bookmark status, record visit, add/remove bookmark, and invalidation handling per D-14/D-15. Wire BrowserPanel navigation/title events to record visits for its own workspaceId, and wire the star control to add/remove bookmarks for that workspace only. Do not add history autocomplete or start-page UI.</action>
  <verify><automated>npx -p node@22 npm test -- src/renderer/stores/browserStore.test.ts src/renderer/panels/BrowserPanel.test.tsx</automated></verify>
  <done>T-U-008, T-U-017, and T-U-030 pass; renderer browser state is explicitly workspace-scoped.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bookmarks bar UI and E2E</name>
  <files>src/renderer/panels/BookmarksBar.tsx, src/renderer/panels/BookmarksBar.test.tsx, src/renderer/panels/BrowserPanel.tsx, e2e/browser-uplift.spec.ts</files>
  <behavior>
    - T-U-018 BookmarksBar renders only supplied workspace bookmarks and navigates on click.
    - T-E-012 user bookmarks a page, restarts, and sees the bookmark in the same workspace.
    - T-E-013 bookmark from workspace A is absent in workspace B.
  </behavior>
  <action>Lift/adapt upstream BookmarksBar UI to Cate toolbar styling and the workspace-scoped renderer store. Show only bookmarks for BrowserPanel workspaceId and navigate the active webview on click. Exclude tabs, start-page, proxy, autocomplete, and global bookmarks promotion per D-06/D-16.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/BookmarksBar.test.tsx src/renderer/stores/browserStore.test.ts</automated>
    <automated>npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-U-018, T-E-012, and T-E-013 pass; bookmarks UI is workspace-scoped.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload/main | Renderer requests privileged browser operations through typed preload APIs. |
| webview guest -> host app | Untrusted web content runs in Electron guest webContents. |
| workspaceId -> browser storage | Workspace identifiers select persistent browser state and Chromium partitions. |
| browser state -> FlashQuery state | Browser state shares workspaceId but must not touch FlashQuery credentials, vaults, indexes, clients, or MCP sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-I | Tampering | renderer browser store | mitigate | Selectors require workspaceId so panels cannot display another workspace bookmarks/history. |
| T-26-05-T | Tampering | bookmark mutation | mitigate | Star toggle sends current panel workspaceId on add/remove. |
| T-26-05-E | Tampering | upstream UI graft | mitigate | Tests and action explicitly exclude tabs/start-page/proxy/autocomplete controls. |
| T-26-05-SC | Tampering | npm installs | accept | No new packages are planned; Package Legitimacy Audit says installs are not applicable. |
</threat_model>

<verification>Run npx -p node@22 npm test -- src/renderer/stores/browserStore.test.ts src/renderer/panels/BookmarksBar.test.tsx src/renderer/panels/BrowserPanel.test.tsx before E2E; then npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts and typecheck.</verification>
<success_criteria>REQ-003 renderer behavior and REQ-007 are covered by T-U-008, T-U-017, T-U-018, T-U-030, T-E-012, and T-E-013 in this plan.</success_criteria>
<source_audit>
GOAL: Browser State and Affordances bookmarks/history UI.
REQ: REQ-003, REQ-007 covered.
RESEARCH: Workspace-parameterized renderer store and no upstream global active workspace.
CONTEXT: Implements D-01, D-02, D-06, D-14, D-15, D-16, D-26, D-28, D-30.
TESTS: T-U-008, T-U-017, T-U-018, T-U-030, T-E-012, T-E-013.
</source_audit>
<output>Create .planning/phases/26-browser-uplift/26-05-SUMMARY.md when done. The summary must state that the two product docs were read first.</output>
