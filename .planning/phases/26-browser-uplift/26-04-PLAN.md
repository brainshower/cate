---
phase: 26-browser-uplift
plan: 04
type: execute
wave: 4
depends_on:
  - 26-01
files_modified:
  - src/main/browserStateStore.ts
  - src/main/browserStateStore.test.ts
  - src/main/ipc/browser.ts
  - src/main/ipc/browser.test.ts
  - src/main/index.ts
  - src/shared/ipc-channels.ts
  - src/preload/index.ts
  - src/shared/electron-api.d.ts
  - src/shared/types.ts
  - e2e/browser-uplift.spec.ts
autonomous: true
requirements:
  - REQ-003
  - REQ-009
must_haves:
  truths:
    - "Main process persists history/bookmarks per explicit workspaceId."
    - "Browser state IPC and preload contracts carry workspaceId and broadcast workspace-scoped invalidations."
    - "Clear-data IPC clears only the target browser partition/state and returns a renderer-usable result."
  artifacts:
    - path: src/main/browserStateStore.ts
      provides: workspace-scoped browser history/bookmark persistence
    - path: src/main/ipc/browser.ts
      provides: browser state and clear-data IPC
    - path: src/shared/electron-api.d.ts
      provides: typed browser IPC/preload contracts
  key_links:
    - from: src/preload/index.ts
      to: src/main/ipc/browser.ts
      via: workspaceId IPC invocations
      pattern: workspaceId
    - from: src/main/ipc/browser.ts
      to: src/main/browserStateStore.ts
      via: state CRUD and clear workspace
      pattern: clearWorkspace
---

<objective>
Create main-process browser state storage and typed browser IPC/preload contracts.

Purpose: Establish REQ-003 persistence and REQ-009 clear-data contracts before renderer UI consumes them.
Output: Workspace-keyed state store, browser IPC module, shared channels/types, preload API declarations, and focused tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
MANDATORY FIRST READ, before implementation or scope decisions, per D-01 and D-02:
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Browser Capture and Control/Browser Uplift - Test Plan.md

Then read:
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-browser-uplift/26-CONTEXT.md
@.planning/phases/26-browser-uplift/26-RESEARCH.md
@.planning/phases/26-browser-uplift/26-VALIDATION.md
@.planning/phases/26-browser-uplift/26-PATTERNS.md
</context>
<context>
@src/main/browserStateStore.ts
@src/main/ipc/flashquery.ts
@src/main/store.ts
@src/shared/ipc-channels.ts
@src/preload/index.ts
@src/shared/electron-api.d.ts
@src/shared/types.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Workspace-keyed browser state store</name>
  <files>src/main/browserStateStore.ts, src/main/browserStateStore.test.ts, src/shared/types.ts</files>
  <behavior>
    - T-U-005 records visits under supplied workspaceId with count/title updates.
    - T-U-006 ignores about:* and other non-recordable URLs.
    - T-U-007 bookmark add/remove operates within the supplied workspace only.
    - T-U-022 clear-data removes only target workspace history/bookmarks.
    - T-U-023 clear-data leaves other workspace browser state intact.
  </behavior>
  <action>Adapt upstream browserStateStore into a main-owned workspace-keyed store per D-14/D-32. Define serializable BrowserHistoryEntry and BrowserBookmark contracts in shared types if needed. Every operation takes explicit workspaceId; no global active workspace. Add helpers for recordVisit, list/remove/clear history, add/remove/list bookmarks, and clearWorkspaceBrowserState. Keep recordability filtering and max history behavior. Do not store FlashQuery tokens, vault references, or MCP state.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/main/browserStateStore.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-U-005, T-U-006, T-U-007, T-U-022, and T-U-023 pass; main browser state is workspace-keyed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Browser IPC/preload contracts</name>
  <files>src/main/ipc/browser.ts, src/main/ipc/browser.test.ts, src/main/index.ts, src/shared/ipc-channels.ts, src/preload/index.ts, src/shared/electron-api.d.ts, e2e/browser-uplift.spec.ts</files>
  <behavior>
    - T-I-002 BROWSER_CLEAR_DATA clears target partition/session mock and returns success/failure result.
    - T-I-003 BROWSER_CLEAR_DATA does not touch mocked FlashQuery credential store or workspace connection.
    - T-E-005 and T-E-006 prove history/bookmarks persist in same workspace and are absent in another workspace.
  </behavior>
  <action>Add browser IPC constants and preload/electron API methods for history/bookmark get/record/add/remove/clear and BROWSER_CLEAR_DATA. All methods take workspaceId. Broadcast BROWSER_HISTORY_CHANGED and BROWSER_BOOKMARKS_CHANGED with workspaceId payload only per D-15. Register browser handlers once from main startup. Implement clear-data using the same partition contract from Plan 26-01 and Electron session APIs, clearing target browser partition plus target workspace history/bookmarks only. Return explicit success/failure details. Add negative mocks proving FlashQuery credential/connection modules are not called.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/main/ipc/browser.test.ts src/main/browserStateStore.test.ts</automated>
    <automated>npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>T-I-002, T-I-003, T-E-005, and T-E-006 pass; browser IPC/preload contracts are workspace-scoped.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> preload/main | Renderer requests privileged browser operations through typed preload APIs. |
| webview guest -> host app | Untrusted web content runs in Electron guest webContents. |
| workspaceId -> browser storage | Workspace identifiers select persistent browser state and Chromium partitions. |
| browser state -> FlashQuery state | Browser state shares workspaceId but must not touch FlashQuery credentials, vaults, indexes, clients, or MCP sessions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-T | Tampering | browser state files | mitigate | All state APIs require workspaceId and tests prove other workspaces remain untouched. |
| T-26-04-I | Tampering | FlashQuery credential boundary | mitigate | IPC tests mock FlashQuery modules and assert clear-data does not call them. |
| T-26-04-R | Tampering | clear-data result | mitigate | Return explicit success/failure result for renderer evidence and user feedback. |
| T-26-04-SC | Tampering | npm installs | accept | No new packages are planned; Package Legitimacy Audit says installs are not applicable. |
</threat_model>

<verification>
Run npx -p node@22 npm test -- src/main/browserStateStore.test.ts src/main/ipc/browser.test.ts first, then npx -p node@22 npm run test:e2e -- e2e/browser-uplift.spec.ts and npx -p node@22 npm run typecheck.
</verification>
<success_criteria>REQ-003 main persistence and REQ-009 IPC foundation are covered by T-U-005/006/007/022/023, T-I-002/003, and T-E-005/006 in this plan.</success_criteria>
<source_audit>
GOAL: Browser State and Affordances foundation plus clear-data contract.
REQ: REQ-003, REQ-009 covered for main/IPC contracts.
RESEARCH: Workspace-parameterized state, main boundary owns privileged cleanup, FlashQuery wipe pitfall addressed.
CONTEXT: Implements D-01, D-02, D-06, D-07, D-14, D-15, D-20, D-21, D-22, D-26, D-28, D-30, D-32.
TESTS: T-U-005, T-U-006, T-U-007, T-U-022, T-U-023, T-I-002, T-I-003, T-E-005, T-E-006.
</source_audit>
<output>Create .planning/phases/26-browser-uplift/26-04-SUMMARY.md when done. The summary must state that the two product docs were read first.</output>
