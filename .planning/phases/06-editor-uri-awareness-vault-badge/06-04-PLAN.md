---
phase: 06-editor-uri-awareness-vault-badge
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/renderer/components/Chip.tsx
  - src/renderer/components/Chip.test.tsx
  - src/renderer/components/VaultBadge.tsx
  - src/renderer/components/VaultBadge.test.tsx
  - src/renderer/docking/DockTabBar.tsx
  - src/renderer/docking/DockTabBar.test.tsx
  - src/renderer/shells/PanelWindowShell.tsx
  - src/renderer/shells/PanelWindowShell.test.tsx
requirements:
  - REQ-032
  - REQ-033
  - REQ-041
  - REQ-042
autonomous: true
must_haves:
  truths:
    - "Vault editor title chrome shows a non-clickable Vault badge with parsed host."
    - "Badge visible copy is exactly `Vault . <host>` when host is available and `Vault` when not."
    - "Badge hover/focus shows the decoded vault-relative path."
    - "Local editor title chrome shows no Vault badge."
    - "No frontmatter, revision, conflict, staleness, version token, expected-version, or if-match UI appears in the badge."
    - "D-01: Downstream agents must read the external Requirements and Test Plan before planning or implementing Phase 6."
    - "D-02: If ambiguity appears, agents re-read the external docs first, then inspect code, before asking the user."
    - "D-03: Every implementation/test task includes both external docs first in read_first, and badge tasks include the UI Spec."
    - "D-04: This plan stays within Phase 6 badge rendering, tooltip, shared chip reuse, title chrome, and focused tests."
    - "D-05: No create-new-vault-document behavior, frontmatter UI, conflict/staleness UI, version persistence, or unrelated vault actions are added."
    - "D-07: Local-file title-bar behavior and local no-badge behavior remain regression requirements."
    - "D-21: Vault editors render a title-bar badge; local-file editors render no badge."
    - "D-22: Badge content is locked to teal Vault icon, text Vault, separator '.', and parsed host, yielding exact copy Vault . <host>."
    - "D-23: Badge surface reuses the Phase 4 Chip primitive or a directly parameterized primitive from the same file."
    - "D-24: The badge is not clickable in v1."
    - "D-25: Badge hover/focus shows decoded vaultPath from parseVaultUri(filePath).vaultPath."
    - "D-26: Badge/title chrome adds no frontmatter parsing or rendering."
    - "D-27: Badge/title chrome adds no version_token, expected_version, or if_match behavior."
    - "D-28: No staleness/conflict UI appears in v1."
    - "D-29: Editor/title chrome tests are part of Phase 6 coverage."
    - "D-32: Tests cover vault badge rendering/content, local no-badge regression, tooltip text, and chip primitive reuse."
    - "D-33: Verification uses focused tests and Node 22/typecheck commands."
  artifacts:
    - path: "src/renderer/components/VaultBadge.tsx"
      provides: "Reusable vault title badge using Chip surface"
    - path: "src/renderer/components/Chip.tsx"
      provides: "Shared chip surface primitive used by connection chip and vault badge"
    - path: "src/renderer/docking/DockTabBar.tsx"
      provides: "Docked and canvas-hosted tab title badge rendering"
    - path: "src/renderer/shells/PanelWindowShell.tsx"
      provides: "Detached panel title badge rendering"
  key_links:
    - from: "src/renderer/docking/DockTabBar.tsx"
      to: "src/renderer/components/VaultBadge.tsx"
      via: "panel.filePath and workspace.flashqueryConnection.url"
      pattern: "VaultBadge"
    - from: "src/renderer/shells/PanelWindowShell.tsx"
      to: "src/renderer/components/VaultBadge.tsx"
      via: "displayPanel.filePath and workspace.flashqueryConnection.url"
      pattern: "VaultBadge"
---

<objective>
Render the vault badge in Cate's actual editor title chrome using the shared chip surface.

Purpose: Users need a quiet source indicator for vault-backed editors without creating a separate editor, status bar, conflict UI, or frontmatter surface.
Output: `ChipSurface` reuse, `VaultBadge`, docked/canvas and detached title chrome rendering, and T-I-094 through T-I-098 coverage.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/Cate/cate/AGENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/ROADMAP.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/REQUIREMENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-RESEARCH.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-UI-SPEC.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/04-vault-panel-shared-chip/04-01-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-03-SUMMARY.md

<interfaces>
From `src/renderer/components/Chip.tsx`:
- Existing `Chip` owns the approved pill surface values: `minHeight: 22`, `borderRadius: 999`, `background: rgba(255,255,255,0.04)`, `border: 1px solid rgba(255,255,255,0.06)`, and 11px system font.

From title chrome:
- `DockTabBar` receives `workspaceId`, `getPanel(panelId)`, and `getPanelTitle(panelId)`; it is used for main dock tabs and canvas-node mini-docks.
- `PanelWindowShell` renders detached panel title text from `displayPanel.title` and has `workspaceId`.

From `src/shared/flashqueryUri.ts`:
- `parseVaultUri(filePath)` returns decoded `vaultPath`; use it for badge trigger and tooltip path.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add vault badge and title chrome tests</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.6, §6.6.7, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-094 through T-I-098 first per D-01/D-03.
    3. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — UI Spec.md` — read §3.6 and §3.7 first for badge/tooltip behavior.
    4. `src/renderer/components/Chip.tsx`
    5. `src/renderer/components/Chip.test.tsx`
    6. `src/renderer/docking/DockTabBar.tsx`
    7. `src/renderer/shells/PanelWindowShell.tsx`
    8. `src/shared/flashqueryUri.ts`
    9. `src/renderer/stores/appStore.ts`
  </read_first>
  <files>src/renderer/components/Chip.test.tsx, src/renderer/components/VaultBadge.test.tsx, src/renderer/docking/DockTabBar.test.tsx, src/renderer/shells/PanelWindowShell.test.tsx</files>
  <behavior>
    - T-I-094: docked/canvas title chrome renders a vault badge for an editor panel whose `filePath` is a vault URI.
    - T-I-095: badge content includes a teal Phosphor `Vault` icon, exact label `Vault`, and host text formatted exactly as `. <host>`; middot/revision wording is design drift and must not be accepted.
    - T-I-096: local-file editor title chrome renders no vault badge.
    - T-I-097: hover or focus shows a tooltip with decoded `vaultPath`.
    - T-I-098: tests prove the badge uses an exported `ChipSurface` or equivalent primitive from `Chip.tsx`, not duplicated inline styles in title chrome.
  </behavior>
  <action>Add `src/renderer/components/VaultBadge.test.tsx` for exact badge content `Vault . fq.local:3100`, host fallback `Vault`, inert click behavior, tooltip delay with fake timers if implemented, decoded path display, no `Vault · <host>`, no `rev 42`, no `version_token`, no `expected_version`, no conflict/staleness copy, and chip surface reuse. Add `src/renderer/docking/DockTabBar.test.tsx` with a minimal stack containing one editor panel and a workspace with `flashqueryConnection.url = 'https://fq.local:3100/mcp'`; assert vault panels render the badge and local editor panels do not. Add focused `src/renderer/shells/PanelWindowShell.test.tsx` coverage, or extend an existing shell test if present, proving detached editor title chrome renders the same `VaultBadge` for vault editor panels and omits it for local editor panels. Extend `Chip.test.tsx` if needed to cover the exported shared surface primitive. Do not add Playwright/E2E coverage; Phase 7 owns that.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/components/Chip.test.tsx src/renderer/components/VaultBadge.test.tsx src/renderer/docking/DockTabBar.test.tsx src/renderer/shells/PanelWindowShell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests include T-I-094 through T-I-098 by ID.
    - Tests fail before implementation for vault badge rendering.
    - Tests assert no revision/conflict/frontmatter UI text appears in badge surfaces.
    - Tests prove local editor no-badge behavior is preserved.
    - Tests prove detached `PanelWindowShell` coverage is automated, not a summary/source-only assertion.
  </acceptance_criteria>
  <done>Badge expectations are encoded before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render vault badge through shared chip chrome</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.6, §6.6.7, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-094 through T-I-098 first per D-01/D-03.
    3. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — UI Spec.md` — read §3.6 and §3.7 first for badge/tooltip behavior.
    4. `src/renderer/components/Chip.tsx`
    5. `src/renderer/components/Chip.test.tsx`
    6. `src/renderer/docking/DockTabBar.tsx`
    7. `src/renderer/shells/PanelWindowShell.tsx`
    8. `src/renderer/shells/PanelWindowShell.test.tsx` if present, otherwise inspect nearby shell tests before creating it.
    9. `src/shared/flashqueryUri.ts`
    10. `src/renderer/stores/appStore.ts`
  </read_first>
  <files>src/renderer/components/Chip.tsx, src/renderer/components/Chip.test.tsx, src/renderer/components/VaultBadge.tsx, src/renderer/components/VaultBadge.test.tsx, src/renderer/docking/DockTabBar.tsx, src/renderer/docking/DockTabBar.test.tsx, src/renderer/shells/PanelWindowShell.tsx, src/renderer/shells/PanelWindowShell.test.tsx</files>
  <action>Refactor `Chip.tsx` to export a shared surface primitive, for example `ChipSurface`, that owns the approved pill style and base classes. Keep the existing `Chip` connection-status API and behavior passing its current tests. Create `src/renderer/components/VaultBadge.tsx` that accepts `{ filePath: string; connectionUrl?: string }`, calls `parseVaultUri(filePath)`, returns `null` for non-vault paths, renders a non-clickable inert `ChipSurface`, uses Phosphor `Vault` at 12px with color `#5AD8B8`, visible label `Vault`, host text from `new URL(connectionUrl).host` when parseable, no misleading host when absent/unparseable, visible host copy exactly `. <host>`, and tooltip text exactly equal to decoded `vaultPath`. Do not permit middot host copy in Phase 6; `Vault · <host>` and `Vault · rev 42` are design drift to reject. Render `VaultBadge` in `DockTabBar` immediately after the title span and before close/right-side actions for editor panels with vault `filePath`, using `workspaceId` to find `WorkspaceInfo.flashqueryConnection.url` from `useAppStore`. Render the same `VaultBadge` in `PanelWindowShell` after detached editor title text and before close, and add automated `PanelWindowShell` coverage proving the detached shell renders `VaultBadge` for vault editor panels and omits it for local editor panels. Do not add click behavior, status state, revision text, frontmatter text, conflict/staleness UI, or status-bar UI. Mark the badge area `WebkitAppRegion: 'no-drag'` only if tooltip hover requires it in detached title bars; do not interfere with tab dragging.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/components/Chip.test.tsx src/renderer/components/VaultBadge.test.tsx src/renderer/docking/DockTabBar.test.tsx src/renderer/shells/PanelWindowShell.test.tsx</automated>
    <automated>npx -p node@22 npm test -- src/main/flashquery/clientManager.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - T-I-094 through T-I-098 pass.
    - `Chip` connection-status tests still pass after extracting the shared surface.
    - `DockTabBar` renders the badge for vault editor panels and not for local editor panels; because `DockTabBar` is used by canvas-node mini-docks, canvas-hosted tab chrome is covered by the same component.
    - `PanelWindowShell.test.tsx` or an equivalent existing shell test proves `PanelWindowShell.tsx` imports/renders `VaultBadge` for detached vault editor windows and omits it for detached local editor windows.
    - `grep -R "rev 42\\|version_token\\|expected_version\\|if_match\\|conflict\\|stale" src/renderer/components/VaultBadge.tsx src/renderer/docking/DockTabBar.tsx src/renderer/shells/PanelWindowShell.tsx` returns no matches except test strings that assert absence.
    - `src/main/flashquery/clientManager.test.ts` still passes, preserving body-only/no-conflict IPC contracts for REQ-041 and REQ-042.
  </acceptance_criteria>
  <done>The editor title chrome displays a reusable, inert vault badge with tooltip while preserving local editor chrome.</done>
</task>
</tasks>

<must_haves>
- REQ-032: vault editor title chrome renders a chip-style `Vault` badge with parsed host and local editors render no badge.
- REQ-033: badge hover/focus displays decoded vault-relative path.
- REQ-041: no frontmatter parsing/rendering surface is added.
- REQ-042: no version, conflict, staleness, expected-version, or if-match UI/data is added.
</must_haves>

<decision_coverage>
- D-01, D-02, D-03: Every task lists the external Requirements and Test Plan as the first two `<read_first>` entries, and includes the UI Spec for badge work.
- D-04: This plan is limited to Phase 6 badge rendering, tooltip, shared chip reuse, title chrome, and focused tests.
- D-05: The plan forbids create-new-vault-document behavior, frontmatter UI, conflict/staleness UI, version-token persistence, and unrelated vault actions.
- D-07: Local-file title-bar behavior and local no-badge behavior are explicit regression requirements.
- D-21: Vault editors render a title-bar badge and local-file editors render no badge.
- D-22: Badge content is locked to teal Phosphor `Vault`, text `Vault`, separator `.`, and parsed host, with exact visible copy `Vault . <host>`.
- D-23: The badge reuses the Phase 4 `Chip` primitive or a directly parameterized primitive from the same file.
- D-24: The badge is not clickable in v1.
- D-25: Badge hover/focus shows the decoded `vaultPath` from `parseVaultUri(filePath).vaultPath`.
- D-26, D-27, D-28: The badge/title chrome adds no frontmatter, version-token, expected-version, if-match, conflict, or staleness UI.
- D-29, D-32, D-33: Tests cover vault badge rendering/content, local no-badge regression, tooltip text, chip primitive reuse, and Node 22 verification.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer title chrome | Vault URI and connection host are rendered in UI; unsafe raw URI/metadata must not leak. |
| Shared component boundary | Chip surface reuse must not make the vault badge clickable or retry-capable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | `VaultBadge` tooltip | mitigate | Tooltip shows only decoded vault-relative path; badge host comes from configured workspace URL host only, not raw tokens or full connection URL. |
| T-06-04-02 | Spoofing | Badge host fallback | mitigate | If connection URL is missing/unparseable, render `Vault` without misleading host. |
| T-06-04-03 | Elevation of Privilege | Badge click surface | mitigate | Badge is inert and does not retry, save, reveal, open, or call IPC. |
| T-06-04-04 | Information Disclosure | Conflict/frontmatter UI | mitigate | Tests/source grep block revision, version, conflict, staleness, frontmatter, expected-version, and if-match UI additions. |
| T-06-04-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx -p node@22 npm test -- src/renderer/components/Chip.test.tsx src/renderer/components/VaultBadge.test.tsx src/renderer/docking/DockTabBar.test.tsx src/renderer/shells/PanelWindowShell.test.tsx`
- `npx -p node@22 npm test -- src/main/flashquery/clientManager.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
T-I-094 through T-I-098 pass, chip regressions pass, Phase 3 body-only/no-conflict tests pass, and local editor title behavior remains covered.
</success_criteria>

<output>
Create `.planning/phases/06-editor-uri-awareness-vault-badge/06-04-SUMMARY.md` when done.
</output>
