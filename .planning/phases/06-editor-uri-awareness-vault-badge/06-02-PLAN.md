---
phase: 06-editor-uri-awareness-vault-badge
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/lib/confirmCloseDirty.ts
requirements:
  - REQ-029
  - REQ-031
  - REQ-041
  - REQ-042
autonomous: true
must_haves:
  truths:
    - "Saving a vault editor writes the Monaco model value through FlashQuery IPC."
    - "Successful vault saves clear dirty state like local saves."
    - "Failed vault saves leave dirty state intact and show the user an error."
    - "Dirty vault editor close confirmation uses Cate's existing unsaved-changes flow."
    - "D-01: Downstream agents must read the external Requirements and Test Plan before planning or implementing Phase 6."
    - "D-02: If ambiguity appears, agents re-read the external docs first, then inspect code, before asking the user."
    - "D-03: Every implementation/test task includes both external docs first in read_first."
    - "D-04: This plan stays within Phase 6 editor save and dirty-state behavior."
    - "D-05: No vault creation, frontmatter UI, conflict/staleness UI, version persistence, or vault-management behavior is added."
    - "D-06: Renderer vault saves use Phase 3 preload IPC and do not instantiate MCP clients or call privileged APIs directly."
    - "D-07: Local saves, dirty state, close-confirm behavior, Monaco model lifecycle, and title behavior remain the regression baseline."
    - "D-13: Save routing is scheme-based: vault calls flashqueryWriteDocument; local calls fsWriteFile."
    - "D-16: Saving while disconnected still attempts the FlashQuery IPC call."
    - "D-17: Vault editors do not persist unsaved body content into PanelState.unsavedContent or temp files."
    - "D-18: Dirty vault editor close uses the existing DIALOG_CONFIRM_UNSAVED flow."
    - "D-26: Editor write behavior does not expose or parse frontmatter."
    - "D-27: Editor does not retain/send version_token, frontmatter, title, tags, expected_version, or if_match."
    - "D-28: No staleness/conflict UI appears in v1."
    - "D-29: EditorPanel.test.tsx is the test home for Phase 6 editor coverage."
    - "D-30: Tests cover vault/local save routing, dirty clearing/preservation, visible errors, and disconnected save attempts."
    - "D-32: Tests cover dirty marker parity, unsavedContent non-use, and close-confirm reuse."
    - "D-33: Verification uses focused EditorPanel tests and Node 22/typecheck commands."
  artifacts:
    - path: "src/renderer/panels/EditorPanel.tsx"
      provides: "URI-scheme-aware save and dirty-state behavior"
    - path: "src/renderer/panels/EditorPanel.test.tsx"
      provides: "T-I-083..087 and T-I-091..093 renderer coverage"
    - path: "src/renderer/lib/confirmCloseDirty.ts"
      provides: "Existing close-confirm behavior consumed by tests; modify only if needed for vault-path display safety"
  key_links:
    - from: "src/renderer/panels/EditorPanel.tsx"
      to: "window.electronAPI.flashqueryWriteDocument"
      via: "save callback vault branch"
      pattern: "flashqueryWriteDocument"
    - from: "src/renderer/lib/confirmCloseDirty.ts"
      to: "window.electronAPI.confirmUnsavedChanges"
      via: "dirty editor close prompt"
      pattern: "confirmUnsavedChanges"
---

<objective>
Route vault saves through FlashQuery and preserve Cate's local dirty-state conventions.

Purpose: Vault documents must be editable, saveable, and close-confirmable without introducing autosave/session persistence or conflict semantics.
Output: `EditorPanel` saves by URI scheme and has focused tests for T-I-083 through T-I-087 and T-I-091 through T-I-093.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/Cate/cate/AGENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/ROADMAP.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/REQUIREMENTS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-CONTEXT.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-RESEARCH.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/03-ipc-surface/03-03-SUMMARY.md
@/Users/matt/Documents/Claude/Projects/Cate/cate/.planning/phases/06-editor-uri-awareness-vault-badge/06-01-SUMMARY.md

<interfaces>
From `src/shared/electron-api.d.ts`:
- `flashqueryWriteDocument(workspaceId: string, vaultPath: string, content: string): Promise<FlashQueryWriteResult>`
- `fsWriteFile(filePath: string, content: string): Promise<void>`
- `confirmUnsavedChanges(options): Promise<'save' | 'discard' | 'cancel'>`

From `src/shared/types.ts`:
- `FlashQueryWriteResult` is `{ success: true; modified: string } | { success: false; error: string }`.
- `PanelState.unsavedContent` is scratch-editor-only and must not be populated for vault editors.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add save, dirty, and close-confirm tests</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.3, §6.6.5, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-083 through T-I-087 plus T-I-091 through T-I-093 first per D-01/D-03.
    3. `src/renderer/panels/EditorPanel.test.tsx`
    4. `src/renderer/panels/EditorPanel.tsx`
    5. `src/renderer/lib/confirmCloseDirty.ts`
    6. `src/renderer/lib/editorSaveRegistry.ts`
    7. `src/renderer/stores/appStore.ts`
  </read_first>
  <files>src/renderer/panels/EditorPanel.test.tsx</files>
  <behavior>
    - T-I-083: saving a dirty vault editor calls `flashqueryWriteDocument(workspaceId, vaultPath, content)` with exactly three preload arguments; no `frontmatter`, `title`, `tags`, `version_token`, `expected_version`, or `if_match` argument/object is passed.
    - T-I-084: saving a dirty local editor still calls `fsWriteFile(localPath, content)`.
    - T-I-085: `{ success: true }` from vault save clears `isDirty`, removes the title bullet, and makes `setPanelDirty(workspaceId, panelId, false)` observable.
    - T-I-086: `{ success: false, error }` or rejected vault save leaves `isDirty` true and renders a user-visible failure message in the editor surface.
    - T-I-087: saving while mocked FlashQuery status is disconnected still attempts `flashqueryWriteDocument`; do not add renderer-side blocking.
    - T-I-091: changing vault model content marks the panel dirty and appends `•` to the title using a decoded vault basename.
    - T-I-092: changing vault model content never sets `PanelState.unsavedContent`.
    - T-I-093: `confirmCloseDirtyPanels([dirtyVaultPanel])` calls the existing `confirmUnsavedChanges` IPC and routes `save` through the registered editor save handler rather than a custom dialog.
  </behavior>
  <action>Extend `EditorPanel.test.tsx` from Plan 06-01. Add helpers to mutate the mocked Monaco model value and fire the editor `onDidChangeModelContent` callback. Add a save trigger by focusing/marking the panel active and dispatching the existing `save-file` event, or call the registered save path through `confirmCloseDirtyPanels` where that is the behavior under test. Mock `flashqueryWriteDocument` returning both success and failure shapes. Add an assertion that the successful vault call receives only `(workspaceId, vaultPath, content)` and not a fourth options object. For T-I-093, import `confirmCloseDirtyPanels` and verify `window.electronAPI.confirmUnsavedChanges` receives the existing dirty panel metadata for a vault editor.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `EditorPanel.test.tsx` includes explicit coverage for T-I-083 through T-I-087 and T-I-091 through T-I-093.
    - Tests prove local save remains on `fsWriteFile`.
    - Tests prove vault failure is user-visible and dirty state remains true.
    - Tests prove no unsaved vault body is written to `PanelState.unsavedContent`.
  </acceptance_criteria>
  <done>Save/dirty/close-confirm expectations are encoded before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement vault save and dirty-state parity</name>
  <read_first>
    1. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md` — read Spec §6.6.3, §6.6.5, §6.8.2, and §6.8.3 first per D-01/D-03.
    2. `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md` — read §4.6 and T-I-083 through T-I-087 plus T-I-091 through T-I-093 first per D-01/D-03.
    3. `src/renderer/panels/EditorPanel.tsx`
    4. `src/renderer/panels/EditorPanel.test.tsx`
    5. `src/shared/flashqueryUri.ts`
    6. `src/renderer/lib/confirmCloseDirty.ts`
  </read_first>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx, src/renderer/lib/confirmCloseDirty.ts</files>
  <action>In `EditorPanel.tsx`, update the `save` callback so the early `if (!editor || diffMode) return false` does not block vault editors once Plan 06-03 normalizes vault diff mode; use an effective diff check that returns false only for local diff mode. After `targetPath` is known, compute `const vaultUri = parseVaultUri(targetPath)`. For vault URIs, call `window.electronAPI.flashqueryWriteDocument(vaultUri.workspaceId, vaultUri.vaultPath, content)`. If the result is `{ success: false }`, set an editor-visible save error state such as `saveError`, render it in `EditorPanel` using existing semantic classes, log the failure, return `false`, and do not clear dirty state. If the call rejects, handle it the same way. If the result is `{ success: true }`, clear `saveError`, set `isDirtyRef.current = false`, call `setPanelDirty(workspaceId, panelId, false)`, and update the panel title to the decoded vault basename without the dirty bullet. For local paths, preserve current Save-As, `fsWriteFile`, filePath persistence, `editor:panel-saved-as`, and title behavior unchanged. Keep scratch `unsavedContent` writes guarded by `!filePathRef.current`; do not add vault temp-file/session persistence. Modify `confirmCloseDirty.ts` only if tests reveal it displays unsafe encoded vault names; if changed, use `parseVaultUri` only for display and keep the existing `confirmUnsavedChanges` IPC flow.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - T-I-083 through T-I-087 and T-I-091 through T-I-093 pass.
    - `flashqueryWriteDocument` is invoked with only `workspaceId`, decoded `vaultPath`, and current model content.
    - No renderer code sends or stores `frontmatter`, `title`, `tags`, `version_token`, `expected_version`, or `if_match`.
    - Successful vault saves clear dirty state and failed vault saves leave dirty state intact with visible error copy.
    - `PanelState.unsavedContent` remains scratch-only for vault editors.
  </acceptance_criteria>
  <done>Vault save behavior matches local editor conventions while preserving body-only and last-write-wins guardrails.</done>
</task>
</tasks>

<must_haves>
- REQ-029: vault saves call `flashqueryWriteDocument`; local saves call `fsWriteFile`; failures are visible and dirty-preserving.
- REQ-031: dirty state, title bullet, close-confirm, and memory-only unsaved buffer behavior match local file-backed editors.
- REQ-041: editor write behavior passes content only and does not expose or edit frontmatter.
- REQ-042: editor does not retain/send `version_token`, `expected_version`, `if_match`, or conflict/staleness UI.
</must_haves>

<decision_coverage>
- D-01, D-02, D-03: Every task lists the external Requirements and Test Plan as the first two `<read_first>` entries.
- D-04: This plan is limited to Phase 6 editor save and dirty-state behavior.
- D-05: The plan forbids vault creation, frontmatter UI, conflict/staleness UI, version-token persistence, and vault-management actions.
- D-06: Vault saves use the existing Phase 3 preload API; renderer code does not instantiate MCP clients or call privileged APIs directly.
- D-07: Local saves, dirty-state behavior, close-confirm behavior, Monaco model lifecycle, and title behavior are regression requirements.
- D-13: The save path routes by URI scheme: vault saves call `flashqueryWriteDocument(workspaceId, vaultPath, model.getValue())`; local saves call `fsWriteFile`.
- D-16: Saving while disconnected still attempts the IPC call.
- D-17: Vault editors do not persist unsaved body content into `PanelState.unsavedContent` or temp files.
- D-18: Dirty vault editor close uses the existing `DIALOG_CONFIRM_UNSAVED` flow.
- D-26, D-27, D-28: The editor uses body/content only, does not retain/send `version_token`, `expected_version`, or `if_match`, and adds no conflict/staleness UI.
- D-29, D-30, D-32, D-33: `EditorPanel.test.tsx` covers save, dirty, unsaved-content, close-confirm, and Node 22 verification.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to preload FlashQuery write API | Untrusted editor content crosses to main-process FlashQuery write handling. |
| Renderer session state | Unsaved vault content must remain in Monaco memory, not persisted into workspace/session state. |
| Renderer to native close-confirm IPC | Dirty vault panels reuse native unsaved-change confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `EditorPanel.save` | mitigate | Route by `parseVaultUri`; vault saves use `flashqueryWriteDocument` only and local saves use `fsWriteFile` only. |
| T-06-02-02 | Information Disclosure | `PanelState.unsavedContent` | mitigate | Preserve scratch-only persistence guard; tests assert vault body is not stored in session state. |
| T-06-02-03 | Repudiation | Save failure UI | mitigate | Render user-visible failure text and keep dirty state on failed vault writes. |
| T-06-02-04 | Information Disclosure | Conflict/version metadata | mitigate | Tests assert no frontmatter/title/tags/version/conflict fields are passed from renderer. |
| T-06-02-SC | Tampering | npm installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run:
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
T-I-083 through T-I-087 and T-I-091 through T-I-093 pass; vault saves work through FlashQuery while local saves and close-confirm behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-editor-uri-awareness-vault-badge/06-02-SUMMARY.md` when done.
</output>
