---
phase: 22-outline-foundation-and-source-navigation
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-03
files_modified:
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/EditorPanel.test.tsx
  - src/renderer/panels/OutlinePanel.tsx
  - src/renderer/stores/appStore.ts
  - src/renderer/stores/appStore.test.ts
autonomous: true
requirements:
  - REQ-002
  - REQ-003
  - REQ-004
  - REQ-020
  - REQ-021
  - REQ-022
coverage:
  - T-I-017
  - T-I-018
  - T-I-019
  - T-I-020
  - T-I-021
  - T-I-022
  - T-A-001
  - T-A-002
  - T-A-003
  - T-A-004
  - T-A-006
  - T-A-007
  - T-M-001
  - T-M-002
  - T-M-003
  - T-M-004
must_haves:
  truths:
    - "Editor toolbar shows Outline toggle next to Preview for regular editors only."
    - "Opening Outline uses the existing right dock zone."
    - "Closing Outline closes only the associated Outline panel."
    - "Preview/save/dirty/model-cache/dispose behavior is preserved."
---

<objective>
Wire the editor toolbar toggle and right-dock open/close workflow, then harden non-interference and manual acceptance coverage for the Phase 22 source-mode Outline.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-RESEARCH.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md
</context>

<tasks>
<task id="22.4.1" type="execute" tdd="true">
  <name>Add the EditorPanel Outline toolbar toggle</name>
  <files>src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <read_first>
    - Product requirements REQ-002, REQ-003, REQ-020, and REQ-022.
    - Product test plan T-I-017 through T-I-022.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md`.
    - `src/renderer/panels/EditorPanel.tsx` around the Preview button, `markdownPreview` state, diff mode rendering, model lifecycle, and cleanup.
    - `src/renderer/stores/appStore.ts` for `createOutline`, `closePanel`, and panel records.
    - `src/renderer/stores/dockStore.ts` for right-zone behavior.
    - `src/renderer/panels/EditorPanel.test.tsx`.
  </read_first>
  <action>Add an Outline toggle button adjacent to the existing Preview button in `EditorPanel.tsx`. The toggle must be hidden in diff mode, use the same muted/off and blue-highlight/on treatment as Preview, and expose title/accessible label text identifying the action as toggling the document outline. When no associated Outline panel is open, clicking calls `createOutline(workspaceId, undefined, { target: 'dock', zone: 'right' })` or an equivalent exact right-dock placement path. When the associated Outline is open, clicking closes only that Outline panel. Preserve Markdown preview toggle text, behavior, and content sync.</action>
  <acceptance_criteria>
    - T-I-017 proves regular editors render the Outline toggle next to Preview.
    - T-I-018 proves diff mode hides the Outline toggle.
    - T-I-019 proves clicking the toggle opens Outline with `{ target: 'dock', zone: 'right' }`.
    - T-I-020 proves clicking the active toggle closes only the associated Outline panel.
    - T-I-021 proves muted/off and blue/on visual state.
    - T-I-022 proves opening Outline does not change Markdown preview state, dirty state, or editor model content.
    - Existing EditorPanel tests for Preview/Source still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>The editor toolbar controls the associated right-dock Outline without regressing Preview.</done>
</task>

<task id="22.4.2" type="execute" tdd="true">
  <name>Harden associated panel close and dock behavior</name>
  <files>src/renderer/stores/appStore.ts, src/renderer/stores/appStore.test.ts, src/renderer/panels/EditorPanel.tsx, src/renderer/panels/EditorPanel.test.tsx</files>
  <read_first>
    - Product requirements REQ-003, REQ-004, REQ-020, and REQ-022.
    - Product test plan T-I-019, T-I-020, T-I-022, and T-A-007.
    - `src/renderer/stores/appStore.ts`.
    - `src/renderer/stores/dockStore.ts`.
    - `src/renderer/panels/EditorPanel.tsx`.
  </read_first>
  <action>If needed, add a source editor association to Outline panel state using an existing `PanelState` extension or a narrow metadata field that does not affect unrelated panels. Use that association so the toolbar closes the Outline panel for the current workspace/editor context only. Add tests where two right-zone panels exist, one Outline and one unrelated panel, and closing Outline leaves the unrelated panel in the dock. Add a test where a second editor does not incorrectly toggle the first editor's associated Outline unless the implementation intentionally supports one workspace-global Outline and documents that behavior in tests.</action>
  <acceptance_criteria>
    - Closing Outline removes only the matching Outline panel id from app-store panels/dock locations.
    - Unrelated right-zone panels remain open and registered after Outline closes.
    - Multiple editor contexts do not produce ambiguous close behavior.
    - No Document Chat or Graph Explorer selection files are created or modified.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/stores/appStore.test.ts src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>rg -n "preview-section-select|Document Chat|documentChat|Graph Explorer|graph explorer" src || true</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline open/close behavior is precise and non-destructive to existing dock panels.</done>
</task>

<task id="22.4.3" type="execute">
  <name>Run focused Phase 22 verification and record manual acceptance checklist</name>
  <files>.planning/phases/22-outline-foundation-and-source-navigation/22-04-SUMMARY.md</files>
  <read_first>
    - Product test plan sections 3.12 and 3.13.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-VALIDATION.md`.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-01-SUMMARY.md` if present.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-02-SUMMARY.md` if present.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-03-SUMMARY.md` if present.
  </read_first>
  <action>Run the focused Phase 22 automated commands under Node 22. Then document manual acceptance status for T-A-001, T-A-002, T-A-003, T-A-004, T-A-006, T-A-007, and T-M-001 through T-M-004 in the plan summary. If Electron manual checks cannot be run in the current environment, mark them as pending manual owner UAT with exact commands/checks rather than claiming pass.</action>
  <acceptance_criteria>
    - `npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts` exits 0.
    - `npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts` exits 0.
    - `npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx` exits 0.
    - `npx -p node@22 npm run typecheck` exits 0.
    - Summary records the status of every Phase 22 acceptance/manual check listed in `22-VALIDATION.md`.
    - Summary explicitly states that REQ-017, REQ-018, and REQ-019 remain deferred to Phase 23.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts</automated>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Phase 22 source-mode Outline is verified and acceptance/manual follow-up is explicit.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-22-10-D | Denial of Service | toolbar/dock close | mitigate | Test closing Outline leaves unrelated dock panels open and does not hide the right dock zone unnecessarily. |
| T-22-11-T | Tampering | editor state | mitigate | Assert Outline toggle does not change `markdownPreview`, dirty state, or model content. |
| T-22-12-I | Information Disclosure | excluded features | mitigate | Grep/test that Document Chat and Graph Explorer selection behavior are not introduced. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts`
- `npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts`
- `npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 22.4 is complete when the editor toolbar opens/closes the right-dock Outline correctly, non-interference tests pass, and Phase 22 acceptance/manual evidence is recorded.
</success_criteria>

<output>
Create `.planning/phases/22-outline-foundation-and-source-navigation/22-04-SUMMARY.md` when done.
</output>
