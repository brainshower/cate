---
phase: 22-outline-foundation-and-source-navigation
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - src/renderer/lib/activeEditorRegistry.ts
  - src/renderer/lib/activeEditorRegistry.test.ts
  - src/renderer/panels/EditorPanel.tsx
  - src/renderer/panels/OutlinePanel.tsx
  - src/renderer/panels/OutlinePanel.test.tsx
autonomous: true
requirements:
  - REQ-004
  - REQ-009
  - REQ-010
  - REQ-011
  - REQ-012
  - REQ-013
  - REQ-014
  - REQ-015
  - REQ-016
  - REQ-020
  - REQ-021
coverage:
  - T-I-001
  - T-I-002
  - T-I-003
  - T-I-004
  - T-I-005
  - T-I-006
  - T-I-007
  - T-I-008
  - T-I-009
  - T-I-010
  - T-I-011
  - T-I-012
  - T-I-013
  - T-I-014
  - T-I-015
  - T-I-016
  - T-I-031
  - T-I-032
  - T-I-033
  - T-I-034
  - T-I-035
must_haves:
  truths:
    - "Outline binds to active Monaco editor/model and degrades safely when none exists."
    - "Source-mode row clicks and Enter-cycle navigation target Monaco, not Markdown preview."
    - "Outline state is isolated per panel instance."
  artifacts:
    - path: "src/renderer/panels/OutlinePanel.tsx"
      provides: "Depth, rendering, cursor tracking, source navigation, live update, search, clear, Enter-cycle, cleanup."
    - path: "src/renderer/lib/activeEditorRegistry.ts"
      provides: "Workspace-aware active editor/model bridge."
---

<objective>
Implement the source-mode Outline panel experience: active editor binding, heading rendering, depth filtering, cursor tracking, source navigation, live updates, search, clear, Enter-cycle, state isolation, theme-compatible styling, and cleanup.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-RESEARCH.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md
</context>

<tasks>
<task id="22.3.1" type="execute" tdd="true">
  <name>Create active editor registry and EditorPanel registration hooks</name>
  <files>src/renderer/lib/activeEditorRegistry.ts, src/renderer/lib/activeEditorRegistry.test.ts, src/renderer/panels/EditorPanel.tsx</files>
  <read_first>
    - Product requirements REQ-004, REQ-013, REQ-020, REQ-022, and Requirements §4.4.
    - Product test plan T-I-006 through T-I-010 and T-I-031 through T-I-035.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md` decisions D-10 through D-12 and D-27.
    - `src/renderer/panels/EditorPanel.tsx` around Monaco create, focus, content subscriptions, model cache, markdown preview state, and cleanup.
    - `src/renderer/panels/EditorPanel.test.tsx` Monaco mock.
    - `src/shared/types.ts` for `PanelState.markdownPreview`.
  </read_first>
  <action>Add a small workspace-aware active editor registry or equivalent adapter in `src/renderer/lib/activeEditorRegistry.ts`. It must track active editor panel id, editor instance, current model, and subscription notifications for active editor/model changes. Wire `EditorPanel.tsx` so regular editors register on mount/focus/model setup and unregister on cleanup before disposed refs can be used. The registry must expose subscribe/getSnapshot-style APIs or another testable interface that OutlinePanel can consume. Keep preview state lookup in OutlinePanel through app-store panel state or include a callback in the active-editor snapshot; do not duplicate `markdownPreview` state.</action>
  <acceptance_criteria>
    - Active editor registry stores entries by workspace id and editor panel id.
    - Focusing a different editor updates the active snapshot.
    - Unregistering the active editor clears or reselects safely without stale disposed refs.
    - Model changes notify subscribers.
    - Registry tests include T-I-006, T-I-007, T-I-010, T-I-033, T-I-034, and T-I-035 traceability where applicable.
    - Existing Preview/Source behavior in `EditorPanel.tsx` remains unchanged by tests.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/activeEditorRegistry.test.ts src/renderer/panels/EditorPanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline can discover and subscribe to the active editor without DOM probing or stale refs.</done>
</task>

<task id="22.3.2" type="execute" tdd="true">
  <name>Render depth-filtered Outline rows and state isolation</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx</files>
  <read_first>
    - Product requirements REQ-009, REQ-010, REQ-020, and REQ-021.
    - Product test plan T-I-001 through T-I-005 and T-M-001 through T-M-004.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md`.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/lib/activeEditorRegistry.ts`.
    - `src/renderer/panels/FlashQueryVaultSearchPanel.tsx` for panel styling and test conventions.
  </read_first>
  <action>Implement OutlinePanel local state with `headings`, `maxDepth`, `searchQuery`, `searchMatchIdx`, and active heading behavior. Render a depth dropdown with options `H1-H2`, `H1-H3`, `H1-H4`, `H1-H5`, `H1-H6`; default `maxDepth` is `3`. Render heading rows from parsed state with indentation values 14, 28, 42, 56, 70, and 84 px. Use stronger font weight for L1 than L2 and smaller text for L4-L6. Use Cate theme classes/variables for surface, text, borders, accent, hover, and muted states. Add an empty state when no active editor/model exists or no headings are present.</action>
  <acceptance_criteria>
    - T-I-001 proves the depth dropdown options and default H1-H3 state.
    - T-I-002 proves row indentation and level-based text styling.
    - T-I-003 proves no-heading/no-editor empty state.
    - T-I-004 proves depth changes update visible headings without remounting the panel.
    - T-I-005 proves two Outline panel instances keep independent search/depth/cycle state.
    - Rendered rows use a 2px left border with transparent default and accent active/focus states.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline renders heading state correctly and isolates state per panel instance.</done>
</task>

<task id="22.3.3" type="execute" tdd="true">
  <name>Add cursor tracking, source jump, and live updates</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx, src/renderer/lib/activeEditorRegistry.ts</files>
  <read_first>
    - Product requirements REQ-004, REQ-011, REQ-012, and REQ-013.
    - Product test plan T-I-006 through T-I-011 and T-I-031 through T-I-035.
    - `src/renderer/panels/OutlinePanel.tsx`.
    - `src/renderer/lib/activeEditorRegistry.ts`.
    - `src/renderer/panels/EditorPanel.test.tsx` Monaco mock patterns.
  </read_first>
  <action>Subscribe OutlinePanel to active editor changes, `editor.onDidChangeCursorPosition()`, `editor.onDidChangeModelContent()`, model/language changes where available, and active model switches. Debounce content-change reparsing by 300ms and clear timers on unmount/rebind. Compute active heading as the nearest parsed heading at or before the cursor line. Row clicks in source mode must guard missing/disposed editor/model, then call `editor.revealLineInCenter(line)`, `editor.setPosition({ lineNumber: line, column: 1 })`, and `editor.focus()`.</action>
  <acceptance_criteria>
    - T-I-006 binds to the active editor and parses its current model.
    - T-I-007 rebinding to a different active editor replaces headings/subscriptions.
    - T-I-008 cursor movement highlights the nearest heading at or before cursor line.
    - T-I-009 recalculates active heading after content, model, and depth changes.
    - T-I-010 no active editor/model does not throw.
    - T-I-011 clicking a source-mode heading calls `revealLineInCenter`, `setPosition({ lineNumber, column: 1 })`, and `focus`.
    - T-I-031 proves content changes debounce parser updates by 300ms.
    - T-I-032 proves language changes trigger reparse where model APIs expose the event.
    - T-I-034 proves unmount disposes subscriptions and clears timers.
    - T-I-035 proves editor teardown with Outline mounted does not access disposed refs.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Outline stays synchronized with the active editor and source navigation is safe.</done>
</task>

<task id="22.3.4" type="execute" tdd="true">
  <name>Add search, clear, highlighting, and Enter-cycle source navigation</name>
  <files>src/renderer/panels/OutlinePanel.tsx, src/renderer/panels/OutlinePanel.test.tsx</files>
  <read_first>
    - Product requirements REQ-014, REQ-015, and REQ-016.
    - Product test plan T-I-012 through T-I-016.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md`.
    - `src/renderer/panels/OutlinePanel.tsx`.
  </read_first>
  <action>Add pinned search/filter input below the panel header. Ignore whitespace-only queries. Match case-insensitively while keeping non-matching headings visible. Apply warm yellow row background to matched rows, wrap exact matched substrings in darker yellow highlight spans, show a clear button only when text is present, reset `searchMatchIdx` to `-1` on clear/query changes, and refocus the input on clear. Pressing Enter cycles matches from first to last and wraps; focused match uses blue background, white text, and accent left border. In Phase 22, Enter-cycle navigation uses source-mode Monaco navigation from task 22.3.3 only.</action>
  <acceptance_criteria>
    - T-I-012 proves search is case-insensitive and ignores whitespace-only input.
    - T-I-013 proves non-matching headings remain visible while match rows/substrings are highlighted.
    - T-I-014 proves clear button appears only when text exists, clears search, resets cycle index, and refocuses input.
    - T-I-015 proves Enter cycles through matches, wraps, applies search-focus styling, and navigates source lines.
    - T-I-016 proves cycle index resets when query changes.
    - Search-focus styling remains distinct from active cursor and yellow match styling.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/OutlinePanel.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Search and source-mode Enter cycling satisfy REQ-014 through REQ-016.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-22-07-I | Information Disclosure | active editor registry | mitigate | Keep registry in renderer memory only, workspace-scoped, and clear refs on unregister/workspace switch; do not store document text beyond component state derived from current model. |
| T-22-08-D | Denial of Service | live parsing | mitigate | Debounce content changes by 300ms and clear timers on unmount/rebind. |
| T-22-09-T | Tampering | Monaco navigation | mitigate | Guard disposed/missing refs and use exact line/column navigation APIs from the requirement. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/lib/activeEditorRegistry.test.ts src/renderer/lib/parseDocumentHeadings.test.ts src/renderer/panels/OutlinePanel.test.tsx`
- `npx -p node@22 npm test -- src/renderer/panels/EditorPanel.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 22.3 is complete when OutlinePanel delivers source-mode behavior for REQ-004, REQ-009 through REQ-016, REQ-020, and REQ-021 without preview routing.
</success_criteria>

<output>
Create `.planning/phases/22-outline-foundation-and-source-navigation/22-03-SUMMARY.md` when done.
</output>
