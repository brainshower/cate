---
phase: 22-outline-foundation-and-source-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/lib/parseDocumentHeadings.ts
  - src/renderer/lib/parseDocumentHeadings.test.ts
autonomous: true
requirements:
  - REQ-005
  - REQ-006
  - REQ-007
  - REQ-008
  - REQ-009
coverage:
  - T-U-007
  - T-U-008
  - T-U-009
  - T-U-010
  - T-U-011
  - T-U-012
  - T-U-013
  - T-U-014
must_haves:
  truths:
    - "Parser logic is pure and testable without React, DOM, Zustand, or Electron."
    - "Parser returns one-based line numbers."
    - "Parser supports Markdown, HTML, and code section markers with max-depth filtering."
  artifacts:
    - path: "src/renderer/lib/parseDocumentHeadings.ts"
      provides: "Pure heading parser and markdown inline text stripping."
      contains: "parseDocumentHeadings"
---

<objective>
Create the pure heading parser utility that powers the Outline panel and prove Markdown, HTML, code marker, inline stripping, and depth behavior with node Vitest tests.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-RESEARCH.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md
</context>

<tasks>
<task id="22.2.1" type="execute" tdd="true">
  <name>Add parser types and Markdown heading support</name>
  <files>src/renderer/lib/parseDocumentHeadings.ts, src/renderer/lib/parseDocumentHeadings.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.5, 2.6, 2.9, and 4.2.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.3 and 4.1.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md` decisions D-13 through D-17.
  </read_first>
  <action>Create `src/renderer/lib/parseDocumentHeadings.ts` exporting `interface DocumentHeading { line: number; level: number; text: string }` and `parseDocumentHeadings(model, maxDepth): DocumentHeading[]`, where `model` is compatible with `Pick<monaco.editor.ITextModel, 'getLineCount' | 'getLineContent'>`. Implement Markdown ATX heading detection for `#` through `######`, one-based line numbers, filtering deeper than `maxDepth`, and a reusable `stripMarkdownInlineFormatting(text)` helper. Strip images, links, bold asterisks, bold underscores, strikethrough, italic asterisks, italic underscores, inline code, and nested `***bold italic***` according to REQ-006.</action>
  <acceptance_criteria>
    - `parseDocumentHeadings` returns H1-H6 Markdown headings with correct one-based line numbers and levels.
    - `maxDepth=3` excludes H4-H6 while retaining H1-H3.
    - Markdown inline formatting examples from REQ-006 return plain heading text.
    - Lines with `#` later in the line are ignored as non-headings.
    - Tests include T-U-007, T-U-008, T-U-009, and T-U-010 traceability.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Markdown heading parsing and inline stripping satisfy REQ-005, REQ-006, and REQ-009.</done>
</task>

<task id="22.2.2" type="execute" tdd="true">
  <name>Add HTML heading support</name>
  <files>src/renderer/lib/parseDocumentHeadings.ts, src/renderer/lib/parseDocumentHeadings.test.ts</files>
  <read_first>
    - Product requirements REQ-007.
    - Product test plan T-U-011 and T-U-012.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/lib/parseDocumentHeadings.test.ts`.
  </read_first>
  <action>Extend the parser to detect HTML headings using a pattern equivalent to `<h([1-6])(?:\\s[^>]*)?>`, capture text through the matching closing tag on the same line, strip broad inner tags with `/<[^>]+>/g`, and filter by `maxDepth`. Keep this utility line-oriented for Phase 22 unless the product docs explicitly require multi-line HTML headings.</action>
  <acceptance_criteria>
    - `<h1>` through `<h6>` headings parse with correct levels.
    - Headings with attributes parse, such as `<h2 id="intro" class="x">Intro</h2>`.
    - Nested inline tags such as `<em>`, `<strong>`, `<b>`, `<i>`, `<code>`, `<span>`, and `<mark>` do not appear in output text.
    - Tests include T-U-011 and T-U-012 traceability.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
  </verify>
  <done>HTML headings satisfy REQ-007.</done>
</task>

<task id="22.2.3" type="execute" tdd="true">
  <name>Add code section marker support</name>
  <files>src/renderer/lib/parseDocumentHeadings.ts, src/renderer/lib/parseDocumentHeadings.test.ts</files>
  <read_first>
    - Product requirements REQ-008.
    - Product test plan T-U-013 and T-U-014.
    - `src/renderer/lib/parseDocumentHeadings.ts`.
    - `src/renderer/lib/parseDocumentHeadings.test.ts`.
  </read_first>
  <action>Extend the parser to recognize code section markers equivalent to `// -- Title --`, `// === Title ===`, `// --- Title ---`, `// ## Title`, and matching block/hash comment variants covered by the product guide. Infer marker level by indentation: 0-1 spaces is level 1, 2-3 spaces is level 2, and 4 or more spaces is level 3. Remove comment markers and decoration from rendered heading text.</action>
  <acceptance_criteria>
    - Slash comment section markers parse with decoration removed.
    - Hash comment section markers parse with decoration removed.
    - Block comment section marker variants parse with decoration removed.
    - Indentation levels map 0-1 spaces to level 1, 2-3 spaces to level 2, and 4+ spaces to level 3.
    - `maxDepth=2` excludes level 3 code markers.
    - Tests include T-U-013 and T-U-014 traceability.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Code section marker parsing satisfies REQ-008.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-22-04-D | Denial of Service | parser | mitigate | Keep parser line-oriented and linear over `getLineCount()`; avoid DOM parsing or unbounded regex backtracking. |
| T-22-05-T | Tampering | source navigation input | mitigate | Return parsed line numbers only from the Monaco model; navigation task guards disposed editor/model before use. |
| T-22-06-I | Information Disclosure | tests/fixtures | accept | Parser tests use inline fixtures and no workspace secrets. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/lib/parseDocumentHeadings.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 22.2 is complete when parser tests T-U-007 through T-U-014 pass and the utility remains pure.
</success_criteria>

<output>
Create `.planning/phases/22-outline-foundation-and-source-navigation/22-02-SUMMARY.md` when done.
</output>
