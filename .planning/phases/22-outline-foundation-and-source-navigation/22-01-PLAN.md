---
phase: 22-outline-foundation-and-source-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/panels.ts
  - src/shared/panels.test.ts
  - src/renderer/panels/registry.ts
  - src/renderer/panels/registry.test.ts
  - src/renderer/stores/appStore.ts
  - src/renderer/stores/appStore.test.ts
  - src/renderer/panels/OutlinePanel.tsx
autonomous: true
requirements:
  - REQ-001
  - REQ-003
coverage:
  - T-U-001
  - T-U-002
  - T-U-003
  - T-U-004
  - T-U-005
  - T-U-006
must_haves:
  truths:
    - "Downstream agents MUST read the Document Outline Requirements and Test Plan before implementing."
    - "`outline` is the panel type name."
    - "Outline uses Cate's existing panel registry and right dock placement machinery."
  artifacts:
    - path: "src/shared/types.ts"
      provides: "`PanelType` includes `outline`."
      contains: "outline"
    - path: "src/shared/panels.ts"
      provides: "`PANEL_DEFINITIONS.outline` shared metadata."
      contains: "Outline"
    - path: "src/renderer/panels/registry.ts"
      provides: "Lazy Outline panel entry and app-store factory delegation."
      contains: "createOutline"
    - path: "src/renderer/stores/appStore.ts"
      provides: "`createOutline(workspaceId, position?, placement?)`."
      contains: "createOutline"
---

<objective>
Register the `outline` panel type across Cate's shared, renderer, and app-store systems so a minimal Outline panel can be created in the right dock zone.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md
@.planning/phases/22-outline-foundation-and-source-navigation/22-UI-SPEC.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md
</context>

<tasks>
<task id="22.1.1" type="execute">
  <name>Register the shared Outline panel type</name>
  <files>src/shared/types.ts, src/shared/panels.ts, src/shared/panels.test.ts</files>
  <read_first>
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Requirements.md` sections 2.1, 2.3, 4.1, and 5.1.
    - `/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Outline-Chat/Document Outline Test Plan.md` sections 3.1 and 3.8.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-CONTEXT.md` decisions D-01 through D-09.
    - `.planning/phases/22-outline-foundation-and-source-navigation/22-PATTERNS.md`.
    - `src/shared/types.ts`.
    - `src/shared/panels.ts`.
    - `src/shared/panels.test.ts`.
  </read_first>
  <action>Add `outline` to the `PanelType` union. Add `PANEL_DEFINITIONS.outline` with label `Outline`, default size near `{ width: 260, height: 500 }`, minimum width no lower than `PANEL_DEFINITIONS.fileExplorer.minimumSize.width`, Cate-compatible brand/switcher/muted colors, a small outline/list-themed `ghostSvg`, and `canLiveOnCanvas: true` unless a stronger local convention emerges during implementation. Extend shared tests with T-U-001, T-U-002, and T-U-003 coverage for type acceptance, locked metadata, sizing, ghost SVG, and canvas drop/default size behavior.</action>
  <acceptance_criteria>
    - `src/shared/types.ts` includes `'outline'` in `PanelType`.
    - `PANEL_DEFINITIONS.outline.label === 'Outline'`.
    - `PANEL_DEFINITIONS.outline.defaultSize.width` is between `240` and `300`.
    - `PANEL_DEFINITIONS.outline.minimumSize.width >= PANEL_DEFINITIONS.fileExplorer.minimumSize.width`.
    - `src/shared/panels.test.ts` includes `T-U-001`, `T-U-002`, and `T-U-003` traceability text or test names.
    - `npx -p node@22 npm test -- src/shared/panels.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>Shared code recognizes `outline` as a first-class panel type.</done>
</task>

<task id="22.1.2" type="execute">
  <name>Register renderer factory and minimal Outline panel</name>
  <files>src/renderer/panels/registry.ts, src/renderer/panels/registry.test.ts, src/renderer/panels/OutlinePanel.tsx</files>
  <read_first>
    - Product requirements REQ-001 and REQ-003.
    - Product test plan T-U-004 through T-U-006 and T-I-019.
    - `src/renderer/panels/registry.ts`.
    - `src/renderer/panels/registry.test.ts`.
    - `src/renderer/panels/FlashQueryVaultSearchPanel.tsx` for panel component conventions.
  </read_first>
  <action>Create `src/renderer/panels/OutlinePanel.tsx` as a minimal typed React panel component that renders an `Outline` identity/empty state and accepts standard `PanelProps`. In `registry.ts`, lazy-load `./OutlinePanel`, assign a Phosphor icon such as `ListBullets` or `ListMagnifyingGlass`, add `PANEL_REGISTRY.outline`, and delegate create to `useAppStore.getState().createOutline(workspaceId, canvasPoint, placement) || null`. Extend registry tests to lock type, label, icon, lazy module, exact argument order, and null-on-failed-creation behavior.</action>
  <acceptance_criteria>
    - `PANEL_REGISTRY.outline.type === 'outline'`.
    - `PANEL_REGISTRY.outline.label === 'Outline'`.
    - `PANEL_REGISTRY.outline.Component` lazy-loads a module with a default export.
    - `PANEL_REGISTRY.outline.create({ workspaceId, canvasPoint, placement })` calls `createOutline(workspaceId, canvasPoint, placement)` with exact argument order.
    - Registry tests include T-U-004, T-U-005, and T-U-006 traceability.
    - `npx -p node@22 npm test -- src/renderer/panels/registry.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/panels/registry.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>The renderer registry can create lazy Outline panels through the normal factory path.</done>
</task>

<task id="22.1.3" type="execute">
  <name>Add app-store creation and right-dock placement coverage</name>
  <files>src/renderer/stores/appStore.ts, src/renderer/stores/appStore.test.ts</files>
  <read_first>
    - Product requirements REQ-001 and REQ-003.
    - Product test plan T-U-003, T-I-019, and T-I-020.
    - `src/renderer/stores/appStore.ts` around `PanelPlacement`, `addAndPlacePanel`, `createFlashQueryVaultSearch`, and `closePanel`.
    - `src/renderer/stores/appStore.test.ts`.
    - `src/renderer/stores/dockStore.ts`.
  </read_first>
  <action>Add `createOutline(workspaceId, position?, placement?)` to the app-store interface and implementation. The method must create a `PanelState` with `type: 'outline'`, `title: 'Outline'`, and `isDirty: false`, then call `addAndPlacePanel(set, get, workspaceId, panel, placement, position)`. Add or extend app-store tests proving normal creation, right dock placement with `{ target: 'dock', zone: 'right' }`, and close behavior that removes only the target Outline panel while leaving unrelated dock panels registered.</action>
  <acceptance_criteria>
    - `src/renderer/stores/appStore.ts` exports a `createOutline` action.
    - Created Outline panels have `type === 'outline'`, `title === 'Outline'`, and `isDirty === false`.
    - A test proves `createOutline(workspaceId, undefined, { target: 'dock', zone: 'right' })` docks into the right zone.
    - A test proves closing one Outline panel does not close an unrelated right-zone panel.
    - `npx -p node@22 npm test -- src/renderer/stores/appStore.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/stores/appStore.test.ts</automated>
    <automated>npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <done>The Outline panel exists in Cate's shared, renderer, store, and dock creation machinery.</done>
</task>
</tasks>

<threat_model>
## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-22-01-T | Tampering | panel registry/store | mitigate | Add exact type/factory tests so panel creation cannot silently route to a different type or placement. |
| T-22-02-D | Denial of Service | dock placement | mitigate | Reuse existing dock store and test close behavior so right-zone panels are not accidentally removed. |
| T-22-03-I | Information Disclosure | renderer state | accept | Outline registration stores no document content or credentials in shared panel metadata. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/shared/panels.test.ts src/renderer/panels/registry.test.ts src/renderer/stores/appStore.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
Plan 22.1 is complete when `outline` is registered without casts, has shared/renderer/store tests, and can be created in the existing right dock zone.
</success_criteria>

<output>
Create `.planning/phases/22-outline-foundation-and-source-navigation/22-01-SUMMARY.md` when done.
</output>
