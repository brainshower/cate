---
phase: 05-settings-dialog-workspace-menu-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/App.tsx
  - src/renderer/dialogs/FlashQueryConnectionDialog.tsx
  - src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx
  - src/renderer/stores/uiStore.test.ts
autonomous: true
requirements:
  - REQ-034
  - REQ-038
must_haves:
  truths:
    - "User can open a FlashQuery Connection dialog for the selected workspace."
    - "User can close the dialog through the existing modal close paths without persisting form state."
  artifacts:
    - path: "src/renderer/dialogs/FlashQueryConnectionDialog.tsx"
      provides: "Dialog shell controlled by useUIStore"
    - path: "src/renderer/App.tsx"
      provides: "Root modal mounting beside SavedLayoutsDialog"
    - path: "src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx"
      provides: "Dialog shell behavior coverage"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/renderer/dialogs/FlashQueryConnectionDialog.tsx"
      via: "root modal render"
      pattern: "FlashQueryConnectionDialog"
    - from: "src/renderer/dialogs/FlashQueryConnectionDialog.tsx"
      to: "src/renderer/stores/uiStore.ts"
      via: "showFlashQueryConnectionDialog and setShowFlashQueryConnectionDialog"
      pattern: "showFlashQueryConnectionDialog"
---

<objective>
Create the Phase 5 dialog shell and mount it in Cate's existing root modal stack.

Purpose: Establish the visible settings surface for REQ-034 and reuse the Phase 4 UI-store visibility slice for REQ-038 without adding persistence or form behavior yet.
Output: A mounted `FlashQueryConnectionDialog` shell with focused tests for render, close, and UI-store visibility.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-UI-SPEC.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
@.planning/phases/04-vault-panel-shared-chip/04-03-SUMMARY.md
@src/renderer/dialogs/SavedLayoutsDialog.tsx
@src/renderer/stores/uiStore.ts
@src/renderer/App.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Verify the existing UI-store dialog state</name>
  <files>src/renderer/stores/uiStore.test.ts</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
    - .planning/phases/04-vault-panel-shared-chip/04-03-SUMMARY.md
    - src/renderer/stores/uiStore.ts
    - src/renderer/stores/uiStore.test.ts
  </read_first>
  <behavior>
    - T-U-055: default `showFlashQueryConnectionDialog` is false.
    - T-U-055: `setShowFlashQueryConnectionDialog(true)` opens the dialog state.
    - T-U-055: `setShowFlashQueryConnectionDialog(false)` closes the dialog state.
  </behavior>
  <action>Per D-01, D-03, D-05, D-26, confirm the Phase 4 store slice already satisfies REQ-038. Extend `src/renderer/stores/uiStore.test.ts` only if any T-U-055 assertion is missing; do not add a second store, persistent dialog data, or token fields to Zustand.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/stores/uiStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `showFlashQueryConnectionDialog` initializes to `false`.
    - The setter toggles true and false in the existing `useUIStore`.
    - No additional dialog form state is stored in `uiStore`.
  </acceptance_criteria>
  <done>All acceptance criteria above are satisfied and the automated verification commands pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the FlashQuery connection dialog shell</name>
  <files>src/renderer/dialogs/FlashQueryConnectionDialog.tsx, src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-UI-SPEC.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
    - src/renderer/dialogs/SavedLayoutsDialog.tsx
    - src/renderer/stores/uiStore.ts
    - src/renderer/stores/appStore.ts
    - src/renderer/panels/FlashQueryVaultPanel.test.tsx
  </read_first>
  <behavior>
    - T-I-050: dialog renders when `showFlashQueryConnectionDialog` is true.
    - T-I-051: dialog renders nothing when the store flag is false.
    - T-I-052: header contains Phosphor `Lightning`, title `FlashQuery Connection`, subtitle `For workspace: <workspace name>`, and close button.
    - T-I-053, T-I-054, T-I-055: Escape, overlay click, and close X close the dialog without calling FlashQuery IPC.
    - UI-SPEC accessibility: opening the shell moves initial focus to the URL field scaffold or its Plan 05-01 focus target; all shell controls are keyboard reachable in visual order; Tab and Shift+Tab stay contained inside the dialog.
  </behavior>
  <action>Per D-04, D-06, D-07, D-17, D-23, D-24, D-25, create `FlashQueryConnectionDialog.tsx` mirroring `SavedLayoutsDialog` overlay, click-outside, Escape, and compact modal chrome. Use `useUIStore.showFlashQueryConnectionDialog`, `useUIStore.setShowFlashQueryConnectionDialog`, and selected workspace data from `useAppStore`. Render `role="dialog"`, `aria-modal="true"`, a teal Phosphor `Lightning`, title `FlashQuery Connection`, subtitle `For workspace: <workspace name>`, and a Phosphor `X` button labelled `Close FlashQuery connection dialog`. Include an initial inert body scaffold for the fields that Plan 05-02 will complete, with a URL input/focus target labelled `FlashQuery URL`; on open, programmatically focus that URL target. Because `SavedLayoutsDialog` does not currently implement focus trapping, add a small dialog-local keydown handler or helper that intercepts Tab/Shift+Tab while open, finds focusable descendants inside the dialog container, wraps focus from last to first and first to last, and never lets Tab escape to the page behind the modal. Preserve keyboard reachability in visual order for close and scaffold controls. Do not wire save, probe, or remove behavior in this plan. Use Cate semantic classes (`text-primary`, `text-secondary`, `text-muted`, `bg-surface-*`, `bg-hover`, `border-subtle`) and avoid rendered stock `zinc`, `gray`, or `slate` classes.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx src/renderer/stores/uiStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Closed dialog returns `null` and leaves no overlay in the DOM.
    - Open dialog has accessible dialog semantics and locked title/subtitle copy.
    - Opening the dialog places focus on the URL field scaffold/focus target, and tests assert keyboard reachability of the close control without mouse interaction.
    - Tab from the last focusable dialog control wraps to the first focusable control, and Shift+Tab from the first focusable control wraps to the last; tests assert focus does not escape the dialog container.
    - Escape, overlay click, and close X set `showFlashQueryConnectionDialog` to false.
    - Tests prove no `flashquerySetConnection`, probe, or token IPC is called by shell-only close paths.
  </acceptance_criteria>
  <done>All acceptance criteria above are satisfied and the automated verification commands pass.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the dialog in the root modal stack</name>
  <files>src/renderer/App.tsx, src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
    - src/renderer/App.tsx
    - src/renderer/dialogs/SavedLayoutsDialog.tsx
    - src/renderer/dialogs/FlashQueryConnectionDialog.tsx
  </read_first>
  <action>Per D-05 and REQ-034, import `FlashQueryConnectionDialog` in `src/renderer/App.tsx` and mount it beside `SavedLayoutsDialog` and `PostUpdateFeedbackDialog` in the existing modal overlay section. Keep the component self-hiding when closed; do not add route-level or settings-window plumbing.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` imports and renders `<FlashQueryConnectionDialog />` in the modal overlay section.
    - The dialog remains globally available from store state and renders nothing when closed.
    - Typecheck passes under Node 22.
  </acceptance_criteria>
  <done>All acceptance criteria above are satisfied and the automated verification commands pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer UI store to dialog | Untrusted renderer interactions toggle a sensitive settings surface. |
| Dialog display to user | Workspace names and future error text become visible UI and must not leak tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-S | Spoofing | `FlashQueryConnectionDialog` shell | mitigate | Use locked title, workspace subtitle, and root modal mount so users can identify the selected workspace before entering credentials. |
| T-05-01-I | Information Disclosure | Dialog shell/test output | mitigate | Do not read or render bearer tokens in Plan 05-01; keep tests free of token snapshots. |
| T-05-01-T | Tampering | `useUIStore` state | accept | Visibility state is renderer-local and does not persist or mutate connection metadata. |
| T-05-01-SC | Tampering | npm installs | mitigate | No package installs are planned; if an executor adds one, stop for Package Legitimacy Gate review. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/stores/uiStore.test.ts src/renderer/dialogs/FlashQueryConnectionDialog.test.ts`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
- REQ-034 dialog shell exists, follows the locked Cate modal chrome, and mounts at the root.
- REQ-038 is verified through the existing `useUIStore` visibility slice.
- Close paths are implemented without connection persistence or token access.
</success_criteria>

<must_haves>
- User can open and close the FlashQuery Connection dialog for the selected workspace.
- Dialog title, subtitle, close behavior, modal semantics, and token-safe shell tests exist.
- No deferred Phase 6 editor behavior, Phase 7 E2E work, OAuth, keychain, stdio, or custom context menu is introduced.
</must_haves>

<output>
Create `.planning/phases/05-settings-dialog-workspace-menu-entry/05-01-SUMMARY.md` when done.
</output>
