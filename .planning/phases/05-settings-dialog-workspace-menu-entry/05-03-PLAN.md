---
phase: 05-settings-dialog-workspace-menu-entry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/sidebar/WorkspaceTab.tsx
  - src/renderer/sidebar/WorkspaceTab.test.tsx
autonomous: true
requirements:
  - REQ-039
must_haves:
  truths:
    - "User can open the FlashQuery Connection dialog from the native workspace context menu."
    - "Workspace context menu remains native and does not render a custom React dropdown."
  artifacts:
    - path: "src/renderer/sidebar/WorkspaceTab.tsx"
      provides: "Native workspace menu item and dispatch handler"
    - path: "src/renderer/sidebar/WorkspaceTab.test.tsx"
      provides: "Menu item order/action/no-custom-menu tests"
  key_links:
    - from: "src/renderer/sidebar/WorkspaceTab.tsx"
      to: "window.electronAPI.showContextMenu"
      via: "NativeContextMenuItem array"
      pattern: "flashquery-connection"
    - from: "src/renderer/sidebar/WorkspaceTab.tsx"
      to: "useUIStore.getState().setShowFlashQueryConnectionDialog(true)"
      via: "context menu result switch"
      pattern: "setShowFlashQueryConnectionDialog"
---

<objective>
Add the workspace context-menu entry that opens the FlashQuery connection dialog.

Purpose: Make the settings dialog reachable from the workspace surface while preserving Cate's native context-menu pattern.
Output: `WorkspaceTab` native menu insertion, switch handling, and focused renderer tests for REQ-039.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-UI-SPEC.md
@.planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
@src/renderer/sidebar/WorkspaceTab.tsx
@src/renderer/sidebar/WorkspaceTab.test.tsx
@src/renderer/stores/uiStore.ts
@src/shared/electron-api.d.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add workspace context-menu coverage</name>
  <files>src/renderer/sidebar/WorkspaceTab.test.tsx</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-UI-SPEC.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
    - src/renderer/sidebar/WorkspaceTab.tsx
    - src/renderer/sidebar/WorkspaceTab.test.tsx
    - src/renderer/stores/appStore.ts
    - src/renderer/stores/uiStore.ts
  </read_first>
  <behavior>
    - T-I-075: `showContextMenu` receives `{ id: 'flashquery-connection', label: 'FlashQuery Connection...' }`.
    - T-I-076: item order is `copy-cwd`, separator, `flashquery-connection`, separator, `duplicate`.
    - T-I-077: selecting the id opens `showFlashQueryConnectionDialog`.
    - T-I-078: rendering `WorkspaceTab` does not create a custom React dropdown/menu element.
  </behavior>
  <action>Per D-19 through D-22 and D-28, extend `WorkspaceTab.test.tsx` with a render harness for the workspace row or tab that can invoke its context-menu handler under jsdom. Mock `window.electronAPI.showContextMenu`, seed a workspace in `useAppStore`, reset `useUIStore.showFlashQueryConnectionDialog` before each test, and assert the exact item id, label, order, returned-id behavior, and absence of custom dropdown DOM. Keep the existing `TerminalPanelRow` tests intact.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/sidebar/WorkspaceTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before the menu item implementation.
    - Tests assert the native menu item array, not a rendered custom menu.
    - Tests prove selecting `flashquery-connection` toggles the UI-store dialog flag and does not duplicate/close/select the workspace.
  </acceptance_criteria>
  <done>All acceptance criteria above are satisfied and the automated verification commands pass.</done>
</task>

<task type="auto">
  <name>Task 2: Insert and handle the FlashQuery Connection native menu item</name>
  <files>src/renderer/sidebar/WorkspaceTab.tsx</files>
  <read_first>
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-CONTEXT.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-UI-SPEC.md
    - .planning/phases/05-settings-dialog-workspace-menu-entry/05-PATTERNS.md
    - src/renderer/sidebar/WorkspaceTab.tsx
    - src/renderer/stores/uiStore.ts
    - src/shared/electron-api.d.ts
  </read_first>
  <action>Per D-19, D-20, D-21, D-22, import `useUIStore` into `WorkspaceTab.tsx`. In the existing `NativeContextMenuItem[]` passed to `window.electronAPI.showContextMenu`, insert `{ id: 'flashquery-connection', label: 'FlashQuery Connection...' }` between `copy-cwd` and `duplicate` with a separator before and after it, producing the exact order `copy-cwd`, separator, `flashquery-connection`, separator, `duplicate`. Add `case 'flashquery-connection': useUIStore.getState().setShowFlashQueryConnectionDialog(true); break` to the returned-id switch. Do not add a React dropdown, popover, portal, menu library, or workspace mutation side effect for this item.</action>
  <verify>
    <automated>npx -p node@22 npm test -- src/renderer/sidebar/WorkspaceTab.test.tsx</automated>
    <automated>npx -p node@22 npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Workspace native menu always includes the FlashQuery Connection item.
    - Item position is exactly between `copy-cwd` and the duplicate group with surrounding separators.
    - Selecting the item opens `showFlashQueryConnectionDialog`.
    - No custom React context-menu UI is introduced.
  </acceptance_criteria>
  <done>All acceptance criteria above are satisfied and the automated verification commands pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to native menu IPC | Renderer passes menu item descriptors to Electron's native context-menu bridge. |
| Native menu result to renderer state | Returned item id triggers renderer actions and must not mutate unrelated workspace state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-S | Spoofing | Workspace context menu label | mitigate | Use locked label `FlashQuery Connection...` in the native workspace menu so the action is identifiable. |
| T-05-03-T | Tampering | Context-menu result handling | mitigate | Handle only exact id `flashquery-connection` and limit action to `setShowFlashQueryConnectionDialog(true)`. |
| T-05-03-I | Information Disclosure | Native menu descriptors | accept | Menu item contains no token, URL, instance ID, or workspace secret. |
| T-05-03-E | Elevation of Privilege | Custom menu bypass | mitigate | Preserve `window.electronAPI.showContextMenu`; do not add direct Electron or Node access in renderer. |
| T-05-03-SC | Tampering | npm installs | mitigate | No package installs are planned; stop for package legitimacy review if an executor tries to add one. |
</threat_model>

<verification>
- `npx -p node@22 npm test -- src/renderer/sidebar/WorkspaceTab.test.tsx`
- `npx -p node@22 npm run typecheck`
</verification>

<success_criteria>
- REQ-039 menu item exists in the required native-menu position.
- Selecting the item opens the Phase 5 dialog visibility state.
- Existing workspace context-menu actions remain intact and no custom context-menu UI is added.
</success_criteria>

<must_haves>
- User can reach FlashQuery connection settings from a workspace context menu.
- Menu rendering remains native through `window.electronAPI.showContextMenu`.
- The menu action has no connection persistence, token handling, or workspace mutation side effect.
</must_haves>

<output>
Create `.planning/phases/05-settings-dialog-workspace-menu-entry/05-03-SUMMARY.md` when done.
</output>
