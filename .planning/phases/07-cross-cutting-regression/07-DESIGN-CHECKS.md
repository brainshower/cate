---
phase: 07-cross-cutting-regression
plan: 03
status: completed
source:
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
  - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
  - .planning/phases/07-cross-cutting-regression/07-CONTEXT.md
updated: 2026-05-30
---

# Phase 07 Design Checks

Durable checklist for Phase 7 manual and design-token verification. The first manual check is a code/design-token review. T-M-002 through T-M-007 are intended to be running-app visual checks against the product design PDFs, but no screenshot/PDF comparison artifact was captured during Phase 7; those rows are therefore marked `MANUAL-DEFERRED` while retaining the behavior and token evidence that was actually run.

## Checklist

| Test ID | Requirement / Design Source | Surface Under Review | Exact Verification Steps | Expected Result | Automated Evidence | Manual Result | Reviewer / Date | Notes |
|---|---|---|---|---|---|---|---|---|
| T-M-001 | REQ-045; Test Plan §4.7.3; Requirements §6.9.2 | `src/renderer/panels/FlashQueryVaultPanel.tsx`, `src/renderer/components/Chip.tsx`, `src/renderer/dialogs/FlashQueryConnectionDialog.tsx`, `src/renderer/components/VaultBadge.tsx` | Review rendered output and source for FlashQuery UI surfaces. Confirm semantic token classes such as `bg-surface-N`, `text-primary`, `text-secondary`, `text-muted`, `bg-hover`, and `border-subtle`; confirm no stock neutral surface/text classes such as `bg-zinc-*`, `text-gray-*`, `text-slate-*`, or `text-zinc-*`. | New FlashQuery UI follows Cate semantic tokens. Allowed brand accent usage is limited to existing FlashQuery accent classes such as `text-teal-400`, `text-[#5AD8B8]`, or the configured `flashqueryVault` tint. | `npx -p node@22 npm test -- src/renderer/panels/FlashQueryVaultPanel.test.tsx src/renderer/components/Chip.test.tsx src/renderer/dialogs/FlashQueryConnectionDialog.test.tsx src/renderer/components/VaultBadge.test.tsx src/renderer/panels/EditorPanel.test.tsx` passed: 5 files, 73 tests. Expanded workspace-menu/component run passed: 6 files, 84 tests. | PASS | Codex executor / 2026-05-30 | Source review confirmed forbidden neutral assertions in panel/chip/dialog/badge tests and only allowed FlashQuery accent usage in `src/shared/panels.ts`. |
| T-M-002 | Test Plan §4.7.5; UI Spec `Designs/FQ Vault Panel/FlashQuery Vault Panel — Populated.pdf` | Running `FlashQueryVaultPanel` populated tree | Launch Cate with the Phase 7 FlashQuery E2E stub or equivalent local setup. Configure a live connection, open the FlashQuery Vault panel, inspect host chrome, status chip, refresh affordance, populated rows, indentation, icons, selection, and spacing. | Populated vault panel visually matches the design intent and Cate panel chrome while preserving wrapper-owned `FlashQuery Vault` identity and a truncatable host. | `npx -p node@22 npm run test:e2e -- e2e/flashquery-happy-path.spec.ts e2e/flashquery-disconnect.spec.ts e2e/flashquery-vault-browse.spec.ts` passed: 3 Electron tests. Component coverage: `FlashQueryVaultPanel.test.tsx` passed inside the 84-test focused run. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Behavior/token evidence exists, but no screenshot or side-by-side comparison against `FlashQuery Vault Panel — Populated.pdf` was captured. |
| T-M-003 | Test Plan §4.7.5; UI Spec `Designs/FQ Vault Panel States/` | Running vault panel no-connection, connecting, disconnected, and empty-vault states | In the running app, inspect no-connection, connecting/probing, disconnected with retry/edit actions, and live empty-vault states. Use stub availability and empty-vault fixture where needed. | All four states match the designed composition, use Cate token styling, surface useful copy, and include no create-new-vault-document affordance. | Electron E2E passed for disconnected/retry and empty-vault states: `e2e/flashquery-disconnect.spec.ts` and `e2e/flashquery-vault-browse.spec.ts`. Component state coverage passed in `FlashQueryVaultPanel.test.tsx`. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Behavior/token evidence exists, but no screenshot or side-by-side comparison against `Designs/FQ Vault Panel States/` was captured. |
| T-M-004 | Test Plan §4.7.5; UI Spec `Designs/FQ Connection status/` | `Chip` connection-status states in vault panel and dialog context | In the running app, inspect connecting, live, and disconnected chips. Confirm only disconnected is actionable, hover shows retry/error information, and live/connecting remain inert. | Three chip states match design with shared chip surface, compact sizing, correct labels, and no stock neutral surface/text styling. | `Chip.test.tsx` passed in both focused runs; Electron E2E passed live and disconnected/retry flows in happy-path and disconnect specs. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Behavior/token evidence exists, but no screenshot or side-by-side comparison against `Designs/FQ Connection status/` was captured. |
| T-M-005 | Test Plan §4.7.5; UI Spec `Designs/Connection settings dialog/` | Running `FlashQueryConnectionDialog` setup, pass, and fail states | Open the workspace FlashQuery Connection dialog in the running app. Inspect first-time setup, edit mode, URL/token fields, token reveal/hide, Test connection success, Test connection failure, Save/Cancel/Remove affordances, and inline validation. | Dialog matches design for pass and fail states, keeps token-safe behavior, and uses Cate modal/input/button styling. | Running app dialog was used in each FlashQuery E2E configure flow; `FlashQueryConnectionDialog.test.tsx` passed in the 84-test focused run with pass/fail probe, validation, reveal/hide, save, cancel, and remove coverage. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Behavior/token evidence exists, but no screenshot or side-by-side comparison against `Designs/Connection settings dialog/` was captured. No bearer token values recorded. |
| T-M-006 | Test Plan §4.7.5; UI Spec `Designs/Workspace Context Menu/` | Native workspace context menu from `src/renderer/sidebar/WorkspaceTab.tsx` | In the running app, right-click a workspace row and inspect native menu entry order. Confirm `FlashQuery Connection...` appears in the UI Spec position between Copy Working Directory and Duplicate Workspace and opens the clicked workspace dialog. | Menu positioning matches design/source order, and selection switches to the clicked workspace before opening the dialog. | `WorkspaceTab.test.tsx` passed in the 84-test focused run; source lines `src/renderer/sidebar/WorkspaceTab.tsx:303-307` order Copy Working Directory, separator, FlashQuery Connection, separator, Duplicate Workspace. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Native OS menu cannot be reliably screenshot-tested in Playwright; menu order and clicked-workspace dialog routing are verified, but no PDF visual comparison artifact was captured. |
| T-M-007 | Test Plan §4.7.5; UI Spec `Designs/Editor panel/`; Phase 6 badge baseline | Running `EditorPanel` with a `flashquery://` vault document and `VaultBadge` | In the running app, open a vault-backed markdown document. Inspect editor title chrome, dirty-state dot position, badge surface, teal Vault icon, exact `Vault · <host>` text with U+00B7, tooltip with decoded vault path, and local-file editor without badge. | Vault editor visually matches design while excluding the known design-spec drift `rev 42` indicator because revision/conflict/frontmatter UI is not v1 scope. The badge must not render `. <host>`. | Electron happy-path E2E passed and opened a vault editor with badge. `VaultBadge.test.tsx` and `EditorPanel.test.tsx` passed in focused runs, including `Vault · fq.local:3100`, U+00B7, no `. <host>`, no local-file badge, tooltip, and no `rev 42`/conflict/frontmatter copy. | MANUAL-DEFERRED | Codex executor / 2026-05-30 | Behavior/token evidence exists and `rev 42` remains excluded as v1 non-scope, but no screenshot or side-by-side comparison against `Designs/Editor panel/` was captured. |

## Evidence Notes

- Final verification should cite the concrete `PASS`, `FAIL`, or `BLOCKED` evidence above.
- Screenshot files, Playwright/Electron run notes, or source-location notes are acceptable evidence when they identify the exact inspected surface.
- Failures or blockers must remain explicit; do not convert a blocked visual check into a pass without running-app evidence.
