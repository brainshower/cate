---
phase: 07-cross-cutting-regression
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/flashquery/clientManager.ts
  - src/main/flashquery/clientManager.test.ts
  - src/main/ipc/flashquery.ts
  - src/main/ipc/flashquery.test.ts
autonomous: true
requirements:
  - REQ-043
  - REQ-044
must_haves:
  truths:
    - "GET /mcp/info probes never send Authorization headers, including manager probes and dialog dry-run probes."
    - "Bearer tokens still flow to authenticated MCP POST transport calls after a successful public info probe."
    - "Existing Cate smoke and drag/panel E2E specs remain unchanged and pass as the REQ-043 baseline."
  artifacts:
    - path: "src/main/flashquery/clientManager.ts"
      provides: "manager probe behavior that omits Authorization on GET /mcp/info"
      contains: "fetch(this.buildInfoUrl(connection.url)"
    - path: "src/main/ipc/flashquery.ts"
      provides: "dialog dry-run probe behavior that omits Authorization on GET /mcp/info"
      contains: "FLASHQUERY_PROBE"
    - path: "src/main/flashquery/clientManager.test.ts"
      provides: "unit coverage for no-auth info probe and authenticated MCP transport"
      contains: "T-U-021"
    - path: "src/main/ipc/flashquery.test.ts"
      provides: "IPC dry-run probe coverage for no-auth info probe"
      contains: "T-I-062"
  key_links:
    - from: "src/main/flashquery/clientManager.ts"
      to: "src/main/flashquery/clientManager.test.ts"
      via: "GET /mcp/info header assertions"
      pattern: "headers.*Accept"
    - from: "src/main/ipc/flashquery.ts"
      to: "src/main/ipc/flashquery.test.ts"
      via: "FLASHQUERY_PROBE handler fetch assertions"
      pattern: "FLASHQUERY_PROBE"
---

<objective>
Correct the product/code drift around FlashQuery readiness probes and preserve Cate's existing E2E regression baseline before new FlashQuery E2E specs depend on stricter stub behavior.

Purpose: Phase 7's stub must enforce the authoritative REQ-004 contract instead of tolerating a bearer token on public `GET /mcp/info` probes.
Output: No-auth probe implementation, updated unit/IPC tests, and a verified existing E2E regression gate.
</objective>

<execution_context>
@/Users/matt/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matt/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-cutting-regression/07-CONTEXT.md
@.planning/phases/07-cross-cutting-regression/07-RESEARCH.md
@.planning/phases/07-cross-cutting-regression/07-PATTERNS.md
@.planning/phases/07-cross-cutting-regression/07-VALIDATION.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
@/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md

<interfaces>
From `src/main/flashquery/clientManager.ts`:
- `FlashQueryClientManager.connect(workspaceId, connection)` performs the public readiness probe.
- `FlashQueryClientManager.listVault/getDocument/writeDocument` create the authenticated MCP transport and must continue to attach bearer auth to POST `/mcp` through `requestInit.headers`.

From `src/main/ipc/flashquery.ts`:
- `FLASHQUERY_PROBE` dry-runs current dialog values without persistence.
- `sanitizeError(error, token)` must keep redacting token-bearing error messages even after the public probe stops sending the token.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 7 | Prove full v1 workflow, restart behavior, existing panel health, and UI design discipline | 07-01, 07-02, 07-03 | COVERED | Regression baseline starts here; E2E workflow and design evidence continue in dependent plans.
REQ | REQ-043 | No regression of existing Cate panels | 07-01, 07-02, 07-03 | COVERED | Existing smoke/drag specs are preserved and run unchanged; final verification reruns full E2E.
REQ | REQ-044 | Connection persists across Cate restart without eager startup probe | 07-01, 07-02, 07-03 | COVERED | Probe auth correction in 07-01 enables strict persistence/lazy-probe assertions in 07-02.
REQ | REQ-045 | Cate design-token discipline | 07-03 | COVERED | Automated token tests and manual design checklist are planned in the final wave.
RESEARCH | R-01 | Strict local FlashQuery HTTP MCP stub with request counters and in-memory documents | 07-02 | COVERED | Stub enforces no-auth `/mcp/info`, supports list/read/write, and resets state.
RESEARCH | R-02 | Test-only reusable E2E userData/root for restart proof | 07-02 | COVERED | `CATE_E2E_USER_DATA_DIR` is scoped to `CATE_E2E=1`.
RESEARCH | R-03 | Durable manual visual/design evidence artifact | 07-03 | COVERED | `07-DESIGN-CHECKS.md` covers T-M-001..007.
CONTEXT | C-01 | Product Requirements/Test Plan docs are authoritative and must be read by downstream agents | 07-01, 07-02, 07-03 | COVERED | Every task includes both supplied product docs in `<read_first>`.
CONTEXT | C-02 | Existing E2E specs must not be weakened or deleted | 07-01 | COVERED | Task 2 adds a diff/no-skip gate.
CONTEXT | C-03 | Restart test must prove persisted connection separately from eager probing | 07-02 | COVERED | Persistence spec resets stub counters between launches.
CONTEXT | C-04 | Stub must support GET `/mcp/info`, list, read, write, up/down, and mutable documents | 07-02 | COVERED | Fixture task requires all listed behaviors.
CONTEXT | C-05 | Design-token and manual visual checks must be durable | 07-03 | COVERED | Checklist and verification artifacts are planned.
CONTEXT | C-06 | Deferred non-v1 features must remain out of scope | 07-02, 07-03 | COVERED | E2E acceptance criteria forbid create, frontmatter editing, conflict detection, OAuth, stdio, and live notifications.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix public info probes to omit bearer auth</name>
  <files>src/main/flashquery/clientManager.ts, src/main/flashquery/clientManager.test.ts, src/main/ipc/flashquery.ts, src/main/ipc/flashquery.test.ts</files>
  <read_first>
    - src/main/flashquery/clientManager.ts
    - src/main/flashquery/clientManager.test.ts
    - src/main/ipc/flashquery.ts
    - src/main/ipc/flashquery.test.ts
    - .planning/phases/07-cross-cutting-regression/07-CONTEXT.md
    - .planning/phases/07-cross-cutting-regression/07-PATTERNS.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
  </read_first>
  <behavior>
    - T-U-021 / REQ-004: manager first-time connect issues `GET /mcp/info` with headers exactly containing `Accept: application/json` and no `Authorization`, even when `connection.auth.token` is non-empty.
    - T-U-023: manager still attaches `Authorization: Bearer <stored-token>` to the MCP `StreamableHTTPClientTransport` request init for subsequent POST `/mcp` tool calls.
    - T-I-062 / REQ-004: dialog dry-run `flashquery:probe` issues `GET /mcp/info` with no `Authorization`, even when the form token is present.
  </behavior>
  <action>Update `FlashQueryClientManager.probeConnection` and the IPC `probeConnection` helper so public info probes build only the `Accept: application/json` header. Remove or rewrite the existing tests that assert bearer auth on the info probe; do not weaken redaction tests, and keep authenticated MCP transport assertions in `clientManager.test.ts` intact. This implements the supplied product docs' REQ-004 correction before E2E stubs assert the contract.</action>
  <acceptance_criteria>
    - Source assertion: neither `src/main/flashquery/clientManager.ts` nor `src/main/ipc/flashquery.ts` assigns `headers.Authorization` in the code path that fetches `buildInfoUrl(...)/mcp/info`.
    - Source assertion: `clientManager.test.ts` contains a bearer-token case proving the info probe omits `Authorization`.
    - Source assertion: `clientManager.test.ts` still asserts MCP transport POST auth via `options.requestInit?.headers?.get('Authorization')`.
    - Source assertion: `flashquery.test.ts` no longer expects `Authorization: Bearer current-token` for `FLASHQUERY_PROBE`.
    - Behavior assertion: token redaction tests still cover token-bearing failures even though probe requests no longer send tokens.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/main/flashquery/clientManager.test.ts src/main/ipc/flashquery.test.ts</automated>
  </verify>
  <done>Manager and dialog readiness probes comply with REQ-004, and authenticated MCP tool calls remain authenticated.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve existing Cate E2E regression baseline</name>
  <files>e2e/smoke.spec.ts, e2e/drag-detach.spec.ts, e2e/drag-move.spec.ts, e2e/drag-canvas-into-canvas.spec.ts, e2e/drag-split.spec.ts</files>
  <read_first>
    - e2e/smoke.spec.ts
    - e2e/drag-detach.spec.ts
    - e2e/drag-move.spec.ts
    - e2e/drag-canvas-into-canvas.spec.ts
    - e2e/drag-split.spec.ts
    - e2e/fixtures/electron-app.ts
    - .planning/phases/07-cross-cutting-regression/07-CONTEXT.md
    - .planning/phases/07-cross-cutting-regression/07-VALIDATION.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Requirements.md
    - /Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Initial connection to FQ/Cate-FlashQuery Integration v1 — Test Plan.md
  </read_first>
  <action>Run the existing REQ-043 Playwright regression specs unchanged: `e2e/smoke.spec.ts`, `e2e/drag-detach.spec.ts`, `e2e/drag-move.spec.ts`, `e2e/drag-canvas-into-canvas.spec.ts`, and `e2e/drag-split.spec.ts`. Do not delete, skip, narrow, or relax assertions in these specs. If a failure is caused by the no-auth probe fix, repair the FlashQuery implementation; if a pre-existing unrelated failure appears, capture the exact failing test and output in the plan summary without weakening the suite.</action>
  <acceptance_criteria>
    - CLI output assertion: the focused Playwright command exits 0 under Node 20/22, or the summary records an unrelated pre-existing failure with exact test name and error.
    - Source assertion: `git diff -- e2e/smoke.spec.ts e2e/drag-detach.spec.ts e2e/drag-move.spec.ts e2e/drag-canvas-into-canvas.spec.ts e2e/drag-split.spec.ts` shows no deleted tests, no `test.skip`, no reduced assertions, and no timeout-only masking changes.
    - Behavior assertion: T-E-001 through T-E-005 remain represented by the existing spec filenames from the authoritative Test Plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- e2e/smoke.spec.ts e2e/drag-detach.spec.ts e2e/drag-move.spec.ts e2e/drag-canvas-into-canvas.spec.ts e2e/drag-split.spec.ts</automated>
  </verify>
  <done>REQ-043 has an unchanged existing E2E regression baseline before new FlashQuery specs are added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer->main IPC | Dialog probe input crosses from untrusted renderer state into privileged main process fetch behavior. |
| main->FlashQuery HTTP | Main process sends public probe and authenticated MCP requests to a user-configured URL. |
| E2E process->app | Test assertions inspect app behavior and must not change production runtime semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-I | Information Disclosure | `FlashQueryClientManager.probeConnection`, IPC `probeConnection` | mitigate | Omit `Authorization` on public `GET /mcp/info`; keep token redaction in error handling. |
| T-07-01-S | Spoofing | user-configured FlashQuery URL | accept | Existing URL validation and probe response parsing remain in place; this plan changes headers only. |
| T-07-01-E | Elevation of Privilege | renderer probe request | mitigate | Renderer still cannot fetch directly; probe remains behind typed IPC in `src/main/ipc/flashquery.ts`. |
| T-07-01-T | Tampering | existing E2E regression specs | mitigate | Do not weaken existing Playwright tests; verify diff for deleted/skipped/narrowed tests. |
| T-07-01-SC | Tampering | npm installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run focused unit/IPC tests for the probe correction, then run the unchanged existing Playwright regression specs for T-E-001 through T-E-005.
</verification>

<success_criteria>
- REQ-004 product drift is corrected in source and tests before the Phase 7 E2E stub enforces no-auth info probes.
- REQ-043 existing smoke/drag/panel specs remain unchanged and are either green or have documented unrelated failures without test weakening.
</success_criteria>

<output>
Create `.planning/phases/07-cross-cutting-regression/07-01-SUMMARY.md` when done.
</output>
