---
milestone: v1.6
milestone_name: Graph Intelligence Monaco Side Panel
audited: 2026-07-01T14:32:06Z
head: 0b92d52
status: passed_with_tech_debt
scores:
  requirements: 23/23 satisfied
  phases: 2/2 phase verifications present, 2/2 phase validations present
  integration: passed, no open cross-phase issues
  flows: passed, deterministic Electron E2E covered
gaps:
  requirements: []
  integration: []
  flows: []
resolved_gap_documents:
  - "/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Graph Intelligence Monaco Side Panel Gaps.md"
tech_debt:
  - "`.planning/STATE.md` frontmatter still says `completed_phases: 1` and `percent: 50` even though the roadmap, phase docs, and `gsd-sdk query init.milestone-op` report v1.6 complete."
nyquist:
  compliant_phases: []
  partial_phases:
    - 27-graph-data-contract-and-whole-document-graph-view
    - 28-selection-detail-local-filter-chrome-polish-and-e2e-hardenin
  missing_phases: []
  overall: partial
---

# Milestone v1.6 Audit: Graph Intelligence Monaco Side Panel

## Result

**Status:** passed_with_tech_debt

The v1.6 milestone satisfies its functional requirements at current HEAD `0b92d52`. All 23 requirements in `.planning/REQUIREMENTS.md` are checked, mapped through the traceability table, covered by Phase 27/28 verification artifacts, and connected by current code and E2E flows. No blocking requirement, integration, or flow gap remains open.

This audit deliberately keeps one non-blocking caveat visible: stale `.planning/STATE.md` progress counters.

## Scope

`ROADMAP.md` identifies v1.6 as "Graph Intelligence Monaco Side Panel", phases 27 and 28.

| Phase | Requirement IDs | Verification | Validation | Audit status |
|---|---|---:|---:|---|
| 27 Graph Data Contract and Whole-Document Graph View | REQ-001..REQ-011, REQ-019, REQ-020, REQ-021, REQ-022, REQ-023 foundation/node backfill | present | present | satisfied |
| 28 Selection Detail, Local Filter, Chrome Polish, and E2E Hardening | REQ-002, REQ-007..REQ-010, REQ-012..REQ-023 edge overlay | present | present | satisfied |

## Requirement Cross-Reference

| Requirement | Traceability | Verification | Final status |
|---|---|---|---|
| REQ-001 - REQ-006 | Phase 27 | graph contract, normalization, source chunk mapping, typed/mixed/embeddings-only modes | satisfied |
| REQ-007 - REQ-011 | Phase 27 | whole-document summary, attention, sections, grouping, Top-N/filter scoping | satisfied |
| REQ-012 - REQ-015 | Phase 28 | section selection view, status notes, claims, expandable edge details | satisfied |
| REQ-016 - REQ-018 | Phase 28 | renderer-local text filter, correct data fields, no-results behavior | satisfied |
| REQ-019 | Phase 27/28 | preview section navigation and edge target opening | satisfied |
| REQ-020 | Phase 27/28 | dock-native accessible layout, theme tokens, overflow coverage | satisfied |
| REQ-021 | Phase 27/28 | loading, unavailable/no-vault, malformed, partial graph metadata recovery | satisfied |
| REQ-022 | Phase 27/28 | accurate chrome counts and independent config/filter state | satisfied |
| REQ-023 | Phase 27/28 | typed `query_graph` IPC/preload/provider path plus node and edge metadata overlay without credential exposure | satisfied |

No orphaned v1.6 requirements were found.

## Cross-Phase Integration

The required `gsd-integration-checker` returned **PASS** for phases 27 and 28 at `0b92d52`.

| Seam | Result | Evidence |
|---|---|---|
| `get_document` graph summary/connections to provider modes | PASS | Provider consumes document connections and derives typed/mixed/embeddings-only graph modes. |
| `query_graph` main/preload bridge to provider node backfill | PASS | Typed IPC/preload path is validated and renderer-facing only through `flashqueryQueryGraph`. |
| Phase 27 node metadata to Phase 28 selection detail | PASS | Selection view reads node metadata for summary, community context, status notes, and claims. |
| Phase 28 edge metadata overlay | PASS | Edge metadata merges by edge `id` and feeds claim-linked selection rows and expanded edge details. |
| Preview and target navigation | PASS | Section navigation uses the preview selection store; selection edge rows reuse the existing open-target route. |
| Local filter behavior | PASS | Filter state is renderer-local, does not reload FlashQuery, and is covered by E2E call counters. |
| Chrome count/config/filter state | PASS | Counts publish pre-filter active totals and config/filter active indicators remain separate. |
| Deterministic Electron E2E | PASS | Graph panel and inspector target-opening specs are enabled and passing, including detached-window coverage. |

## Gap Document Review

Reviewed product gap document:

`/Users/matt/Documents/Claude/Projects/FlashQuery/flashquery-product/Product/Cate/Monaco Extension Testing/Graph Explorer/Graph Intelligence Monaco Side Panel Gaps.md`

Phase 27 records two gap resolutions under `AI Dev Agent Resolution`:

| Gap | Audit result |
|---|---|
| Gap 1 stale/deleted edges leaking into user-facing lists/counts | resolved; active connection filtering now feeds relation filtering, ordering, visible rows, toolbar count, connected markers, and section tallies. |
| Gap 2 missing Phase 27 test IDs and inaccurate verification evidence | resolved; the missing IDs were restored and verified during gap closeout. The temporary phase-specific npm guard was later retired to keep package scripts focused on normal test suites. |

Phase 28 records thirteen gap resolutions under `AI Dev Agent Resolution`, all verified as closed at `0b92d52`:

| Gap | Audit result |
|---|---|
| F-01 lint errors in Phase-28-touched files | resolved for scoped files; targeted ESLint passed, while unrelated global lint debt remains tech debt. |
| F-02 pending-analysis affordance hidden behind `hasAttention` | resolved; attention and section rows show pending analysis while node metadata loads. |
| F-03 staleness-risk false note for `none`/`unknown` | resolved; durable staleness notes are gated and labeled. |
| F-04 `T-C-034` deferred/resolved status coverage | resolved; dedicated coverage now exercises deferred/resolved statuses and whole-document attention invariants. |
| F-05 detached dock-window target-opening skip | resolved; detached-window E2E is enabled and deterministic. |
| F-06 `T-C-030` double-booking | resolved; stale/deleted-edge behavior uses canonical `T-C-042`, and `T-C-030` is pinned to REQ-012. |
| F-07 missing Phase 28 ID gate | resolved during gap closeout; the temporary phase-specific npm guard was later retired to keep package scripts focused on normal test suites. |
| F-08 cross-document selection edge opening coverage | resolved; component coverage verifies cross-document target opening from selection rows. |
| F-09 selection-view chrome connection count coverage | resolved; pre-filter active selection count is covered. |
| F-10 vacuous `T-E-004` fallback assertion | resolved; E2E opens config before asserting graph-only controls are absent. |
| F-11 hardcoded score colors | resolved; `ScorePie` uses Cate theme tokens. |
| F-12 narrow-width no-overlap execution gap | resolved; E2E checks real rendered horizontal overflow. |
| F-13 minor forward-compat coverage seams | resolved; coverage now pins general sort, node `include_content`, structured question claims, and unknown certainty/staleness behavior. |

## Verification Evidence

Fresh audit-run gates:

```bash
npm run typecheck
```

Result: passed. The temporary Phase 27/28 ID gates had already served the gap-resolution audit and were later retired.

Node runtime enforcement added after the original audit:

```bash
npm run typecheck
```

Result under default `/usr/local/bin/node` `v26.0.0`: failed before running TypeScript with "Unsupported Node.js version 26.0.0. Cate requires >=20 <23."

Global lint debt resolved after the original audit:

```bash
npm run lint
npm run typecheck
npm run test:unit
npm run test:e2e
```

Result under Node `v20.20.2`: lint passed with zero warnings, typecheck passed, the full unit suite passed 112 test files / 1100 tests with 3 skipped, and the full E2E suite passed 88 tests with 2 skipped.

Additional verification from the v1.6 gap closeout and integration checker:

```bash
npm run test:unit -- src/renderer/lib/semanticConnectionsProvider.test.ts src/renderer/lib/semanticConnections.test.ts src/renderer/panels/SemanticConnectionsPanel.test.tsx
npm run test:e2e -- e2e/semantic-connections-graph.spec.ts
npm run test:e2e -- e2e/semantic-connections-inspector.spec.ts
```

Results: focused unit/component graph suites passed, graph E2E passed 5/5, and inspector E2E passed 12/12.

## Nyquist Coverage

| Phase | VALIDATION.md | Classification | Note |
|---|---|---|---|
| 27 | present | PARTIAL | Validation strategy exists but frontmatter remains `status: draft` and no `nyquist_compliant: true` flag is present. |
| 28 | present | PARTIAL | Validation strategy exists but frontmatter remains `status: draft` and no `nyquist_compliant: true` flag is present. |

Both phases have validation artifacts and passing evidence, but neither declares formal Nyquist compliance.

## Tech Debt

| Area | Item |
|---|---|
| Planning metadata | `.planning/STATE.md` frontmatter still says `completed_phases: 1` and `percent: 50`; roadmap and `gsd-sdk query init.milestone-op` report both phases complete. |
| Nyquist metadata | Phase 27 and Phase 28 validation files are present but still `status: draft` without `nyquist_compliant: true`. |

## Verdict

`passed_with_tech_debt`. The Graph Intelligence Monaco Side Panel milestone is functionally complete and cross-phase integrated. No current code or requirement gap blocks milestone archival; the remaining work is audit hygiene and repo-wide maintenance.
